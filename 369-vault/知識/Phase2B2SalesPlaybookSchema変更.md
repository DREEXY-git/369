# Phase2B2SalesPlaybookSchema変更

> 目次に戻る → [[index]] ／ 関連 → [[Phase2B1SalesPlaybook設計]] ・ [[Phase2A2Schema変更]] ・ [[PhaseX04本番スモーク定型化]]
> コード側の正: `369/docs/audit/52_phase2b2_sales_playbook_schema_change.md`（Phase 2-B-2）

## 結論（1行）

**営業プレイブックの「器」（SalesPlaybookEntry テーブル）ができた — 設計図（doc51）どおり・既存の棚は無傷・ローカル検証は全green・本番反映と本番確認はこれから（判定 GO）。**

## 何をしたか（非エンジニア向け）

- データベースに新しいテーブルを1つ追加した。**追加だけ**で、既存のテーブル・データに触る種類の命令（削除・変更）はゼロであることを機械チェックで確認済み。
- 事前にあなたが決めた3点（呼称=Phase 2-B のまま／関連参照=ID配列／型の種類=切り口・反論対応・提案準備・トークの4種）をそのまま採用した。
- 「言ってはいけないこと」欄（doNotSay）・「外部AI送信は既定でオフ」など、設計段階の安全装置がすべて器に入った。

## 検証（2度目の schema 変更も同じ型で）

- 接続先が自分のPC内のDB（localhost）であることを**値を見せずに**確認してから実行（本番DBには触っていない）。
- prisma validate → ローカル migration 適用 → テスト211本 → typecheck → lint → build、すべて green。
- [[Phase2A2Schema変更]] で確立した手順（migrate コマンドの罠回避を含む）がそのまま再利用でき、迷いなく完走した。**初回に作った「型」は2回目から配当を生む**。

## まだやっていないこと

- 画面なし・デモデータなし・AI参照なし（2-B-3〜2-B-5・各別承認）。
- **本番反映と本番確認が未実施**: main push（別承認）→ Vercel が本番DBにテーブルを作る → [[PhaseX04本番スモーク定型化]] の型で利用者実測（新画面が無いのが正常・既存画面の無回帰が確認点）。

## 次は人間の判断

1. schema変更 commit の main 反映（push 承認）。
2. 本番確認の実測（5分・既存画面がいつも通りか）。
3. Phase 2-B-3（見える化＋デモデータ）の承認。

## 関連ノート

- [[Phase2B1SalesPlaybook設計]] — この器の設計図。
- [[Phase2A2Schema変更]] — schema 変更の型と罠の初出。
- [[PhaseX04本番スモーク定型化]] — 本番確認で使う型。
