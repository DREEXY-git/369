# 61. Phase 2-B-5-PROD — SalesPlaybookEntry AI参照の本番確認記録（判定 GO・一度不完全報告→再実測で確定）

> Phase 2-B-5-PROD-RECHECK（docs-only）。対象は Phase 2-B-5 実装 commit `83d35bc`（main 反映済み・doc60）。
> 本確認は**利用者の Vercel 画面・本番画面の実測**によるものであり、**AI が本番接続確認したものではない**。
> 実測値はチャット提出（初回報告→read-only 切り分け→再実測→検索質問文の追補、の4段階で完成）。実測日を含む各値は利用者申告値をそのまま記録する。

---

## 1. 非エンジニア向け要約

- **AIが営業プレイブックを読む機能が、本番で動くことを利用者実測で確認できました**。判定は **GO** です。
- 決め手は2点: ①ナレッジ検索の「**参照した会社の頭脳**」に、本番で作った営業プレイブックのタイトルが表示された ②**AI参照の監査ログ（ai_reference・対象 SalesPlaybookEntry）が `/admin/data-access-logs` に記録されていた**。
- これで **Phase 2-B-5 は本番確認まで完全クローズ**。「最新の本番確認GO済みプロダクト基準」を **Phase 2-B-5 / `83d35bc`** に更新します。
- 途中で「ai_reference が見当たらない」という報告がありましたが、原因は**見る場所**（監査ログ本体ではなく機密参照ログ/`/admin/data-access-logs`）で、再実測により機能自体は正常と確認されました。

## 2. 経緯（初回報告 → 切り分け → 再実測）

1. **初回報告（部分的・GO保留)**: 「ai_reference が監査ログに見当たらない」「全部68点・テンプレート回答」「Supabase にエラーログ」等の報告。GO 記録は作成せず停止。
2. **read-only 切り分け（repo実測・コード修正なし）**:
   - **68点・テンプレート回答は FakeLLM の仕様**（本番は AI キー未設定のためデモ用AIで動作。ナレッジ検索の信頼度は FakeLLM では 0.68 固定＝表示は常に **68**%。実LLM接続は外部送信の解禁とセットの別承認）。
   - Supabase のエラー（`supabase_migrations.schema_migrations` 不存在・source: dashboard）は **Supabase 管理画面自身のクエリ**であり、Prisma 管理の本アプリとは無関係の可能性が高い（断定しない・アプリ影響なし）。
   - ai_reference の表示場所は監査ログ本体ではなく **`/admin/data-access-logs`**（紫の「AI参照」バッジ）または /admin/audit 下部の機密参照ログ（最新30件）。
   - ai_reference は「参照した会社の頭脳」に実際に表示された時のみ記録される。**アーカイブ済みプレイブックは参照対象外**。
   - 会社方針・商品カタログ画面から営業プレイブックへのリンクが無いのは**現状仕様**（回帰ではない・改善候補）。
3. **再実測（利用者・2026-07-04 申告）**: 下表のとおり全項目 OK → **GO**。

## 3. 利用者再実測値（2026-07-04 申告・チャット提出）

| # | 項目 | 実測値 |
|---|---|---|
| 1 | Vercel | Ready / green / build成功 |
| 2 | latest commit | `83d35bc` |
| 3 | ログイン・ナビ・/brain/playbooks | すべて OK |
| 4 | 使用した営業プレイブック | **本番UIで新規作成**（**本番に実在する営業プレイブック**・未アーカイブ）・タイトル「**テスト1**」 |
| 5 | 入力安全性 | OK（架空内容のみ・顧客名/実会社名/個人名/成果数値/口コミ/顧客の声/実価格なし） |
| 6 | ナレッジ検索の質問 | 「**test1**」（追補で提出・作成したテストデータの主要語句） |
| 7 | AI回答 | 表示された |
| 8 | **「参照した会社の頭脳」** | **表示された・上記プレイブックのタイトルも表示された** |
| 9 | **`/admin/data-access-logs`** | **確認した。紫の「AI参照」（action=ai_reference）の行があり、entityType は SalesPlaybookEntry だった** |
| 10 | 既存画面（会社方針・商品カタログ・ナレッジ検索・顧客・LeadMap） | すべて OK（無回帰） |
| 11 | エラー画面 | なし |
| 12 | 外部送信なし・SNS投稿なし・口コミ投稿なし・顧客の声公開なし・ENSHiN OS 外部発信なし | 確認済み |
| 13 | 68点・テンプレ回答 | **FakeLLM 仕様として理解済み** |
| 14 | AI非確認注記 | あり（利用者自身の本番画面実測・AIが本番接続確認したものではない） |

- 注記: 実測日は利用者申告値をそのまま記録（本プロジェクトの全記録で同方針・前後関係の解釈は申告値を正とし AI は断定しない）。

## 4. 判定

- **判定: GO**（GO 条件すべて充足・NG/未確認 0件）。
- AI参照の一連の流れ（プレイブック作成 → 検索 → 参照表示 → **writeAIDataAccess による ai_reference 記録**）が本番で end-to-end に動作した。監査ログの粒度（レコードごと・entityType=SalesPlaybookEntry）も設計（doc59）どおり。
- **「最新の本番確認GO済みプロダクト基準」を Phase 2-B-5 / `83d35bc` に更新する**（前基準 Phase 2-B-4 / `26a7a30` は履歴として保持）。
- **Phase 2-B-5 は本番確認まで完全クローズ**。externalAiAllowed は全件 false のまま＝外部LLMへの送出は構造的にゼロを維持。

## 5. 今回あわせて記録する改善候補（いずれも別承認・本判定には影響しない）

1. 会社方針・商品カタログ画面 → 営業プレイブックへのタブ導線追加（現状は片方向）。
2. 「アーカイブ」の文言説明・色/フォントの視認性改善（Company Brain 3画面）。
3. 実LLMキー設定（=テンプレ回答の解消。外部送信解禁とセットの重い承認・maskText/externalAiAllowed ゲートの再確認込み）。

## 6. 安全確認（今回の記録作業で行っていないこと）

- docs-only: コード変更なし・DB操作なし・本番DB操作なし・migrate deploy 手動実行なし・Vercel設定変更なし。
- 外部送信なし・SNS投稿なし・口コミ投稿なし・顧客の声公開なし・**ENSHiN OS 外部発信なし**・**Phase 8（実課金）なし**・MCP/API公開なし。
- 外部LLM送信解禁なし・externalAiAllowed true UI なし・高機密ラベル対応なし。
- push なし（commit-only・main push は別承認）。

## 7. 次アクション（いずれも別承認）

1. 本記録 commit の push-only（feature＋main）。
2. **Phase 2-B 全体クローズ判定**（doc48 の 2-A-CLOSE と同じ型・doc62 候補）。今回は進まない。
3. §5 の改善候補の採否判断。
- 参照: 実装=doc60／設計=doc59／本番確認の型=doc49／前例=doc47（AI参照の本番確認・データ前提差の教訓）。
