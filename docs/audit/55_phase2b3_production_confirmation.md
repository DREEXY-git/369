# 55. Phase 2-B-3 本番確認記録 — SalesPlaybookEntry read-only 可視化（利用者再確認・判定 HOLD）

> docs-only / production confirmation record。**コード・DB・schema・migration の変更は一切なし。**
> フェーズ: Phase 2-B-3-PROD / 現在位置は git refs を正とする。
> 対象 commit: `a2bb2b6`（SalesPlaybookEntry read-only 可視化。main 反映済み）。
> **判定: HOLD（2026-07-05・利用者実測）。**
> 訂正の経緯: 本書は当初、初回報告「全てGO」に基づく GO 記録として作成されたが、**main 反映前に利用者の再確認で「営業プレイブックが表示されていない」と報告された**ため HOLD へ訂正（`461d008`）。その後の**詳細な最新実測で、症状は「ナビに表示されない」ことに絞られた**（本改訂で反映）。**前回GO記録は main 未反映のまま取り消し相当**（feature branch 上の履歴としては残る・上書きしない）。

---

## 1. 非エンジニア向け要約

- 詳細な再実測の結果、状況がはっきりしました: **画面そのもの（/brain/playbooks）は正常に動いています**。URL を直接開くと画面が開き、空一覧（本番は seed 未実行のため正常）・閲覧専用（ボタンなし）も設計どおりです。
- **問題は1点だけ: ナビ（メニュー）に「営業プレイブック」が表示されていない**ことです。GO 条件の1つ「ナビに表示される」を満たさないため、**判定は HOLD** です。
- 症状が「画面全体の不達」ではなく**ナビ表示の問題に絞られた**のは大きな前進です。既存画面はすべて正常・エラーなし・外部送信なしで、壊れているものはありません。
- **原因はまだ断定しません**。次は read-only の原因調査です（コード修正はまだしません）。**GO済み基準は Phase 2-B-2 / `811b8c6` のまま動かしません**。

## 2. 本番確認記録（利用者実測・2026-07-05）

**本確認は利用者の Vercel 画面・本番画面の実測によるものであり、AI が本番接続確認したものではない。** AI は本番DB・本番環境に直接触っていない。

### 2-1. 実測の時系列

1. **初回報告**: 「全てGO」（ナビ表示も OK と報告）→ GO 記録を作成（`6c1b5e9`・feature のみ・main 未反映）。
2. **再確認**: 「営業プレイバックは再確認をしたら表示されていない」→ GO 差し止め・HOLD 訂正（`461d008`）。この時点では表示されない箇所は未特定。
3. **最新実測（詳細・こちらが最終）**: 下表のとおり。**症状はナビ表示に絞られた**。

### 2-2. 最新実測（詳細）

| 項目 | 利用者実測値 | 判定 |
|---|---|---|
| Vercel build / deploy | Ready / green | OK |
| Vercel latest commit | `a2bb2b6` | OK |
| 本番ログイン | OK | OK |
| **ナビ「営業プレイブック」の表示** | **NG（表示されていない）** | **NG** |
| /brain/playbooks（URL 直打ち） | **開く** | OK |
| 一覧の状態 | **空一覧が表示される（本番は seed 未実行のため正常）** | OK |
| 作成・編集・アーカイブボタン | なし（read-only として正常） | OK |
| 既存画面（会社方針・商品カタログ・ナレッジ検索・顧客・LeadMap） | いつも通り | OK |
| エラー画面 | なし | OK |
| 外部送信・SNS投稿・口コミ投稿・顧客の声公開の不発生 | OK（発生なし） | OK |
| **総合判定** | GO条件「ナビに営業プレイブックが表示される」を満たさないため | **HOLD** |

## 3. 症状の整理と原因について（未特定・断定しない）

**確定した症状**: ルート・画面・権限ガード・データ表示は本番で正常に機能している（直打ちで開き空一覧＝実装どおり）。**NG はナビの1項目の表示のみ**。

**原因は未特定**。次の read-only 原因調査で切り分ける。調査の入口になり得る観点のみ列挙する（いずれも仮説であり断定しない）:

- **キャッシュ／反映タイミング**: ナビは全画面共通の部品のため、ブラウザ／CDN が古い版を保持している可能性（2-A-3a の doc37→38 と同型の前例。初回報告で「表示された」ことがある点とも整合し得る）。ハードリロード・別ブラウザでの再実測が有効。
- **repo側の確認点（read-only）**: nav.ts の該当1行の main 反映内容の再確認・ナビ表示の権限フィルタや表示条件の有無（「会社の頭脳」は表示されている点との差分）。
- 調査では本番再実測（ハードリロード・別ブラウザ・ログアウト/ログイン）と repo 側 read-only 確認を行う。**DB・認証・RBAC・本番環境・Vercel 環境変数は変更しない。コード修正なし。**

## 4. 変更していないもの・発生していないこと

- コード修正・nav.ts 修正・schema / migration / seed / rbac / labels / package / lock: **無変更**（本記録は docs-only）
- 作成・編集・アーカイブ実装・Server Action・writeAudit・writeDataAccess・AI参照: **なし**
- 外部LLM送信解禁・高機密ラベル対応・externalAiAllowed true UI: **なし**
- **ENSHiN OS 外部発信・口コミ投稿・SNS投稿・顧客の声公開・推薦コメント掲載: なし**（外部送信なし）
- **Phase 8（課金）: なし**・MCP/API公開なし・本番DB操作なし

## 5. 判定と次アクション

- **判定: HOLD（Phase 2-B-3 本番確認・2026-07-05・利用者実測）。** 理由: GO 条件の1つ「ナビに営業プレイブックが表示される」を満たさない（それ以外の項目はすべて OK）。
- 「最新の本番確認GO済みプロダクト基準」は **Phase 2-B-2 / `811b8c6` のまま更新しない**。Phase 2-B-3 / `a2bb2b6` は「main反映済み・本番確認HOLD（症状=ナビ表示のみ）」の状態。
- 次アクション:
  1. 本HOLD記録の main 反映（push-only・別承認・GO記録 `6c1b5e9`＋HOLD訂正 `461d008`＋本精密化のセット）。
  2. **Phase 2-B-3 の read-only 原因調査**（別ミッション。本番再実測=ハードリロード・別ブラウザ＋repo側 nav の read-only 確認。コード修正はしない）。
  3. 原因特定後に必要なら最小修正 → 再実測 → GO記録（doc56 候補）。
- **GO記録の作成・原因修正・Phase 2-B-4 には進まない。**
- 参照: 実装=doc54／HOLD→解消の前例=doc37→38（2-A-3a・キャッシュ起因＝今回と同型の可能性）・doc46→47（2-A-3c-2・データ前提差）。
