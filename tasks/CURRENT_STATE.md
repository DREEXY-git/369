# CURRENT_STATE — IKEZAKI OS

> 現在地の「1枚サマリー」。今の真実だけを書きます。長い経緯は `tasks/PROGRESS.md`、詳細監査は `docs/audit/` を参照。

## 状態管理ルール

- このファイルは**現在地の1枚サマリー**であり、履歴の集積ではありません。
- **push 反映状態は git refs（`git rev-parse HEAD` / `git rev-parse origin/main` / `git log origin/main`）を正**とします。
- `PROGRESS.md` は**履歴・判断メモ**であり、現在地の唯一の正ではありません（現在地はこのファイルと git refs）。
- 一時的な状態（未push・承認待ち 等）は**原則として永続表現に残しません**。必要なときは最終報告で扱います。
- このファイルは**大きな節目ごと**に更新します（毎コミットでは更新しません）。

## Git 反映状態の扱い

- **現在の HEAD ／ origin/main ／ 未push有無 ／ 作業ツリー状態は、常に git コマンドの結果を正**とします。このファイルには、コミットごとに変わる現在値を固定しません（このファイル自身を更新すると位置が変わるため、固定値を書くと即座に古くなります）。
- 反映状態を確認するコマンド:
  - `git rev-parse --short HEAD`
  - `git rev-parse --short origin/main`
  - `git log --oneline origin/main..HEAD`（未pushの一覧。空なら未pushなし）
  - `git status --short`（作業ツリー。空なら clean）
- 現在の作業ブランチも `git branch --show-current` を正とし、このファイルには固定しません。
- **このファイルに固定してよい commit は、「Phase 完了基準」と「最新の本番確認GO済みプロダクト基準」の基準 commit だけ**です。現在 HEAD・origin/main・作業ブランチ・未push などの現在位置は git を参照してください。

## Phase の現在地

- **【最新・本番統合】Phase 3.5/4 完成済み4機能を app main へ順次統合＝本番反映完了（2026-07-12・記録: PR #33=RC / #34=C19 / #35=P4+CP+WF・base commit `c8dc1f4`）**: RC #32（Phase 3＋C21）本番リリース後、完成済み4機能を **①C22 → ②C19 → ③Phase4+ControlPlane+Workflow** の順で main へ統合し Vercel Production へ自動デプロイ。**C22**（`/growth/referral` 顧客名取得段階遮断 read-only・main `7efb22b`）／**C19**（広告改善案 承認ブリッジ・決定的PK・`MarketingSuggestion.approvalStatus` 列を非破壊追加・main `acdd090`）／**③**（**Phase4 ai_run_resume**＝AI承認ゲートの人間approve/reject・**承認しても run は「再開待ち(QUEUED)」に置くのみで実行/外部送信/DB変更ゼロ**・単一transaction＋isAi二重防御／**Control Plane** `/ai-control` read-only／**Workflow Dry Run** `/workflows` read-only・危険Action BLOCKED・未知はfail-closed停止・client-local state・main `c8dc1f4`）。③は追加マイグレーションなし（DBスキーマ変更ゼロ）。各統合で unit **540/0**・typecheck/lint/build clean・E2E（実PG・実UI）緑・CI stage1＋stage3_e2e success を確認（③ CI で1件 flake＝C19 `ads_suggestion_bridge:87` の既知並行timing・再実行で緑・コード変更なし）。**外部送信/実LLM/課金/実支払/削除は一切なし**。Codex 独立監査は使用制限のため来週へ後ろ倒し（人間 Preview 確認をゲートとして先行・後日 retroactive cross-check）。禁止Gate（本番DB破壊/Production migration/seed/reset/Secrets/実顧客PII/外部送信/課金）は非接触。
- **Phase 1: 正式完了（Phase 1-50・判定根拠は doc24 の GO）。完了基準 commit: `e95f887`**（※現在 HEAD ではなく完了基準。詳細 `docs/audit/25_phase1_completion_record.md`）。
- **Phase X: 完了済み（Phase X-CLOSE-01・判定 GO）。完了基準 commit: `70d4d06`**（※現在 HEAD ではなく完了基準。詳細 `docs/audit/32_phase_x_completion_record.md`）。恒久資産=E2E smoke green 回帰ゲート（11/11）＋roadmap 9本＋Feature Registry＋各種 Matrix＋Phase 2 entry review。
- **Phase 2-A: 正式完了（Phase 2-A-CLOSE・判定 GO・2026-07-04）。完了対象の最新本番確認GO済みプロダクト基準: Phase 2-A-3c-2 / `85f1bf3`**（詳細 `docs/audit/48_phase2a_completion_record.md`・doc14 §45）。Company Brain foundation＝「器（schema）→ read-only 可視化 → 人間書き込み2テーブル → AI参照＋ai_reference ログ」がすべて本番確認GOまで完了。HOLD 2件は追記主義で解消済み。高機密・外部LLM解禁・3c-5・後続3テーブル・Phase 8・ENSHiN OS外部発信は後続別承認。以下は Phase 2-A 各段の当時記録。
- **Phase 3（AI Growth Engine）: 進入 GO（人間判断・2026-07-08・記録: doc145＋`docs/roadmap/46`）。ただし Phase 3 GO は実装開始ではない**。GO 根拠＝回帰ゲート緑（stage3_e2e 72 passed/0 failed・run 28930122157/28934614261/28937029131 の3 run 連続 success）／C=0/D=0/F=0／F3 seed・schema 不要／redaction 健全（スタッフに原価・粗利の実値露出なし）／安全封印維持（EXTERNAL_SEND_ENABLED=false・FakeLLM・externalAiAllowed 既定 false・SuppressionList 強制）。人間 GO 判断6件＝高機密 runtime 統制②据え置き GO／outreach opt-out 正式化 GO／positive Consent 用途別分離 GO／回帰ゲートは CI 実測で充足 GO／外部送信・実LLM・課金・本番 deploy は個別承認制維持 GO／Phase 3 最初の縦切り＝**AI Growth Opportunity Control Tower v0**。**外部送信・実LLM・課金・本番 deploy・runtime 解禁・externalAiAllowed true・EXTERNAL_SEND_ENABLED true は Phase 3 GO 後も各々個別の人間承認まで行わない**。次は AI Growth Opportunity Control Tower v0 設計ミッション（docs-only）。
- 2-A-3a（Company Brain read-only 可視化）: seed に架空デモデータ（CompanyPolicy 5件＋ProductCatalogItem 8件・全件 externalAiAllowed=false・PII/secret/実価格なし）が入り、read-only 一覧2画面（`/brain/policies`・`/brain/catalog`・knowledge:read＋tenantId スコープ）とナビ1行（会社の頭脳）が実装済み（記録: doc36）。**smoke は12本体制で 12/12 green（既存11本回帰なし）**。schema・migration・RBAC・labels は 2-A-2 から無変更。**作成・編集・Server Action・writeAudit/writeDataAccess の本実装は 2-A-3b の個別人間承認まで行わない**（read-only 可視化完了、作成/編集は 2-A-3b 承認待ち）。前段: 2-A-2 schema 変更＋本番確認 GO（doc34・doc35・doc14 §38）。
- **2-A-3a の本番確認は 一度 HOLD → 再実測 GO で解消済み（利用者実測・2026-07-03・記録: doc38＋doc14 §40）**: 初回実測ではナビ「会社の頭脳」と `/brain/*` 2画面が本番未確認/NG で HOLD（doc37＋doc14 §39・記録として保持）。ハードリロード/開き直し後の再実測で、ナビ・2画面・作成/編集/削除ボタン無し・既存画面すべて GO を確認し解消。前回NGの原因はキャッシュ/反映タイミングの可能性が高いが断定しない。**Phase 2-A-3a は本番確認まで完全クローズ**。
- **2-A-3b-1（CompanyPolicy 書き込み最小実装）＋安全補正＋本番確認GO 完了（記録: doc39＋doc40＋doc41）**: 会社方針のみに作成・編集・アーカイブの3操作を実装（Server Action＋入力検証＋writeAudit・物理削除なし・externalAiAllowed は UI で変更不可）。安全補正で **AIロールは権限にかかわらず会社方針の mutation を一律拒否（rbac 無変更・actions 側で人間専用化）**・**扱える label は NORMAL / INTERNAL のみ（高機密ラベルは writeDataAccess 実装時まで保留）**。**本番確認も利用者実測で GO（2026-07-04・doc41＋doc14 §41）＝書き込み第一段は完全クローズ**。
- **2-A-3b-2（ProductCatalogItem 書き込み最小実装）＋本番確認GO 完了（記録: doc42＋doc43＋doc14 §42）**: 商品カタログに同じ型で3操作を実装。**安全境界（AI mutation禁止・label 2択・externalAiAllowed 封印・ソフトアーカイブ）を最初から組み込み、修正ループ0回で完走**。**priceNote は説明テキストのみで請求・課金・見積・会計に接続しない**。**smoke は14本体制で 14/14 green（既存13本回帰なし）**。**本番確認も利用者実測で GO（2026-07-04）＝Company Brain の2テーブルの人間書き込みは本番確認まで完了**。
- **2-A-3c-1（AI参照経路＋writeDataAccess 設計・docs-only）完了（記録: doc44）**: AI が Company Brain を読む段の設計を固定。参照範囲=tenantId・archivedAt:null・NORMAL/INTERNAL のみ／外部LLM送信は externalAiAllowed=true＋maskText 済みのみ（true UI 無し＝構造的にゼロ）／記録は ai_reference をレコードごと1件／第一接続タスクはナレッジ検索。
- **2-A-3c-2（Company Brain AI参照の最小実装）完了 — 一度 HOLD → 再実測 GO で解消済み（実装: doc45／HOLD: doc46＋doc14 §43／解消GO: doc47＋doc14 §44）**: ナレッジ検索のみに Company Brain 参照を注入（read-only・NORMAL/INTERNAL・canAccessLabel・外部LLM時は externalAiAllowed ゲートで注入ゼロの安全側デフォルト）。初回本番確認（2026-07-04）は「値引き承認ルール」の AI回答・参照セクション未確認で HOLD。read-only 原因調査と利用者再実測（2026-07-04）により、**原因は本番データ前提差（対象 CompanyPolicy が本番に不在）でありコードのバグではない**と確定。本番UIで会社方針を作成後、AI回答・「参照した会社の頭脳」・参照元タイトル・CompanyPolicy の ai_reference ログすべて GO。**Phase 2-A-3c-2 は本番確認まで完全クローズ**。高機密ラベル・externalAiAllowed true UI・外部LLM送信の解禁は 3c-5 の個別人間承認まで行わない。ENSHiN OS の外部発信・口コミ・SNS・顧客の声公開・許諾管理実装は未着手。
- **CRM Customer / Contact 閲覧統制の追加監査（docs/roadmap/26 Candidate）: docs-only 完了（判定 B: HOLD・2026-07-06・記録: doc125＋`docs/roadmap/26`）／369-vault非編集・実装なし（コード差分ゼロ・read-only 監査）**。doc124 の未確認点を追加監査。**重要訂正**: doc124 が canAccessLabel 直 grep で見落としていたが、**Customer 詳細（`customers/[id]/page.tsx:30`）は `assertCanViewConfidential(user,{label: customer.label})`（ABAC・security/policy.ts）を実行しており、canAccessLabel（ラベル統制）＋DataAccessLog（confidential_view）が配線済み**（meetings/finance/invoices も同型）＝**Customer 詳細閲覧の高機密ラベル runtime 統制は閉じている**。tenantId スコープ・変更系は hasPermission('customer',...)＋writeAudit。**残るズレ/未確認**: Customer 一覧（`customers/page.tsx`）は行ごとの assertCanViewConfidential なし（LabelBadge のみ）・Contact 単体閲覧経路の統制未確認・LocalBusinessLead は leadmap 権限系（別体系）・doc110 標準閲覧式(customer-pain-access.ts の canViewCustomerPainDetail/isHumanUser)は apps 未接続。**明確な無統制経路は発見せず（C ではない）**。安全側で **判定 B: HOLD**（Customer 詳細は前進・一覧/Contact 未確認）。**Phase 3 進入は引き続き HOLD・人間 Phase Gate 承認事項**。**runtime 解禁なし・canAccessLabel/writeDataAccess 実装なし・schema変更なし・migrationなし・RBAC変更なし・company-brain-reference変更なし・leadmap/customers 実装変更なし・externalAiAllowed true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc125 push-only**（別承認）または Customer 一覧・Contact 単体閲覧統制の追加監査 docs-only。
- **v7.0 FINAL（Lane R1/R2 完了→PR #18 freeze・P4 Bridge 実装・P4Q 実 queue 証拠・C19 schema Gate 材料）（2026-07-12・記録: PR #18 comment 4950964048・roadmap82/83・branch `claude/p4-human-gate-resume-v1`）**: **Lane R2** = Codex P2 `r3565885993` へ実 Prisma/PostgreSQL 証拠12件（並行2申請・CAS後失敗/監査失敗の実rollback・並行approve/reject・冪等・対象消失・cross-tenant・AI拒否・正常系・outreach非干渉・PII非保存・外部作用0）を CI ephemeral PG 上で毎run実行・全て実DB最終状態re-fetchでassert。**Lane R1** = 継承mobile topbar P2を`ARTIFACT_ONLY / NOT_REPRODUCED_IN_HUMAN_PREVIEW`と整合記録の上、<sm優先順位表示（Bell/avatar/logout/build badge保証・theme/approvals/role textは≥sm＋drawer代替導線）＋320/360/390/430px probe E2E（boundingBox/overflow/accname/clean screenshot）で red→green。PR #18 fixed head **`c8b6065`**・CI `29190155634` success・unit 472/0・**E2E 145/0**・artifact `8259308684`（21 files）・freeze・`CLAUDE_FIXED_V70`投稿済み。**Lane P4**（roadmap82・schema変更不要と判定）= AI承認ゲートの人間approve（内部処理のみで再開完了・外部作用0）/reject（terminal・再開不可）を gate CAS→run遷移count===1→ApprovalRequest 1:1決定レコード→監査の単一transactionで実装（`ai-gate-bridge.ts`・/approvalsのread-only注記を判断UIへ）。契約unit 6件＋実PG/実UI 7件 green・フルE2E **152/0**（ローカル・使い捨てPG＋実chromium）。**Lane P4Q** = loopback Redis（port 6390・prefix v70test・persistenceなし）で BullMQ 実queue 証拠 **7/7 green**（retry・attempts上限failed telemetry・secret 0（failedReason/stacktrace/raw Redis全key走査）・重複jobId収束・承認前実行禁止・reject済みrun再開禁止・tenant別行のみ・worker再起動整合）。CI未収載のため `BULLMQ_REAL_QUEUE_EVIDENCE_GAP` はCI証拠としては維持（ローカル実測evidenceで縮小）。**Lane P35** = roadmap83 に C19 schema 案A/B比較（推奨=案A `MarketingSuggestion.approvalStatus`・Prisma差分/migration/rollback/backfill/テスト計画）と C22 前進判定（schema-free分析縦切りは可能・本サイクル未実装・受入/再開条件固定）を docs-only 正本化。schema適用なし。RC は `CODEX_RELEASE_PASS_V70` 未受領のため未作成。
- **Phase 3.5 承認ブリッジ実装（C21完了・C19停止）P35_READY（2026-07-12・記録: PR #18・roadmap81 §11・branch `claude/p35-approval-bridges-v1`・PR #14 非変更）**: roadmap81 §2/§3 を実装へ移行（V68 head `3d808a7` を通常 merge 後）。**C21 SEO/Content = 実装完了（review-only）**: `content_review` を `ApprovalAction` へ追加＋状態機械純ロジック `content-approval.ts`（unit 7）。`requestContentApprovalAction`（認証→`marketing:update` かつ人間→cuid 検証→tenant scoped 取得→**`ContentAsset.status`(draft/rejected→pending_approval) の条件付き updateMany を原子的ガード**にし CAS 勝者のみ `ApprovalRequest(content_review)` 作成・payload/DataAccessLog はメタのみ）。`decideApprovalAction` を **PENDING 限定 CAS 化**（冪等・二重 submit 防止・既存 outreach 二重送信レースも封鎖）＋`content_review` 分岐で ContentAsset を approved/rejected（**社内状態のみ・公開/CMS/外部送信/実LLM/課金/自動実行ゼロ**）。UI: `/marketing/content` 申請ボタン＋バッジ＋`?highlight`、`/approvals` 逆 deep link。E2E 3本（approve/reject 往復・重複 PENDING 1件のみ・AI ロール非表示）。**C19 広告改善案 = `SCHEMA_CHANGE_APPROVAL_REQUIRED` で停止（完成主張なし）**: `MarketingSuggestion` に可変 status 列が無く（§1 が列追加を回避）`ApprovalRequest` に unique 制約も無いため、重複 PENDING の原子的防止が既存 schema では不可能（check-then-create=TOCTOU のみ）。必要 schema 変更（案A: `MarketingSuggestion.approvalStatus`／案B: 部分 unique index）は roadmap81 §11 に固定・人間承認待ち。検証: unit **452/0**・tsc/lint/build/safety 0・diff-check/secret clean・CI run `29184051254` 両 stage success・**E2E `130 passed/0 failed`（ログ本文）**・artifact `e2e-screenshots-29184051254`（16 files）・env 封印（fake/log/false）。Draft PR **#18**（base=PR #14 branch）。main/Production/DB/Secrets/migration/seed/外部送信/実LLM/課金 非接触・禁止 path 非編集。Codex 監査待ち（`CLAUDE_P35_READY_V68`）。
- **v6.8 Grammar P1(object/array文法)封鎖・3D canvas URL同期 修復 進行中（2026-07-12・記録: PR #14・Codex 新規 Grammar P1/P2・recovery `claude/full-recovery-v61` head 更新中）**: **WIP-1 Grammar P1（release-blocking）**: v6.7 の tail 検証が **object と array を区別せず** comma 後を無条件に値位置扱いしていたため、`{"password":"abc","O0F144S68"}`（key 無し quoted value）・key 無し nested object/array・key without colon が漏洩（現 head 実測再現）。→ tail 検証を **container stack 付き bounded single-pass parser**（`buildContainerStack`＋`grammarTailValid`）へ置換。object=`AFTER_VALUE→(comma)→OBJ_KEY→OBJ_COLON→VALUE`、array=`AFTER_VALUE→(comma)→VALUE` を区別し、object key は quoted string 必須。値の直属 container を前置スキャンで stack 化し、全 container を閉じ切った残りは `afterContainerExit`（glued 継続弾き＋quote 無し純プローズのみ保持）。文法を確定できない字は末尾まで fail-closed。O(n)・ReDoS/再帰なし。否定テスト: keyless quoted/bare/nested/array・key without colon・colon without value・二重 comma・空白/tab/LF/CRLF・混在 backslash・nested・複数機密 key・unclosed・maxLen 200/8000・100KB を direct＋worker 3経路で sentinel 0＋旧 84/1152/1764/V67 whitespace 全回帰＋positive oracle（正規 field/nested/array/container 後 prose）保持。**WIP-2 3D canvas URL同期（P2）**: Three.js raycast click が `setSelectedId` を直接呼び `?agent=` 未更新だった問題を、**`selectAgentRef`（毎レンダ更新 ref）経由で 2D 一覧と同じ `selectAgent`（router.push）へ統一**。scene 再構築なし・cleanup/WebGL fallback 維持。E2E: 実 canvas click（`__nameplateProbe`/`__pickAt`）→detail ID＋URL 更新→back/forward 同期。ローカル全電池 unit **444/0**・tsc/lint/build/safety 0・diff-check clean・sentinel はテストのみ。Critical/High=0 は **Codex `CODEX_PASS_V68` 判定待ち**。main/Production/Vercel設定/DB/Secrets/外部送信/実LLM/課金 接触なし・禁止 path 非編集。**branch freeze**。Release Candidate は `CODEX_PASS_V68`＋`CODEX_EVIDENCE_READY_V68` 後。Phase 3.5 承認ブリッジは別 branch `claude/p35-approval-bridges-v1`（PR #14 非変更）で実装予定。
- **v6.7 残存Whitespace P1封鎖・tenant逆参照境界・完全Parity(直接照合)・真client nav 修復 進行中（2026-07-12・記録: PR #14・Codex 新規 space/LF P1・recovery `claude/full-recovery-v61` head 更新中）**: **WIP-1 残存P1（release-blocking）**: v6.6 の `tailIsSafeContinuation` が**値後の空白/LF tail を無条件に prose 扱い**し、任意 bare token＋balanced quote を安全 prose と誤認（`{"password":"abc" O0F144S66"x"}` / `\nO0F144S66"x"`）。現 head で **420/588 実測再現**→**container 文脈判定 `isInsideJsonContainer`** を導入し、値が `{`/`[` の内側なら空白後 bare も **構造検証**へ回す（正規の次 field/array/container 終端だけ保持・それ以外末尾まで fail-closed）。container 外の純プローズは **quote を含まない純テキストだけ保持**（`proseTailPureText`・引用符が現れたら安全境界を証明できず fail-closed）。container 出切り後も同規則で再帰判定。O(n)・ReDoS なし。否定テスト: 6keys×2quotes×3depths×7seps×7 malformed = **1764 matrix** を direct(8000/200)＋worker 3経路で sentinel 0＋旧 1152/6.6 broken 全回帰＋benign（container 外 quote 無し prose・container 終端後 prose）保持＋線形。**WIP-2 tenant 逆参照**: 一覧 run を own-tenant agent ID 集合で制限し、表示 agent 名を relation（tenant 非スコープ）ではなく tenant スコープ read model 由来へ。「own-tenant run が foreign agent を参照」fixture で一覧に run task・foreign agent 名が 0＋404。**WIP-3 完全 Parity/真 client nav**: 8名を `getAiCharacter(key)` 期待値へ**直接照合**（fullName/personality/skills(name:level)/traits/mistakes/eval/epithet）＋**portrait 決定論 fingerprint**（全 appearance 値・`data-portrait-fp`）を期待値と一致。`AiOffice` に選択→`router.push(?agent=)` を追加し、**画面上の社員選択（真の client nav）**で query 変更→back/forward・query 無し手動選択・無効 ID を検証。プロフィール要素の element screenshot（desktop/mobile・評価コメントまで可視）を artifact 化。ローカル全電池 unit **439/0**・tsc/lint/build/safety 0・diff-check clean・sentinel はテスト/コメントのみ。Critical/High=0 は **Codex `CODEX_PASS_V67` 判定待ち**。main/Production/Vercel設定/DB/Secrets/外部送信/実LLM/課金 接触なし・禁止 path 非編集。**branch freeze**。Release Candidate は `CODEX_PASS_V67`＋`CODEX_EVIDENCE_READY_V67` 後・main/Production は `HUMAN_PRODUCTION_GO_V67` まで禁止。
- **v6.6 新規P1(balanced-but-invalid tail)封鎖・AI社員完全Parity 修復 進行中（2026-07-12・記録: PR #14・Codex 新規P1 comment 3564933388・recovery `claude/full-recovery-v61` head 更新中）**: **WIP-1 新規P1（release-blocking）**: v6.5 の `tailIsStructurallyClean` が **quote 数の均衡だけ**で健全判定するため `{"password":"abc",O0F144S64"x"}` の `,O0F144S64"x"`（balanced-but-invalid）を偽 closer として信頼し `abc` のみマスク→`O0F144S64` 残存。現 head で **30/42 broken-matrix 漏洩＋exact P1** を実測再現→`scanValueEnd` の tail 検証を **文法的 bounded state machine**（`structuredTailValid`: EXPECT_SEP/EXPECT_VALUE＋depth＋JSON プリミティブ検証／`consumeQuotedString`／container 出切りは prose 委譲／prose は `proseTailClean`）へ全面強化。**候補 closer は後続 tail が「正規の次フィールド/配列要素/container 終端/record 終端」として解析できる場合のみ採用**、非プリミティブ bare・値の隣接・未閉 string・裸 escaped quote・途中切断（未閉 container）は末尾まで fail-closed。quote balance/parity/次文字/quote 個数/最後の quote 単独 heuristic 非依存・O(n)・ReDoS なし・文字列固有パッチなし。否定テストは「正規 record を separator/key/colon/quote 単位で壊す独立生成 matrix」（direct 8000/200・worker 保存/再throw/Action要約 3経路）で sentinel 0＋正規複数 field/primitive/nested/末尾 prose の保持＋線形性。**WIP-2 完全Parity**: 8名の name/epithet/personality/skills(name:level 直列)/traits/mistakes/evaluationNote を **詳細⇔3D の画面上実値で field 単位比較**（fingerprint は補助）＋portrait(SVG)＋全項目非空＋URL 同期/404/overflow 維持。**WIP-3**: v6.5 非退行（tenant 子境界・最小 select・metadata-only DataAccessLog・モバイルNAV 67・stale・build 情報・AI 権限封印）を既存 suite で再確認。ローカル全電池 unit **433/0**・tsc/lint/build/safety 0・diff-check clean・sentinel はテスト/コメントのみ。Critical/High=0 は **Codex `CODEX_PASS_V66` 判定待ち**。main/Production/DB/Secrets/外部送信/実LLM/課金 接触なし・docs/function-master・codex/**・.github・AGENTS・.codex・独立vault 非編集。**Codex 再監査まで branch freeze**。Release Candidate は `CODEX_PASS_V66`＋`CODEX_EVIDENCE_READY_V66` 後・main/Production は人間 `PRODUCTION_GO_V66` まで禁止。
- **v6.5 残存P1(汎用マスク)・tenant境界・人物同期・モバイルNAV受入 修復 進行中（2026-07-12・記録: PR #14・Codex v6.4 独立再監査 comment 4948356332＝High·P1 2・recovery `claude/full-recovery-v61` head 更新中）**: **WIP-1 汎用マスク漏洩（release-blocking P1）**: Codex が「同 depth 引用符 1 個＝一意 closer」規則に対し **偽 closer を開始と同 depth・後続 terminal quote を別 depth** にした生成 matrix で **84/84 sentinel 残存**（例 `{"password":"abc",O0F144S64\"}` → `O0F144S64` 漏洩）。現 head で **576/1152 実測再現**→修正: `scanValueEnd` の quotedish 分岐を **clean close（開始と同 depth の最初の閉じ引用符）＋後続 tail の構造検証**へ全面変更（tail に文字列外 escaped/別 depth quote or 未均衡があれば構造未確定＝**走査末尾まで fail-closed**・健全時のみ最小マスクで別フィールドを保持）。文字列固有パッチ・parity/次文字/quote数単独依存を廃止・線形（ReDoS/二次なし）。否定テストは sentinel matrix 全 1152 件（direct 8000/200・worker 保存/再throw/Action要約の3経路）＋decoy 内元秘密の 0 残存を直接 assert＋benign 保持＋v6.2/6.3/6.4 全回帰。**WIP-2 tenant境界（High: nested child isolation）**: `/ai-agents` list/detail の子（run/action/memory）を親 relation 任せにせず **各クエリに tenantId 明示**＋広域 include→最小 select（input/output/error 等の未使用 payload/PII/secret を取得しない）＋機密表示に `writeDataAccess`（メタのみ・件数と対象 ID のみ）。否定 E2E: agentId 一致・tenantId 別の越境子が詳細/一覧に漏れない ephemeral fixture。**WIP-3 人物 full parity＋AiOffice URL同期（P2）**: 8名の portrait(SVG)/profile 本文（性格）まで一覧/詳細/3D で値一致 assert・`AiOffice` に `useSearchParams` の反応 effect を追加し client-nav（?agent=A→B）とブラウザ back/forward で選択追従（無効・別テナント ID は無視＝存在を漏らさず・クエリ無しは手動選択を尊重）。**WIP-4 モバイルNAV受入**: `role=dialog`/`aria-modal`/初期フォーカス（drawer 内）＋閉時のトリガー復帰・`aria-expanded` 反応を追加。E2E: OWNER 67 導線の DOM 描画・overlay クリックで閉じる・背景スクロール復元・focus/aria・desktop 非退行。**WIP-5 Evidence語彙**: 非退行（roadmap80 定義語彙のまま・格上げなし）。ローカル全電池 unit **428/0**・tsc/lint/build/safety 0・diff-check clean・sentinel はテストのみ。BullMQ 実 queue は EVIDENCE_GAP 維持。Critical/High=0 は **Codex `CODEX_PASS_V65` 判定待ち**。main/Production/DB/Secrets/外部送信/実LLM/課金 接触なし・docs/function-master・codex/**・.github・AGENTS・.codex 非編集。**Codex 再監査まで branch freeze**。§12 Git 統合は `CODEX_PASS_V65` 後のみ。
- **v6.4 残存P1・モバイルNAV・AI社員表示・Evidence語彙 修復 進行中（2026-07-12・記録: PR #14・Codex `CODEX_REVIEW_RESULT_V63`＝CHANGES_REQUIRED・recovery head 更新中）**: **WIP-1 残存P1（release-blocking）**: 内部 escaped quote **depth 3**（`\"`）の直後を comma/space/semicolon/brace/bracket/LF にした5/6ケースで秘密残存を実測再現。原因は開始 depth1 と内部 depth3 を **parity（偶奇）で同一視**し、攻撃者操作可能な次文字を closing 判定に使用していたこと。修正: `scanValueEnd` を **実 backslash depth（個数）一致**で同レベル引用符を判定し、同レベルが**ちょうど1個なら一意に close・0/2個以上は fail-closed で末尾までマスク**、閉じ直後に glued token が続けば一緒にマスク（over-mask）へ変更。parity/closerFollows 依存を廃止。否定テストは depth-matrix（depth3×6区切り）＋空値→秘密＝fail-closed＋多フィールドJSON over-mask＋glued suffix＋worker 保存/再throw/Action要約 の sentinel 非残存を直接 assert。**WIP-2 モバイルNAV**: drawer が backdrop-blur header の containing block に閉じ込められ artifact 288×63px（NAV項目0）→ `createPortal(document.body)` で viewport 全体へ。E2E: drawer 高さ>700・深い導線（Operations実行）へ scroll＋click 遷移・Escape で閉じる。**WIP-3 AI社員表示/parity**: 長い permissionLevel を nowrap Badge から分離（短い自律ラベル「監督付き」＋折返しガードレール行）・desktop/mobile カード要素単位 overflow 検査・**8名全員の canonical key/fullName/state を一覧/詳細/3D の3画面間で値一致** assert（data-agent-key/state/name）・実在別tenant 404 維持。**WIP-4 Evidence語彙**: roadmap80 の定義外語彙 `CI_GREEN`/`REQUIREMENT_ONLY`/`IMPLEMENTED_ON_DRAFT` を定義済み `CI_VERIFIED`/`ROADMAP_ONLY`/`DRAFT_IMPLEMENTED` へ統一（Function-ID 未記載を格上げしない・SF/MF/freee/HR は段階実装対象に維持）。ローカル unit 423/0・tsc/lint/build/safety 0。BullMQ 実 queue は EVIDENCE_GAP 維持。Critical/High=0 は **Codex 判定待ち**。main/Production/DB/Secrets/外部送信/実LLM/課金 接触なし・docs/function-master・codex/**・PR#15・.github・AGENTS・.codex 非編集。
- **v6.3 P1 mask 2件クローズ 進行中（2026-07-12・記録: PR #14・thread PRRT_kwDOTBo7Kc6QIOwi/PRRT_kwDOTBo7Kc6QIOwj・recovery head `a280f12`）**: v6.2 の bounded scanner に対し Codex が新規 P1 2件を提起。**P1-1**: escaped-quoted 値の内部 delimiter（comma/semicolon/brace/bracket）で scanner が打ち切り suffix 残存（`{\"password\":\"abc,COMMASECRET\"}` → `COMMASECRET` 漏れ）。**P1-2**: quoted 値の改行で打ち切り2行目が保存・再throw 値へ残存（`{"password":"abc\nNEWLINESECRET"}` → `NEWLINESECRET` 漏れ）。両方を現 head で実測再現→修正: `scanValueEnd` の quotedish 分岐を「開始引用符と同じ escape レベルの対応する閉じ引用符まで消費（内部 delimiter/改行/エスケープ引用符は読み飛ばす・閉じは直後が空白/構造区切り/末尾のものだけ有効・破損連続 escaped は fail-closed で末尾まで）」へ変更。global unescape へ戻さず linear-time 維持。否定テストは sentinel（COMMASECRET/SEMICOLON/BRACE/BRACKET/NEWLINE 等・prefix/suffix 含む元秘密）が出力・保存・再throw に一文字列として残らないことを直接 assert＋100KB bounded＋過剰マスクなし＋v6.2 全ケース回帰。unit 411/0・tsc/lint/build/safety 0・git diff --check clean・変更3ファイル（scanner＋2テスト）のみ。既存回帰（stale/build badge/8名parity/NAV 67/worker rethrow）維持。BullMQ 実 queue は EVIDENCE_GAP 維持。Critical/High=0 は Claude 自己申告でなく **Codex 判定待ち**。main/Production/DB/Secrets/外部送信/実LLM/課金 接触なし・docs/function-master・PR#15・.github・AGENTS・.codex 非編集。**Codex 再監査まで branch freeze**。
- **v6.2 Codex Track B 指摘クローズ 進行中（2026-07-12・記録: audit176 v6.2訂正・roadmap80 §3/§9・PR #14 review `4678311330`・recovery `claude/full-recovery-v61`）**: Codex Track B が **CHANGES REQUIRED**（High 1・P2 2・Evidence Gap・未解決 thread 5）。v6.1 の `RECOVERY_READY_FOR_HUMAN_GO` は**撤回**し `CHANGES_REQUIRED / CODEX_REVIEW_PENDING` へ格下げ。**High（秘密マスク）**: 値内部 escaped quote（`{"password":"abc\"SECRET"}`）で suffix 残存を実測再現し、JSON 全体 unescape を廃止して **quote/escape 状態を理解する bounded scanner**へ全面再設計（受入例＋escaped/nested/array/suffix/single-quote/巨大入力の否定テストで秘密一文字列非残存を直接 assert・過剰マスクなし）。**P2**: stale 判定を単一正本 `isStaleActiveRun`（RUNNING/QUEUED・null=stale・同一 threshold/now）に統一し pre(`shouldCreateRun`)/post(worker rival) を一致（表形式網羅＋worker 回帰）／build 識別バッジを `admin:read`（EXECUTIVE/READ_ONLY も成立）から **OWNER/ADMIN role key 本体**へ限定し他 role は server 未送出（肯定/否定 unit＋E2E STAFF 非表示/ADMIN 表示）。**Evidence Gap**: AI社員 **8名全員**の list→detail→office 往復 parity＋**実在別 tenant fixture（隔離・後始末つき）で 404**（`.first()`/架空文字列を廃止）。**NAV 契約**: 67 件の href 集合・順序を明示契約化（置換で red）＋12 グループ見出し＋RBAC 非表示≠消失テスト。**視覚証拠**: 8 named PNG（nav/ai-agents/ai-office × desktop/mobile）＋横 overflow・canvas 非blank・build badge role 別の DOM 実測。**BullMQ 実 queue は EVIDENCE_GAP 維持**。**doc 証拠格上げ是正**: schema 存在を PRODUCTION_VERIFIED としない語彙（`SCHEMA_ONLY`/`IMPLEMENTATION_UNVERIFIED`/`CI_VERIFIED`/`DEFERRED`/`ROADMAP_ONLY`/`NOT_PLANNED`）へ厳密化・競合台帳の overclaim を Function-ID 単位へ格下げ。**段階的完全実装方針**（MVP先行→Wave1 各カテゴリ AI DX/Marketing→Wave2 Salesforce系→Wave3 会計→Wave4 人事労務→Wave5 外部連携）を正本化し `NOT_PLANNED`→`DEFERRED`/`ROADMAP_ONLY`（実送金/税務断定/採否は HUMAN_ONLY 維持）。ローカル unit 398/0・tsc/lint/build/safety 0・git diff --check clean。main/Production/DB/Secrets/外部送信/実LLM/課金/実支払 接触なし・**Codex 再監査完了まで GO 提案しない**。docs/function-master/.github/AGENTS/.codex 非編集。
- **v6.1 完全復旧・4軸ロードマップ・競合機能台帳 進行中（2026-07-11・記録: audit176・roadmap80・PR #13/#14・recovery `claude/full-recovery-v61` head `b1cc686`）**: **根本原因＝deployment lineage**（利用者の Vercel 画面 NAV=63 は main `ffd586b`＝Production deployment `5353709567` と一致・欠落4機能は integration `7ef2d9f`＝Preview deployment `5404300707` にコードとして存在＝コード欠落ではない・削除0）。**P1/P2 再監査（red先行→修正）**: maskRunError の escaped-quote値/改行直後値/裸session の残存3経路を実測再現し `\"`unescape＋`:`/`=`直後改行join＋裸session キー追加で封鎖（否定テスト5・17敵対ケース全マスク）／worker 作成後 rival 判定へ `isStaleRunningRun` を適用しクラッシュ残骸が新規runを恒久的に潰す二重実行誤判定を修正（回帰テスト）／NAV_PAGE_GATES へ marketing系3件追加（整合性修正・seed全ロールは marketing:read 保持で可視挙動不変）／BullMQ実queue証拠は **EVIDENCE_GAP 維持**。**AI社員⇔3D Office 人物正本統一**: getAiCharacter(key)＋deriveAgentState を単一正本にし /ai-agents 一覧（ポートレート＋状態）・/ai-agents/[id]（AiProfileCard＋状態）・/ai-office(?agent= 初期選択) を双方向 deep link で接続（DB重複保存なし・別テナントid 404）。**NAV 静的契約テスト**（67件・重複0・全hrefにpage実体・復旧4導線・削除でred）＋**build識別バッジ**（OWNER/ADMINのみ・Production/Preview＋short SHA・非機密メタのみ）。ローカル全電池 unit 365/0・tsc/lint/build/safety 0。**4軸Phase併記ルール**（Repository lineage/Business Phase/Strategy/R Stage）と workstream 固有名（P3-GROWTH/P3-Q2C/P35-CHANNELS/P4-WORKFORCE 等）を roadmap80 で正本化・**AI社員開発環境matrix/Salesforce台帳/MoneyForward・freee台帳**を証拠段階（REQUIREMENT_ONLY〜PRODUCTION_VERIFIED）＋方式（NATIVE/ASSISTIVE/CONNECTOR/NOT_PLANNED/HUMAN_ONLY）で整理（「置き換え済み」「同等」と断定せず・実送金/実課金/税務断定はしない）。main merge・Production 反映は**人間 GO 後のみ**（1画面報告で GO/NO-GO）・Codex Track B delta review 依頼済み・本番/DB/Secrets/migrate/seed/外部送信/実LLM/課金 接触なし。
- **v5.8 Claude×Codex 協調統合 完了（2026-07-11・記録: roadmap69§0追補/roadmap77§5/roadmap78・PR #7/#8 の CLAUDE_ACK/CLAUDE_INTEGRATED コメント）**: **Codex PR #7（完全機能台帳 v1）を feature へ統合** — SHA 期待値一致（raw a07458fb…/normalized 18e3cc79…）・件数再集計一致（50カテゴリ・原子機能2,553・Stable ID 7,485）・secret 0・broken link 0。原典取得不能のため生成器 --check 未実行 → **ATOMIC_LEDGER_SYNC = GITHUB_CANONICAL_SYNCED / SOURCE_RECHECK_WAITING**（Phase 3 正式完了宣言と PR #3 main 統合は引き続き HOLD）。**Codex PR #8（自動引き継ぎ Hook）を feature へ統合**（read-only 検証・実行テスト済み・Hook 有効化は次回セッション再起動時に確認）。**Codex 指摘の High 2 件を修正**（maskRunError 全面強化＋否定テスト7・worker 例外の安全な再throw＋テスト）・Medium 6 件対応（dealId ABAC・顧客名の取得段階遮断・SEO入力/出力上限・CAS遷移・二重Run収束・AIゲート read-only 可視化。完全原子化と承認bridgeは roadmap78 の設計 Gate）。feature→A→B→C→D 非force順次伝播・全 head CI green をログ本文確認（#210 93/0・#212 99/0・#214 100/0・#216 103/0・D は PR #9 参照）。Stream D 改善（初期自動選択・プロフィール全体 screenshot・emoji→Lucide・本文12px化・横overflowゼロのDOM検証）。PR #2〜#9 全て Draft 維持・main merge なし・封印不変。
- **v5.6 三系統前進 完了（2026-07-11・記録: roadmap72-76/audit171-174）**: **WIP-0** Evidence ID 補正（EVID- 接頭辞・対応表 roadmap72 が正・保守的 Function ID Evidence Map・ATOMIC_LEDGER_SYNC=PENDING 継続）。**Stream A2（PR #4）**= C21 SEO/Content v0（/marketing/content・SEOブリーフAI下書き＝fake直呼び・禁止クレーム（No.1/業界初等）は生成せず人間確認へ・命令注入で生成中止・/marketing ホームに marketing:read ゲート・CI #188 `99 passed`/0）。**Stream B2（PR #5）**= Agent Control Plane v0（RUN_TRANSITIONS 許可表・terminal 巻き戻し禁止・二重Run防止・stale RUNNING(>2h) は working と断定しない・COMPLETED=SUCCEEDED 別名/BLOCKED=導出で schema 変更なし・producer は MORNING_REPORT_JOB 1本・maskRunError・ナビ権限フィルタ・webglcontextlost フォールバック・CI screenshot artifact 追加・CI #190 `100 passed`/0・artifact `e2e-screenshots-29149576645` 取得）。**Stream C1（PR #6・claude/stream-c-work-evidence-v56・B2完了SHAから分岐）**= AI Work Evidence Cockpit v0（/ai-office タブ化＝3Dオフィス/人間の作業インボックス/成果台帳・証拠5区分 measured/self_reported/estimated/unverified/unavailable・削減時間は baseline 未計測のため「計測なし」固定＝推定しない・財務行は finance:read のみ・Inbox は deep link のみ・CI #196 `103 passed`/0）。**C22** は Referral 実装前 Gate 設計を docs-only で正本化（roadmap76・schema/migration は別の明示承認）。PR #3/#4/#5/#6 すべて Draft 維持・merge なし・封印 env 不変。
- **v5.5 両ストリーム v0 完了（2026-07-11・記録: roadmap70-71/audit169-170）**: **Stream A（PR #4・claude/stream-a-growth-channels-v55）**= C19 Ads read model＋AI改善案下書き（/marketing/ads・チャネル状態盤・封印中表示・fake直呼びで実LLM経路が構造的に不在・CI #172 `96 passed`/0・Evidence C19-RO-01 クローズ）。**Stream B（PR #5・claude/stream-b-ai-office-v55）**= AI Workforce read model＋3Dバーチャルオフィス v0（/ai-office・証拠由来の8状態導出・no telemetryはunknown・人間の承認デスク接続線・2Dフォールバック・モバイル簡略・three@0.185.1・/ai-agents ゲート補正・CI #174 `97 passed`/0・canvas非blankピクセル検査をCIで実証）。両PRともDraft・mergeなし・基準SHA 6477279。C21/C22 は roadmap69 §2 の DoR/DoD で台帳化済み（未着手・C22はschema Gate必須）。
- **案B+ 採用（人間 Phase Gate 決定・2026-07-11・正本: roadmap69/audit168）**: Phase 3 = **AI Growth Engine v0** としてクローズ準備（「全機能完成」とは表現しない）。C19/C21/C22 は **Phase 3.5 Growth Channels** として正本化（削除・中止・完成扱いにしない・オーナー/DoR/DoD/段階/Gate は roadmap69 §2）。Phase 3.5 と Phase 4（AI Workforce/3D Office・Epic は roadmap69 §3）を並行前進。**Phase 4 完了条件に主要 Growth Channel 最低1つの read-only→AI下書き→人間承認 接続を必須化**。外部操作（外部送信・実LLM・実広告・SNS投稿・PR公開・課金）は封印継続。**ATOMIC_LEDGER_SYNC=PENDING 継続**（docs/function-master 不在・期待hash/統合手順は roadmap69 §0・**Phase 3 正式完了と PR #3 の main 統合は PENDING 解除まで HOLD**）。実行は共通正本化 commit から子ブランチ2本（stream-a-growth-channels-v55 / stream-b-ai-office-v55・各 Draft PR・merge なし）。
- **WIP-3〜7: Phase 3 クローズ準備（docs/roadmap/64-68・オートパイロット v5.4）: 完了（2026-07-11・記録: doc163-167）**。境界クローズ4連 WIP を各々「設計→実装→敵対的レビュー→修正→CI green（ログ本文確認）→push」の完全サイクルで完遂: **WIP-3** /growth・/growth/events・/dx 財務境界（counts 変種・finance 行の取得段階遮断・無権限 Server Action 裏口削除・CI #161 `88 passed`/0）／**WIP-4** Quote/Invoice/Print＋/deals 閲覧境界（quote:read 判断の明文化・envelope→assert→fetch・宛先の可視ラベルガード・action の customerId/dealId テナント/ラベル検証・customer-visibility 共有ヘルパ化・CI #162 `91 passed`/0）／**WIP-5** Topbar 承認バッジ＋/dashboard・/dashboard/ceo・/reports/morning の承認シグナル取得段階遮断（入口条件を approval:approve に統一・CI #163 `93 passed`/0）／**WIP-6** Customer/Contact PII の 29 経路機械監査台帳（roadmap67 §1 が正）＋Critical 1（/planning-hokko が認証のみで全顧客名表示）・High 2 を修正（golden-path 系に fail-closed 可視ラベル引数・実DBで `in:[]` 挙動実証）。**0 Critical/0 High の証拠は台帳＋各レビュー追補（roadmap64-67 の §5/§6）**。**Phase 3 完了は宣言しない**（C18/C20/C27/C38 達成・C19/C21/C22 未達 → 案A/案B を人間 Phase Gate に提示・roadmap68 §2 が正）。Phase 4 最初の3 WIP 案 = roadmap68 §3。HOLD: Contact 統治（schema Gate 必須）・PII 二次保存・AI照合 over-fetch ほか roadmap67 §3。封印 env（fake/log/false）全期間不変・main への merge なし。
- **WIP1: CRM 閲覧境界クローズ＋WIP2: Growth Event Ledger 成果可視化（docs/roadmap/61-63・オートパイロット v5.2）: 完了（判定 WIP1 完全クローズ〔CI run 29135772068 = `83 passed`/0 をログ本文確認〕・WIP2 実装完了・2026-07-11・記録: doc160-162）**。**WIP1**: Customer の7つの穴（一覧の無権限全行表示／詳細の取得後判定／拒否画面の顧客名表示／edit 素通し／timeline・insights の ABAC 欠如／**insights の render 時 LLM 送出構造**／update の label 判定なし）を全経路クローズ — customer:read をデータ取得前に適用・二段階取得（envelope→ABAC→本体・label 固定で TOCTOU 閉鎖）・一覧は label クエリ除外（非マネージャは高機密6種も除外）・拒否画面から PII 撤去・render 時 AI 呼び出し撤去（`19ff866`→`e77870c`→`6961a72`）。Contact=`NO_DIRECT_RUNTIME_SURFACE` 証拠化。**doc125 の HOLD は追記クローズ**（「CRM 全体完成」とは呼ばない）。**WIP2**: Control Tower に「成果と削減時間（Growth Event Ledger）」read-only セクション（7日/30日件数・カテゴリ別・売上効果・コスト削減・削減時間・未計測明示・/growth/events リンク）。金額は canViewFinance のみ・**非財務閲覧者には finance カテゴリを件数集計から完全除外（算術復元・存在シグナル遮断）**・削減時間は自己申告集計と明記（`50afc9c`→`662e745`→`8222bcb`）。敵対的レビュー計5視点で critical 1（e2e waitForURL）含む10件を push 前修正。e2e 80→85件。schema/seed/RBAC定義/label許可表/ci.yml 不変。
- **P3-CT-5 完全クローズ＋Control Tower v0 完遂（docs/roadmap/60 Candidate・統合オートパイロット v4.0）: 完了（判定 CT v0 全段完遂・2026-07-10・記録: doc157-159＋`docs/roadmap/58-60`）**。1回の統合実行で: ①完全機能台帳 v1.0 を正本化し **C41-C44 番号ズレを追記主義で整合**（Phase 3 の正対応 = C18-C22＋C27＋C38・roadmap58 §4 が正・`25b5cb9`）②P3-CT-5 設計＋Gate A〜I 全 PASS（roadmap59・`848317c`）③実装 = Control Tower に**「人間の判断待ち（承認導線）」セクション**（承認待ち PENDING 件数＋/approvals 入口は approval:approve 権限者のみ・下書き件数＋/leadmap/leads 入口は leadmap:read・**リンクのみ・実行ボタンなし・count のみで金額/PII 非取得**・`14d6b7d`）④push 前敵対的レビュー（3視点独立）で**既存 medium 1件を発見し push 前に修正**（/leadmap/leads 配下3ページに read ゲート追加・`d1d8e36`）⑤push（人間 GO）→ CI **run 29127896331（#156）= success・Run E2E `80 passed (1.2m)` / 0 failed をログ本文で確認**（env fake/log/false・growth_control_tower 8件 green・stage3_e2e **15 run 連続 green**）。**P3-CT-0〜5 全段完遂＝Control Tower v0 完成**（CT-6/7 は畳み込み消化・72→80件）。残課題 LOW/INFO は roadmap60 §4 に記録（Topbar 件数の need-to-know 等）。
- **P3-CT-4 完全クローズ＋P3-CT-5 準備の正本化（docs/roadmap/57 Candidate）: docs-only 完了・実装なし・push なし（判定 P3-CT-4 完全クローズ・2026-07-10・記録: doc156＋`docs/roadmap/57`）／369-vault非編集**。push 済み commit `d45491c` の CI **run 29122397143（run #154）= success・stage1/stage3_e2e success・Run E2E 77 passed / 0 failed をログ本文で直接確認**（`Running 77 tests using 2 workers`→`77 passed (1.2m)`・env LLM_PROVIDER=fake / MAIL_PROVIDER=log / EXTERNAL_SEND_ENABLED=false・growth_control_tower 5件 green・stage3_e2e **13 run 連続 green**: 28930122157→…→29122397143）を正本化。roadmap56 本文の「76 見込み」は §18 追補＋roadmap57 §4 で **77 確定**に整理（追記主義・本文は書き換えない）。**P3-CT-0〜4 完了・CT-6/7 は各段に畳み込み消化・次段は P3-CT-5（承認導線 deep link 強化・既存 /approvals・OutreachDraft への接続のみ・新規送信は作らない・状態永続化/新 schema が必要なら実装前 Gate で STOP）**。Phase 系譜・3系統・50カテゴリ（C41-C44 = CT-4 確定）・20大カテゴリ・追加19領域・5本柱との接続と初期 MVP 禁止事項非接触を roadmap57 §7-13 に固定。次回 P3-CT-5 設計＋実装前 Gate 統合ミッションのプロンプトは **roadmap57 §19**。
- **P3-CT-4 push 前 敵対的レビュー＋redaction 修正（roadmap56 §18 追補）: 修正完了・commit-only（判定 high 1件修正・low 2件修正・e2e 74→77・2026-07-10）／369-vault非編集**。未 push commit `83fd4bc` に対し **6視点×独立検証の敵対的レビュー（15エージェント）**を実施し、**high 1件（6視点すべてが独立検出）**: 社長が finance カード（未回収リスク/低粗利）から生成した下書きメモに finance 件数（deterministicDraft の reason＋FakeLLM echo の2経路）が入り、**メモ表示クエリが閲覧者の canViewFinance を見ていなかった**ため担当者に件数が漏れる（カード redaction の迂回）——を確定。**修正**: ①page.tsx のメモクエリに閲覧者側 redaction フィルタ（canViewFinance=false は financeGated カード由来メモを entityId notIn で除外）②生成ボタンを `isHumanUser＋hasPermission(leadmap,create)` でゲート＋denied/blocked の安全な通知表示（low 2件・既存 e2e 文言と非重複）③e2e 追加「担当者には finance 由来の AI 下書きメモが表示されない」＋社長テストに unpaid_risk 生成を追加（**74→77件**）。actions.ts の安全境界・schema/RBAC/seed は不変。再検証: pnpm test 278/0・typecheck×2・lint・safety・diff-check・secret NONE 全緑。**push 前に発見・修正のため外部影響ゼロ**。
- **P3-CT-4 Control Tower FakeLLM 下書き生成 実装（docs/roadmap/56 Candidate＋コード3ファイル）: 実装完了・commit-only（判定 実装完了・STOP 非該当・単体278 passed・2026-07-10・記録: doc155＋`docs/roadmap/56`）／369-vault非編集**。実装前 Gate PASS（roadmap55）と CI **run 29116334142 = success・74/0**（ログ本文直接確認・stage3_e2e 12 run 連続 green）を前提に、roadmap55 §7 の実装計画どおり最小実装。**変更コード3ファイルのみ**: ①`apps/web/app/(app)/growth/control-tower/actions.ts` **新規**（`generateControlTowerNextStepAction`: requireUser→**isHumanUser で AI ロール拒否**→hasPermission(leadmap,'create')→**card key 9件 allowlist 検証**→getControlTowerData でサーバー側再取得・存在確認→**redacted/financeGated×!canViewFinance 拒否（二重防御）**→FakeLLM 入力を安全項目のみに制限（key/タイトル/優先度/reason/redact済み件数/財務表示 boolean・**金額実値・生PII・placeId を渡さない**）→safeAiInput（high 注入は生成中止）→`getLLMProvider().chat`（fake 必須）＋**deterministic fallback**→`saveAIOutputStandard`（task=generateControlTowerNextStep・AIOutput 下書き保存・**UsageEvent ai.output.generated 自動計測**・PII 自動フラグ）→`writeAudit(action='ai_run', entityType='AIOutput')`→revalidatePath→redirect）②`page.tsx`（非 redacted カードに「AI 下書きメモを作る」ボタン `ct-generate-<key>`・redacted カードは安全な別文言のみ「AI 下書きは財務権限者のみ作成できます。」＝既存 redaction 文言と非重複で staff e2e toHaveCount(2) 不変・下部に「AI 下書きメモ（最新・下書きのみ）」read-only セクション `ct-memo-item`・**送信/承認/削除ボタンなし**）③`growth_control_tower.spec.ts`（**e2e 2件追加・74→76**: 社長が生成→メモ表示／担当者は redacted finance 2カードに生成ボタンなし＋非 finance には出る・testid で strict-mode 安全・送信テストなし・既存2件不変）。**任意ファイル（shared 純ロジック＋単体）は不使用**（deterministic 文言は actions.ts 内で完結・最小差分）。**触らず**: schema/migrations/seed/rbac/labels/db.ts/audit.ts/usage-events.ts/ai-safety-server.ts/leadmap/approvals/finance/ci.yml/playwright.config.ts/package/lockfile/369-vault。**監査**: writeAudit=既存 ai_run（1生成=1件）・DataAccessLog=P3-CT-3 方針維持（生成時のサーバー側再取得で閲覧ログ1件は仕様どおり）・UsageEvent=saveAIOutputStandard 経由の ai.output.generated のみ（recordUsageEvent 直呼びなし・新 eventType なし・usage_only）。**AI 境界**: 人間起点のみ・FakeLLM/deterministic のみ・AI は送信/承認/削除しない・生成物は必ず下書き（メモ本文にも明記）・重要操作は既存 /approvals 委譲。**検証**: `pnpm test` **278 passed / 0 failed**・web/shared typecheck exit 0・lint exit 0・safety exit 0（ui 157→158）・diff-check OK・secret NONE・禁止領域差分 0・artifact なし・vault 差分 0。**e2e 実走はサンドボックス不可→push 後 CI で 76 passed / 0 failed（既存74＋新規2）を確認**。**STOP 非該当**（新 schema/RBAC/seed/package/eventType/状態永続化/外部送信/実LLM いずれも不要のまま完了）。**redaction 不変（担当者に原価・粗利・未回収の実値なし）・Customer/Contact 生 PII 非増加・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **P3-CT-4 実装 push-only（別承認）→ CI で 76/0 確認 → P3-CT-5 設計（別承認）**。
- **P3-CT-4 Control Tower FakeLLM 下書き生成 実装前 Gate（docs/roadmap/55 Candidate）: docs-only 完了・実装なし（判定 **Gate PASS**・既存 schema/RBAC/seed/安全基盤のみで成立・STOP 非該当・2026-07-10・記録: doc154＋`docs/roadmap/55`）／369-vault非編集・コード差分ゼロ（Gate 判定と実装計画のみ）**。P3-CT-4 設計 docs push 後 CI **run 29114587876 = success・Run E2E 74 passed / 0 failed**（ログ本文で env fake/log/false・Running 74 tests・74 passed・growth_control_tower 2件 ✓ を直接確認・stage3_e2e 11 run 連続 green）を前提に、roadmap54 §5.2/§6 の実装候補を **read-only 実査（推定で断定しない）**で最終判定。**Gate 判定 A〜I 全項目 YES**: A 既存 schema のみ=YES（AIOutput/OutreachDraft/ApprovalRequest 既存・新 table/column/enum 不要）／B 既存 RBAC のみ=YES（`hasPermission(leadmap,'create')` の前例 generateOutreachAction:94＋`isHumanUser`(rbac.ts:156) で AI ロール拒否・AI_AGENT/AI_ASSISTANT に送信/承認/削除を追加しない原則不変）／C 既存 seed のみ=YES（CI 74/0 が既存 seed での CT カード表示を実測証明・追加 e2e も既存データで可）／D FakeLLM のみ=YES（`getLLMProvider` は `LLM_PROVIDER || 'fake'` でキー未設定は必ず Fake フォールバック providers/index.ts:21-24・全タスクに fakeX 実装・実LLM/AIコスト/externalAiAllowed true 不要）／E redaction/PII=成立（redacted カードは UI ボタン非表示＋Server Action 拒否の二重防御・FakeLLM 入力はカード key/redact 済み件数/優先度のみで金額実値・生 PII・placeId を渡さない）／F HCG/Consent/Suppression=不変で成立（AI は送信/承認/削除しない・下書きのみ・実送信は既存 ApprovalRequest→/approvals 委譲・isSuppressed/用途別分離維持）／G Audit/UsageEvent=YES（writeAudit=既存 `ai_run` 前例 leadmap/actions.ts:117・DataAccessLog=P3-CT-3 配線維持・UsageEvent=saveAIOutputStandard 経由 `ai.output.generated` のみ・新 eventType/billable_candidate 不要）／H 実装対象=確定（**`growth/control-tower/actions.ts` 新規＋`page.tsx` ボタン＋e2e 1〜2件（74→75〜76）・@hokko/ai タスク追加は不採用（getLLMProvider 直呼び＋deterministic 組み立て）・任意で shared 純ロジック＋単体**・db.ts/audit.ts/usage-events.ts/ai-safety-server.ts/schema/seed/RBAC/labels/ci.yml/playwright.config.ts/package/lockfile は触らず成立）／I STOP 条件=非該当（実装中に必要化したら即 STOP・別承認）。**検証**: `git diff --check` OK・secret NONE・safety exit 0・禁止領域差分なし・artifact なし・369-vault 差分なし（docs/tasks 5ファイルのみ）。**実装なし・app変更なし・tests変更なし・schema変更なし・migrationなし・seed変更なし・RBAC変更なし・ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・状態永続化なし・redaction 不変・PII 非増加・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc154/roadmap55 push-only（別承認）→ CI で 74/0 確認 → P3-CT-4 実装ミッション（別承認・roadmap55 §7 の実装計画どおり）**。
- **P3-CT-4 Control Tower FakeLLM 下書き生成 設計（docs/roadmap/54 Candidate）: docs-only 完了・実装なし（判定 設計完了・既存 schema のみで成立・STOP 非該当・2026-07-08・記録: doc153＋`docs/roadmap/54`）／369-vault非編集・コード差分ゼロ（設計のみ）**。P3-CT-3 実装 push 後 CI **run 28952843194 = success・stage3_e2e ログ本文で 74 passed / 0 failed・env fake/log/false・growth_control_tower 2件 green を直接確認済み**（stage3_e2e 10 run 連続 green）を前提に、Control Tower のカードから「次の一手ドラフト」を生成する P3-CT-4 を docs-only 設計。**大原則**: AI は提案するだけ・人間が判断/承認/送信・AI は外部送信/承認/削除しない・請求/会計/契約/督促/返金/値引きを確定しない・生成物は必ず下書き・重要操作は ApprovalRequest／`/approvals` 経由。**生成物の設計判断**: ①汎用「次の一手ドラフト」= 人間ボタン起点 → FakeLLM（LLM_PROVIDER=fake・env 未設定は自動 Fake）or deterministic → **既存 `saveAIOutputStandard` で AIOutput 保存**（採用・本命。UsageEvent `ai.output.generated` 自動計測・PII 自動フラグ・mutation は AIOutput 1 create のみ）②リード営業文 = **既存 OutreachDraft 導線（`/leadmap` generateOutreachAction）へ deep link 委譲**（採用・二重実装しない・isSuppressed/OutreachApproval/HCG の既存統制がそのまま効く）③deterministic 単独=不採用（fallback 文言）④CT から ApprovalRequest 直作成=不採用（既存ドメイン導線へ委譲）。**最重要判定 = 既存 schema のみで成立（YES）**: AIOutput・OutreachDraft・ApprovalRequest・AuditLog・DataAccessLog・UsageEvent・saveAIOutputStandard・safeAiInput・assertAiToolAllowed・guardDangerousAction・isSuppressed すべて既存＝**新規 table/column/enum/migration・新 RBAC action/role・新 seed・新 package・新 UsageEvent eventType・状態永続化はいずれも不要**。**redaction**: financeGated かつ canViewFinance=false のカードは**生成不可（ボタン非表示＋Server Action 拒否の二重防御）**・安全文言は「財務閲覧権限が必要」系 deterministic のみ・**FakeLLM 入力はカード key／redact 済み件数／優先度ラベルのみ（金額実値・顧客名・メール・電話・住所・placeId を渡さない）**。**監査**: writeAudit=既存 `ai_run`（1生成=1件）／DataAccessLog=P3-CT-3 の read/confidential_view 方針維持／UsageEvent=既存 `ai.output.generated` のみ（billing=usage_only・billable_candidate 不使用・新 eventType は STOP）／metadata に PII/secret/本文/prompt/金額/カード別件数を入れない。**HCG/Consent/Suppression**: 送信ボタンを作らない・deep link まで・suppressed 相手は既存導線の抑止がそのまま効く・口コミ量産/ステマ/Googleレビュー投稿は禁止側に固定。**実装候補（別承認）**: `apps/web/app/(app)/growth/control-tower/actions.ts` 新規（requireUser・人間限定・redacted 拒否・saveAIOutputStandard・writeAudit ai_run）＋page.tsx ボタン＋（必要なら）@hokko/ai タスク＋e2e 1〜2件（74→75〜76）。**STOP 条件**（新 schema/RBAC/seed/package/eventType/外部送信/実LLM/状態永続化/生PII/redaction 破り）を明文化。**検証**: read-only 実査（generateOutreachForLead・ai-safety-server.ts・schema 3 モデル・approval.ts・isSuppressed・CT 3ファイル）・`git diff --check` OK・secret NONE・safety exit 0・禁止領域差分なし・artifact なし・369-vault 差分なし（docs/tasks 5ファイルのみ）。**実装なし・app変更なし・tests変更なし・schema変更なし・migrationなし・seed変更なし・RBAC変更なし・ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・状態永続化なし・redaction 不変・PII 非増加・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc153/roadmap54 push-only（別承認）→ CI で 74/0 確認 → P3-CT-4 実装前 Gate（別承認・docs-only）**。
- **P3-CT-3 Control Tower 監査ログ配線 実装（docs/roadmap/53 Candidate＋control-tower.ts）: 実装完了・commit-only（判定 実装完了・コード変更1ファイルのみ・業務 mutation なし・redaction 不変・単体278 passed・2026-07-08・記録: doc152＋`docs/roadmap/53`）／369-vault非編集**。P3-CT-3 docs push 後 CI **run 28949692213 = success・74 passed / 0 failed**（env fake/log/false・growth_control_tower 2件 green をログ本文で直接確認）を前提に、roadmap52 §6.2/§8 の設計どおり `apps/web/lib/domains/growth/control-tower.ts` の **1ファイルのみ**を最小変更。**変更点**: `getControlTowerData` 末尾で `actionableCount` を先に1回算出し、DataAccessLog 用 metadata を **allowlist 3項目（financeVisible/cardCount/actionableCount）のみ**に固定。**canViewFinance=true** → 既存 `confidential_view`（entityType=GrowthControlTower・entityId=null・label=INTERNAL・purpose=growth_control_tower_view）＋今回追加 `policyDecision:'allow'`・metadata。**canViewFinance=false（redacted 閲覧）** → 新規に `action:'read'`・同 entityType/label/purpose・policyDecision:'allow'・metadata を **1閲覧=1件**記録（従来は無記録＝担当者に finance 実値が出ていない証跡を DataAccessLog で完結）。**1閲覧=1件**（カード別9件・レコード別にしない）・error handling は既存 `await` 直呼び・返却値 `{cards,canViewFinance,actionableCount}` 不変。**writeAudit 不採用**（AuditLog は mutation 専用・read-only 画面には配線しない）・**UsageEvent 不採用**（画面閲覧は資源消費でない・既存 8 emit 不変）・**既存 DataAccessAction（read/confidential_view）のみ使用**（新 action 値なし）。**metadata denylist**（金額・原価・粗利・未回収額・請求額・カード別件数・finance系件数・顧客名/メール/電話/住所・担当者名・placeId・Google由来・secret・env値・token・AIプロンプト・生成文・本文・score・scoreBreakdown・URLクエリ・IP以外の端末詳細）をコードコメントで明記し実際に入れていない。**画面（page.tsx）・純ロジック（growth-control-tower.ts）・e2e spec・db.ts・audit.ts・usage-events.ts は不変**。**STOP 非該当**（新規 schema/table/column/enum/migration なし・新 DataAccessAction 値なし・新 RBAC/seed なし・状態永続化なし・redaction で塞げない機密表示なし・ci.yml/playwright.config.ts/package/lock 非変更）。**redaction 方針不変**（担当者に原価・粗利・未回収・請求金額の実値を渡さない・lib 段階で件数 null・追加した read ログにも finance 値なし）。**Customer/Contact の生 PII 非増加**（metadata denylist で構造的に防止・既存顧客は件数のみ）。**検証**: `pnpm test` **278 passed / 0 failed**（回帰なし）・`pnpm --filter @hokko/web typecheck` exit 0・`pnpm --filter @hokko/shared typecheck` exit 0・`pnpm lint` exit 0・`node scripts/check-company-brain-safety.mjs` exit 0（actions 4・ui 157）・`git diff --check` OK・secret NONE・禁止領域差分なし（コード変更 control-tower.ts 1ファイルのみ）・artifact なし・369-vault 差分なし。**e2e 実緑は push 後 CI（stage3_e2e）で 74 passed / 0 failed（growth_control_tower 2件 green）を確認**（DataAccessLog 記録は画面表示に出ないため既存 e2e 緑維持見込み）。**業務データ mutation なし・schema変更なし・migrationなし・seed変更なし・RBAC変更なし・labels変更なし・ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・状態永続化なし・artifact 非 git add・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **P3-CT-3 実装 push-only（別承認）→ CI で 74/0 確認 → P3-CT-4 設計（FakeLLM 下書き生成・別承認）**。
- **P3-CT-3 Control Tower 監査ログ配線設計（docs/roadmap/52 Candidate）: docs-only 完了・実装なし（判定 設計完了・Gate PASS・既存基盤のみで成立・2026-07-08・記録: doc151＋`docs/roadmap/52`）／369-vault非編集・コード差分ゼロ（設計のみ）**。P3-CT-2 push 後 CI **run 28946738844（head_sha `83bd185`）= success・stage1/stage3_e2e success・Run E2E 74 passed / 0 failed**（growth_control_tower の社長閲覧・担当者 redaction 2件 green・stage3_e2e 8 run 連続 green）を前提に、Control Tower の監査ログ配線（writeAudit / writeDataAccess / UsageEvent の境界）を実装前に docs-only で確定。**現状の read-only 実査**: `apps/web/lib/domains/growth/control-tower.ts` は canViewFinance=true のときのみ `writeDataAccess(action=confidential_view, entityType=GrowthControlTower, label=INTERNAL, purpose=growth_control_tower_view)` を1閲覧=1件記録（redacted 閲覧は無記録）・writeAudit/UsageEvent は未配線。**方針確定**: ①**writeAudit 不採用**（リポジトリ規約上 AuditLog は mutation・重要操作専用で閲覧の前例ゼロ・CT v0 は read-only で対象操作なし。将来 P3-CT-4 の mutation 段で別途配線）②**writeDataAccess 採用・拡張案確定**（finance 閲覧の confidential_view 1件は据え置き＋policyDecision:'allow'/metadata 付与、**redacted 閲覧にも `action:'read'`・label INTERNAL・同 purpose を1閲覧=1件追加**＝担当者に finance 実値が出ていない証跡を DataAccessLog で完結。粒度は1閲覧=1件・entityId null・await 直呼び既存パターン踏襲）③**UsageEvent 不採用**（既存 emit 8種はすべて資源消費で画面閲覧の前例なし・CT 起点 AI 生成が入っても既存 `ai.output.generated` で自動計測・閲覧回数の利用量化は要望が出たら別承認で設計）④**metadata 境界固定**: allowlist=`financeVisible`/`cardCount`/`actionableCount` の3項目のみ・denylist=金額/原価/粗利/未回収額/**カード別件数**/生PII/placeId/secret/本文/scoreBreakdown。**Gate 判定 = PASS（STOP 非該当）**: AuditLog/DataAccessLog/UsageEvent・`writeAudit`/`writeDataAccess`/`recordUsageEvent`・action 値 `read`/`confidential_view` はすべて既存＝**新規 schema/enum/カラム/migration/RBAC/seed 不要**。STOP 条件（実装段）＝新 action 値・新テーブル/カラム・metadata に PII/finance 実値・状態永続化・writeAudit の閲覧流用。**実装候補（別承認）は `apps/web/lib/domains/growth/control-tower.ts` 1ファイルのみ**（page.tsx・shared 純ロジック・e2e・db.ts・audit.ts・usage-events.ts・schema・seed・RBAC は触らない）。テスト方針＝DB 依存のため単体追加なし（278 passed 体制不変）・表示不変で e2e 74/0 維持を push 後 CI で確認。**redaction 不変**（担当者に原価・粗利・未回収の実値なし・本設計は帳簿のみで表示不変）。**Customer/Contact の生 PII 非増加**（metadata denylist で構造的に防止）。**検証**: `git diff --check` OK・secret NONE・`node scripts/check-company-brain-safety.mjs` exit 0・禁止領域差分なし・artifact なし・369-vault 差分なし（docs/tasks 5ファイルのみ）。**実装なし・app変更なし・tests変更なし・schema変更なし・migrationなし・seed変更なし・RBAC変更なし・ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・状態永続化なし・artifact 非 git add・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc151/roadmap52 push-only（別承認）→ CI で 74/0 確認 → P3-CT-3 実装ミッション（別承認・roadmap52 §6.2/§8 のとおり）**。
- **P3-CT-2 Control Tower 優先度ロジック精緻化（docs/roadmap/51 Candidate＋純ロジック）: 実装完了・commit-only（判定 純ロジック精緻化完了・業務 mutation なし・redaction 不変・単体278 passed・2026-07-08・記録: doc150＋`docs/roadmap/51`）／369-vault非編集**。P3-CT-1 CI green（run 28944487139=success・74/0・growth_control_tower 2件 green）を前提に、`packages/shared/src/growth-control-tower.ts` の DB非依存 純ロジックを精緻化。**旧 v0**: `score=baseWeight+min(count,20)`（redacted=baseWeight×0.6）。**新 P3-CT-2**: **score=重要度(businessImpact)×(基礎 0.7＋緊急度 0.3×urgency)×信頼度(confidence)**（deterministic・上限 `CONTROL_TOWER_SCORE_MAX=100`）。urgency=`min(count/urgencyCap,1)`（件数で飽和・過剰に釣り上げない）。confidence は Golden Path/Finance=1.0・Deal=0.95・リード=0.9・次回接触=0.85・Company Brain=0.7・既存顧客件数のみ=0.6。**empty（count=0・非redacted）は score=0**、**redacted（financeGated かつ canViewFinance=false）は「要確認」中位**＝`round(businessImpact×0.6×confidence)`・count=null・urgency=0・reason は redaction 文言。各カードに `scoreBreakdown`（businessImpact/urgency/confidence/redactedFloor）を付与し**説明可能**化（乱数・時刻・PII・金額なし）。同点は重要度降順で安定ソート。priority 閾値 high>=70/medium>=40/low。件数がある社長ビューは 社長が見るべき成長機会・未回収リスク・停滞商談・低粗利改善候補 が上位に来やすく、count=0 は上位に来ない。**redaction 方針は不変**（担当者に原価・粗利・未回収・請求金額の実値を渡さない・pure logic のデータ構造でも count を持たない）。**Customer/Contact の生 PII 非増加**（既存顧客は件数のみ）。**変更ファイル**: `packages/shared/src/growth-control-tower.ts`（純ロジック・型に scoreBreakdown 追加）＋`packages/shared/src/__tests__/growth-control-tower.test.ts`（6→13 テスト：score 上限・urgency 飽和・単調非減少・上位順・redacted 中位・priority ラベル・scoreBreakdown）。**未変更（最小差分）**: control-tower.ts（signals 不変）・page.tsx・growth_control_tower.spec.ts（見出し・redaction 不変）。**STOP 条件非該当**（新規 schema/table/column/enum/migration なし・状態永続化なし・新規 RBAC action/role なし・新規 seed なし・redaction で塞げない機密表示なし・ci.yml/playwright.config.ts/package/lock 非変更）。**検証**: `pnpm test` **278 passed / 0 failed**（従来271＋新規7・回帰なし）・`pnpm --filter @hokko/web typecheck` exit 0・`pnpm --filter @hokko/shared typecheck` exit 0・`pnpm lint` exit 0・`node scripts/check-company-brain-safety.mjs` exit 0・`git diff --check` OK・secret NONE・禁止領域差分なし・artifact なし。**e2e 実緑は push 後の CI（stage3_e2e）で 74 passed / 0 failed（担当者 redaction 2件・社長 0件）を確認**（純ロジック変更は見出し・redaction 表示不変のため既存 e2e は緑維持見込み）。**app変更なし（page/lib/e2e 不変）・schema変更なし・migrationなし・seed変更なし・RBAC変更なし・ci.yml変更なし・playwright.config.ts変更なし・package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・業務データ mutation なし・状態永続化なし・artifact 非 git add・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **P3-CT-2 push-only（別承認）→ CI で 74/0 確認 → P3-CT-3 設計（状態管理が要れば schema 影響を実装前 Gate で判定・別承認）**。
- **P3-CT-1 E2E selector hardening（docs/roadmap/50 Candidate＋e2e spec 1本）: tests-only 完了・commit-only（判定 selector 安定化完了・実装本体変更なし・redaction 方針不変・2026-07-08・記録: doc149＋`docs/roadmap/50`）／369-vault非編集**。P3-CT-1 実装 commit `6cc9d2c` を push する前に、`apps/web/tests/e2e/growth_control_tower.spec.ts` の担当者 redaction assertion を **Playwright strict mode 安全化**（tests-only）。**strict-mode risk**: finance-gated カードは「未回収リスク」「低粗利改善候補」の**2枚**あり、担当者（canViewFinance=false）では同一 redaction 文言 `原価・粗利は財務閲覧権限が必要です（機密情報）。` が **2要素一致** → 旧 `getByText(...).toBeVisible()` は strict-mode violation で赤化し得た（脚注は当該文言を含まない文へ実装時に変更済みのため担当者=2件・社長=0件）。**修正**: 担当者テストは `const redactionMessages = page.getByText('原価・粗利は財務閲覧権限が必要です',{exact:false}); await expect(redactionMessages).toHaveCount(2); await expect(redactionMessages.first()).toBeVisible();`＋`非表示` Stat に `.first()`／社長テストは `await expect(...redaction...).toHaveCount(0);`＋`表示可` Stat に `.first()`。**意図不変・むしろ「finance 2枚とも redacted・社長は 0」を件数で厳密化**＝担当者に原価・粗利・未回収の実値が出ないことを担保強化。**実装本体（page.tsx・control-tower.ts・growth-control-tower.ts・nav.ts・index.ts）は一切変更なし**（変更は e2e spec 1本＋docs/tasks）。**検証**: `pnpm test` 271 passed / 0 failed・`pnpm --filter @hokko/web typecheck` exit 0・`pnpm --filter @hokko/shared typecheck` exit 0・`pnpm lint` exit 0・`node scripts/check-company-brain-safety.mjs` exit 0・`git diff --check` OK・secret NONE・禁止領域差分なし・artifact なし。**e2e 実緑は push 後の CI（stage3_e2e）で growth_control_tower 2件を含む 74 passed / 0 failed を確認**。**schema変更なし・migrationなし・seed変更なし・RBAC変更なし・ci.yml変更なし・playwright.config.ts変更なし・package.json/lockfile変更なし・redaction 方針不変・PII 非増加・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・状態永続化追加なし・機能拡張なし・artifact 非 git add・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **P3-CT-1 push-only（別承認）→ CI で 74/0 確認 → P3-CT-2（優先度ロジック精緻化・別承認）**。
- **P3-CT-1 AI Growth Control Tower v0 read-only 画面 実装（docs/roadmap/49 Candidate＋実装）: 実装完了・commit-only（判定 read-only 実装完了・業務 mutation なし・単体271 passed・型/lint/safety緑・e2e実緑はpush後CI・2026-07-08・記録: doc148＋`docs/roadmap/49`）／369-vault非編集**。Gate PASS（doc147/48）と回帰ゲート緑（run 28940565283=72/0・6run連続）を受け、`/growth/control-tower` の **read-only 成長機会管制塔** を最小実装。**実装ファイル**: 新規 `packages/shared/src/growth-control-tower.ts`（DB非依存 純ロジック・カード型・優先度スコアリング・`buildControlTowerCards`/`countActionableCards`・redaction 文言・deterministic）＋`packages/shared/src/__tests__/growth-control-tower.test.ts`（単体6件）＋`packages/shared/src/index.ts`（barrel export 1行）／新規 `apps/web/lib/domains/growth/control-tower.ts`（tenantId スコープ read-only prisma count＋既存 `getGoldenPathExecutiveDashboardData` 再利用＋finance gating＋finance 参照時のみ `writeDataAccess(confidential_view)` 最小1件・金額/PII 非記録）／新規 `apps/web/app/(app)/growth/control-tower/page.tsx`（RSC・force-dynamic・requireUser・canViewFinance=hasPermission(user,'finance','read')・既存 UI 再利用・**mutation/Server Action/form/外部送信導線なし**・重要操作は既存導線 deep link）／変更 `apps/web/components/shell/nav.ts`（Radar import＋「Growth・DX OS」に Growthコントロールタワー 1行）／新規 `apps/web/tests/e2e/growth_control_tower.spec.ts`（read-only smoke＋redaction）。**初期カード9枚**（社長が見るべき成長機会・未回収リスク(finance)・停滞商談・低粗利改善候補(finance)・未追客リード・高機会リード・次回接触推奨・Company Brain 改善候補・既存顧客追加提案候補=件数のみ）。**redaction**: finance 系（未回収リスク・低粗利改善候補）は canViewFinance=false のとき**データ整形層で件数を集計せず null**・UI は `原価・粗利は財務閲覧権限が必要です（機密情報）。`（app 既存文言）表示＝**担当者に金額実値を渡さない**。**Customer 一覧行レベル・Contact 単体は据え置き・生 PII 列を増やさない**（既存顧客カードは Customer 件数のみ）。**AI 境界**: FakeLLM・実LLM/AIコストなし・AI は外部送信/承認/削除なし・v0 は LLM 未呼び出しの deterministic 文言のみ。**STOP 条件非該当**（新規 schema/table/column/enum/migration なし・状態永続化なし(dismiss/snooze/read/pin/priority override なし)・新規 RBAC action/role なし・新規 seed なし・ci.yml/playwright.config.ts/package/lock 非変更）。**検証**: `pnpm test` 271 passed / 0 failed（従来265＋新規6・回帰なし）・`pnpm --filter @hokko/web typecheck` exit 0・`pnpm --filter @hokko/shared typecheck` exit 0・`pnpm lint` exit 0・`node scripts/check-company-brain-safety.mjs` exit 0（ui 156→157）・`git diff --check` OK・secret NONE・禁止領域差分なし・artifact なし。**e2e 実緑は本サンドボックスでは実走不能→push 後の CI（stage3_e2e）で 74 passed / 0 failed（既存72＋新規2）を確認**。**Phase 3 GO 済み・Control Tower v0 は P3-CT-1 実装完了・以降 P3-CT-2〜7 は別承認**。**schema変更なし・migrationなし・seed変更なし・RBAC変更なし・ci.yml変更なし・playwright.config.ts変更なし・package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・業務データ mutation なし・状態永続化なし・artifact 非 git add・network policy回避なし・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **P3-CT-1 push-only（別承認）→ CI で 74/0 確認 → P3-CT-2（優先度ロジック精緻化・別承認）**。
- **AI Growth Control Tower v0 実装前 Gate / schema影響判定 / 最小実装計画（docs/roadmap/48 Candidate）: docs-only 完了（判定 Gate PASS・既存 schema/RBAC/seed のみで成立・実装未着手・2026-07-08・記録: doc147＋`docs/roadmap/48`）／369-vault非編集・コード差分なし（計画のみ）**。roadmap47/doc146 の設計を受け、実装前に v0 が既存 schema・既存 RBAC・既存 seed のみで成立するかを **read-only 実査**で最終判定。**前提 CI**: doc146/roadmap47 push で起動した **run 28939408568（run_number 146・head_sha 39edd19）= completed / success・stage1 success・stage3_e2e success・Run E2E step success・Upload report on failure=skipped**＝stage3_e2e は **5 run 連続で完全 green（72/0）**（28930122157/28934614261/28937029131/28938318122/28939408568）。**read-only 実査**: 既存 `/growth`（`apps/web/app/(app)/growth/page.tsx`）は requireUser＋tenantId prisma 読み取りで成立・mutation のみ hasPermission／`packages/shared/src/rbac.ts` は RESOURCES に deal/finance/marketing/leadmap を持ち READ_ONLY=grant(RESOURCES,['read','ai_read'])＝**既存 read 権限あり**／財務 redaction は `getGoldenPathExecutiveDashboardData`＋`redactExecutiveFinance`/`visibleGoldenPathActions`(@hokko/shared) で **再利用可**／`apps/web/lib/db.ts` に writeAudit・writeDataAccess 実在／`seed.ts` に `prisma.deal.create`(237・deal 出現19)・LocalBusinessLead(3)・CompanyPolicy(2)＋e2e 72/0 で **対象データ seed 済み**／nav.ts「Growth・DX OS」に1行追加可。**判定（10項目）**: ①既存 schema のみで成立=YES／②新規 table/column=NO／③状態永続化(dismiss/snooze/read/pin/priority override)=v0 では**不要・非目標**（採用時は STOP 別承認）／④新規 RBAC action/role=NO／⑤新規 seed=NO／⑥初期カード9案は既存データで表示可（顧客追加提案候補は件数/匿名指標中心）／⑦PII/Contact を増やさず成立=YES（据え置き）／⑧財務値は canViewFinance/redaction で制御=YES／⑨writeAudit/writeDataAccess 配線可／⑩e2e 72/0 を壊さない（追加は read-only smoke＋redaction）。**Gate 判定 = PASS（STOP 非該当）／v0 は既存 schema・既存 RBAC・既存 seed のみで成立**。**P3-CT-1 最小実装計画**を確定（URL `/growth/control-tower`・追加 page.tsx＋`packages/shared/src/growth-control-tower.ts` 純ロジック＋データ整形層・nav 1行・再利用関数・権限/redaction/監査/AI 境界・e2e `growth_control_tower.spec.ts` 追加案・STOP 条件・実装プロンプト案）。**Control Tower v0 は実装未着手（本書は Gate と計画のみ・コードは書いていない）**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・Control Tower v0 実装開始なし・artifact 非 git add・network policy回避なし・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc147/roadmap48 push-only（別承認）→ P3-CT-1 Control Tower v0 read-only 画面 実装ミッション（別承認・実装あり）**。**将来 状態永続化・新規権限・新規 seed が必要になれば実装前に STOP・別承認**。
- **AI Growth Opportunity Control Tower v0 設計（docs/roadmap/47 Candidate）: docs-only 完了（判定 設計完了・実装未着手／schema 影響があれば別承認・2026-07-08・記録: doc146＋`docs/roadmap/47`）／369-vault非編集・コード差分なし（設計のみ）**。Phase 3 GO（doc145/roadmap46）を受けた最初の縦切り **AI Growth Opportunity Control Tower v0** を **docs-only で設計**（実装はしない）。**設計着手の前提 CI**: doc145/roadmap46 push で起動した **run 28938318122（run_number 145・head_sha d36887d）= completed / success・stage1 success・stage3_e2e success・Run E2E step success・Upload report on failure=skipped**＝これで stage3_e2e は **run 28930122157/28934614261/28937029131/28938318122 の 4 run 連続で完全 green（72/0）**。**設計要点**: 目的＝既存データ（LeadMap リード `LocalBusinessLead`・商談 `Deal`・Company Brain・Golden Path・Finance Bridge・Usage/Audit）から成長機会を1画面（`/growth/control-tower`・既存 `/growth`・nav「Growth・DX OS」の隣）に read-only 集約し社長が次の一手を判断／非目標＝外部送信自動化・実LLM・課金・本番 deploy・runtime 解禁・externalAiAllowed true・EXTERNAL_SEND_ENABLED true・新規 schema 先行追加・AI の自動承認/削除/送信はしない／初期カード9案（未追客リード・停滞商談・高機会リード・Company Brain 改善提案・低粗利改善候補・未回収リスク・次回接触推奨・既存顧客追加提案候補・社長が見るべき成長機会）／権限＝tenantId スコープ・hasPermission・canViewFinance・canAccessLabel・**Customer 一覧行レベル/Contact 単体は据え置き・生 PII 列を足さない**／監査＝writeAudit・機密参照 writeDataAccess・UsageEvent 既存方針／AI 境界＝FakeLLM・AI は外部送信/承認/削除なし・生成物は下書き・重要操作は ApprovalRequest／redaction＝既存 `events/[id]/page.tsx` の `canViewFinance ? 金額 : redaction` 踏襲（スタッフに原価・粗利の実値露出なし・件数中心）。**schema 影響判定**: **v0 は既存 schema のみで read-only 集約が成立する見込み**。ただし「カード dismiss/既読 の状態永続化」「新規権限 action/ロール」「新規デモデータ」が必要になった時点で**実装前に STOP して別承認**（schema/migration/RBAC/seed）。**実装フェーズ案** P3-CT-0(設計)〜CT-7(push/CI/Gate)。**テスト方針**: 既存 e2e 72/0 を壊さない・追加は read-only smoke＋redaction 相当・security/HCG/Consent/Suppression 回帰なし。roadmap47 に目的（§1）・非目標（§2）・対象データ（§3）・カード案（§4）・画面（§5）・権限（§6）・監査（§7）・AI 境界（§8）・schema 影響判定（§9）・テスト（§10）・実装フェーズ（§11）・50カテゴリ Matrix（§12・C41-44 直接Phase3開始）・Evidence/Assumption/Unknowns/Risk（§13）を記録。**Phase 3 GO 済みだが Control Tower v0 は設計のみ・実装未着手**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・Control Tower v0 実装開始なし・artifact 非 git add・network policy回避なし・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **Control Tower v0 実装前 Gate / schema 影響判定 / 最小実装計画ミッション（docs-only）**、および doc146/roadmap47 push-only（別承認）。
- **Phase 3 GO 記録（docs/roadmap/46 Candidate）: docs-only 完了（判定 Phase 3 GO・人間判断による正式記録／実装は未着手／次は AI Growth Opportunity Control Tower v0 設計・2026-07-08・記録: doc145＋`docs/roadmap/46`）／369-vault非編集・コード差分なし（記録のみ）**。doc144/roadmap45 の判断材料を受け、人間が Phase 3（AI Growth Engine）進入を **GO** と判断した事実を正式記録。**GO の CI 実測根拠**: push で起動した **run 28937029131（run_number 144・head_sha 18d246f）= completed / success・stage1 success・stage3_e2e success・Run E2E「72 passed (1.0m)」（0 failed）・Upload report on failure=skipped・operations:44 ✓(2.3s)・planning_hokko:45 ✓(1.3s)**。これで stage3_e2e は **run 28930122157/28934614261/28937029131 の 3 run 連続で完全 green（72/0）**。**C=0/D=0/F=0・F3 seed 不要・schema/migration 不要**（tests-only 修正のみで到達）。**redaction 健全**（スタッフが詳細到達も `原価・粗利は財務閲覧権限が必要です（機密情報）。` のみ描画・原価・粗利の実値露出なし）。**封印維持**（EXTERNAL_SEND_ENABLED=false・LLM_PROVIDER=fake・MAIL_PROVIDER=log・externalAiAllowed 既定 false・SuppressionList 強制・Human Certification Gate 維持・Consent 用途別分離・outreach opt-out 正式方針）。**人間 GO 判断6件**: 高機密 runtime 統制②据え置き GO／outreach opt-out 正式化 GO／positive Consent 用途別分離 GO／回帰ゲートは CI 実測で充足 GO／外部送信・実LLM・課金・本番 deploy は個別承認制維持 GO／Phase 3 最初の縦切り＝**AI Growth Opportunity Control Tower v0**。**Phase 3 GO は実装開始ではない**（設計は次ミッション）。**外部送信・実LLM・課金・本番 deploy・runtime 解禁・externalAiAllowed true・EXTERNAL_SEND_ENABLED true は GO 後も各々個別の人間承認まで行わない**。roadmap46 に Phase 2-A 完了（§2）・CI green 3 run（§3）・C/D/F 確定（§4）・F3/schema 不要（§5）・redaction 健全（§6）・封印（§7）・人間 GO 判断（§8）・Phase 3 進入（§9）・Control Tower v0 概要（§10）・個別承認境界（§11）・50カテゴリ Matrix（§13・C41-44 を直接Phase3開始へ格上げ）・Evidence Map（§17）を記録。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/課金/本番なし・runtime 解禁なし・Phase 3 実装開始なし・artifact 非 git add・network policy回避なし・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **AI Growth Opportunity Control Tower v0 設計ミッション（docs-only）**、および doc145/roadmap46 push-only（別承認）。
- **Phase 3 最終 Phase Gate 判断シート（docs/roadmap/45 Candidate）: docs-only 完了（判定 Phase 3 は技術ゲート大部分充足・最終 Phase Gate 人間承認前のため HOLD・2026-07-08・記録: doc144＋`docs/roadmap/45`）／369-vault非編集・コード差分なし（記録のみ）**。人間が Phase 3 GO/HOLD を正式判断するための最終判断材料を1枚に集約（**本書は GO そのものではない・最終判断は人間**）。**技術ゲート充足状況**: ①Phase 2-A 完了記録=充足（doc48・基準85f1bf3）／②高機密ラベル runtime 統制=詳細閲覧は閉・Customer 一覧行レベル/Contact 単体は据え置き（判定 B・doc124-127）＝条件付き充足／③外部送信 HCG=充足（閉）／④Consent/Suppression=充足（isSuppressed 強制）／⑤AI 境界=充足（AI_READABLE_LABELS=NORMAL/INTERNAL・FakeLLM）／⑥**回帰ゲート緑（e2e 含む）=充足（run 28930122157 と 28934614261 の 2 run 連続で stage1 success・stage3_e2e success・Run E2E 72 passed / 0 failed・Upload report on failure=skipped／docs-only push でも stage1・stage3_e2e が起動し両方 green）**。**C=0/D=0/F=0 最終確定・F3 seed 不要・schema/migration 不要**。**redaction 健全**（operations:44・planning_hokko:45 green＝スタッフが詳細到達も `原価・粗利は財務閲覧権限が必要です（機密情報）。` のみ描画・原価・粗利の実値露出なし）。**Global AI Rules 現状値**: LLM_PROVIDER=fake・MAIL_PROVIDER=log・EXTERNAL_SEND_ENABLED=false・externalAiAllowed 既定 false。**まだ GO でない理由=最終 Phase Gate 人間承認が未実施**。**人間が判断すべき論点**: 高機密 runtime 統制②の据え置き可否・outreach opt-out 正式化・positive Consent 用途別分離・回帰ゲートは CI 実測で可か・Phase 3 最初の縦切りスコープ（封印を伴わない設計か）・GO 後も EXTERNAL_SEND_ENABLED/externalAiAllowed true 解禁/実LLM/実課金/本番 deploy は個別承認必須の原則維持。**GO の初手**=Phase 3 GO 記録ミッション（docs-only・封印維持）→Phase 3 最小縦切り設計（docs-only）。**HOLD の初手**=高機密 runtime 統制②格上げ設計 or Phase 3 縦切り候補棚卸し（docs-only）。**Phase 3 進入は引き続き HOLD**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・runtime 解禁なし・artifact 非 git add・network policy回避なし・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc144/roadmap45 push-only**（別承認）→ 人間による **最終 Phase 3 Phase Gate 承認（GO/HOLD 正式判断）**。GO の場合のみ Phase 3 GO 記録ミッションへ。
- **CI Stage 3 E2E F1e green result（docs/roadmap/44 Candidate）: docs-only 完了（判定 F1e完全成功／stage3_e2e green／72 passed 0 failed／Phase 3 は最終 Phase Gate 承認前のため HOLD・2026-07-08・記録: doc143＋`docs/roadmap/44`）／369-vault非編集・コード差分なし（記録のみ）**。F1e tests-only 修正コミット `a6447b9` の push 後 CI **run 28930122157（run_number 142・event push・head_sha a6447b9）= completed / success** を read-only 実測で記録＝**stage1 success**（safety/unit/typecheck/lint 全 success）／**stage3_e2e success**（ephemeral Postgres/migrate/seed/build/chromium/**Run E2E success**）／**「Upload Playwright report and traces on failure」= skipped**（失敗ゼロの傍証）／**Run E2E = 72 passed / 0 failed**（前回 run 28927924922 = 66/6 → **+6 passed / −6 failed**・退行ゼロ・env `LLM_PROVIDER=fake`/`MAIL_PROVIDER=log`/`EXTERNAL_SEND_ENABLED=false`）。**F1e対象6件すべて green**（ログ ✓）: operations:44（2.5s）・planning_hokko:16/24/35/45・golden_path_actions:15。**redaction 2件（operations:44・planning_hokko:45）green＝スタッフが実案件詳細に到達し `原価・粗利は財務閲覧権限が必要です（機密情報）。`（events/[id]/page.tsx line173・canViewFinance 偽）が描画・原価・粗利の実値は非表示＝機密漏えいなし**。他の「スタッフは機密を閲覧できない」系（executive_dashboard:47・finance_bridge:40・finance_formalize:30・invoice_payment:32・operations_exec:50・dunning:70/81 等）もすべて ✓。**C=SEED_DATA_DRIFT=0／D=TRUE_APP_BUG=0／F=INSUFFICIENT_EVIDENCE=0 を CI 実測で最終確定**（tests-only 修正のみで 72/0＝seed/schema/app 無変更）。**F3 seed / schema 変更は不要**（確定）。**regression なし**（F1d 緑化 4件維持・既存 66 件維持＝72/72）。**safety/HCG/Consent/Security/Marketplace Gate 影響なし・Global AI Rules 維持**。**Phase 3 GO 条件⑥（回帰ゲート緑＝e2e含む）は技術的に充足したが、Phase 3 進入は最終 Phase Gate 人間承認が必須のため HOLD 継続**（本記録は「Phase 3 GO」ではない）。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・runtime 解禁なし・artifact download/git add なし・network policy回避なし・force push/amend/rebase/reset なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc143/roadmap44 push-only**（別承認）→ 人間による **最終 Phase 3 Phase Gate 承認（GO/HOLD 正式判断）**、または **Phase 3 GO 記録ミッション**（人間が GO を選択した場合）。
- **CI Stage 3 E2E F1e tests-only fix（docs/roadmap/43 Candidate＋e2e spec 3本）: tests-only 完了（判定 F1e適用完了／CI_STAGE3_E2E_RED 継続（push→CI 緑化まで）／Phase 3 HOLD・2026-07-08・記録: doc142＋`docs/roadmap/43`）／369-vault非編集・app本体変更ゼロ**。doc141/roadmap42 で確定した残6件（C=0/D=0・真因 tests-only）を **e2e spec のみ**で最小修正。**app read-only 確認**: `operations/events/[id]/page.tsx` に `Golden Path — 現在地と次の一手`（CardTitle line96）・`粗利率` Stat（line170）・`原価・粗利は財務閲覧権限が必要です（機密情報）。`（line173）が実在し、財務欄は `canViewFinance ? 金額 : redaction`（line165-174）で**権限どおり出し分け**（社長=金額／スタッフ=redaction・機密値非表示）＝**app 健全・D 証跡なし**。**修正**: golden_path_actions:15＝`getByText('対処:')`→`getByRole('heading',{name:/推奨アクション/})`（B・cockpit 実在見出し）／planning_hokko:16＝`getByText('Golden Path — 現在地と次の一手')`→`getByRole('heading',{name:/今すぐ見るべき案件/})`（B・/planning-hokko 実在見出し）／planning_hokko:24/35/45＝一覧セレクタを `a[href^="/operations/events/"]:not([href$="/new"])').first()` へ（A・/new 除外で実案件詳細へ／:24 assertion は heading 化）／operations:44＝作成ステップに `input[name="revenue"]` 入力＋`Promise.all([waitForURL(/…events\/(?!new…)/), click])` で実 detail URL を race-safe 捕捉（A/setup）。**redaction/finance assertion（原価・粗利は財務閲覧権限が必要です・粗利率・Golden Path — 現在地と次の一手）は app 実在のため維持**（tests 側の navigation/expected を実在に整合）。**検証**: `pnpm --filter @hokko/web typecheck` exit 0・`pnpm lint` exit 0・`node scripts/check-company-brain-safety.mjs` exit 0・diff --check OK・secret NONE・旧パターン grep=0・新パターン grep=存在。e2e 実緑は push 後の CI（stage3_e2e）で確認（**66 passed / 6 failed → 72/0** 見込み）。**F3 seed / schema は不要**（C=0）。**残 F 論点**: redaction 2件の detail 到達後の描画は push 後 CI で確認（app 上は line173 実在・権限出し分け／出ない or 機密露出なら D=TRUE_APP_BUG として停止・人間承認）。**失敗は表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・runtime 解禁なし・artifact 非 git add・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1e push-only**（別承認）→ CI で 72/0＋redaction 2件緑を確認 → stage3_e2e green → 最終 Phase Gate 承認。
- **CI Stage 3 E2E C6 artifact analysis（docs/roadmap/42 Candidate）: docs-only 完了（判定 C=0/D=0/F=0・真因 tests-only／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-08・記録: doc141＋`docs/roadmap/42`）／369-vault非編集・コード差分なし（記録のみ）**。ローカル到達 artifact（**playwright-report・ID 8158827253・run 28919165959**・チャット添付）を read-only で /tmp 展開し、残 6 failed の **error-context.md（ARIA スナップショット）＋screenshot** を目視分析（trace は存在確認のみ・zip/展開物/screenshot/trace は **git add せず**）。**重要訂正**: 前分類「C暫定6件」は誤りで、**artifact 目視により C=SEED_DATA_DRIFT=0／D=TRUE_APP_BUG=0／F=INSUFFICIENT_EVIDENCE=0**（データは存在・アプリ健全）と確定。**真因は tests-only の A=TEST_SELECTOR_DRIFT/setup 4件＋B=TEXT_EXPECTATION_DRIFT 2件**: golden_path_actions:15＝**B**（/dashboard/ceo 完全描画・「今すぐ見るべき案件」あり・`対処:` 不在＝文言ドリフト）／planning_hokko:16＝**B**（/planning-hokko に3案件・KPI・`対処:`・財務値あり・`Golden Path — 現在地と次の一手` literal 不在）／planning_hokko:24/35/45＝**A**（`page.locator('a[href^="/operations/events/"]').first()` が一覧先頭の「イベント案件を作成」(/new) リンクを掴み作成フォームへ・案件は seed に3件存在）／operations:44＝**A/setup**（作成ステップ想定売上未入力＋`waitForURL('**/operations/events/**')` race で url=/new 捕捉）。**redaction 2件（operations:44・planning_hokko:45）はスタッフが /operations/events/new 作成フォームに留まり、原価・粗利・金額いずれの機密値も露出なし＝D重大（機密漏えい）なし**。社長は /planning-hokko で財務値を正当に閲覧（権限どおり）。**F3 seed / schema 変更は不要**（C=0）。緑化は **tests-only（F1e）で 66/6→72/0 到達可能**。残 F 論点: redaction メッセージの描画正当性は detail 未到達のため未検証（漏えいは無い・F1e 後に再確認）。**失敗は表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・runtime 解禁なし・artifact blob URL curl なし・network policy回避なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1e tests-only 修正**（別承認）→ CI で 66/6→72/0 確認 → stage3_e2e green → 最終 Phase Gate 承認。
- **CI Stage 3 E2E F1d result（docs/roadmap/41 Candidate）: docs-only 完了（判定 F1d完全成功／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-08・記録: doc140＋`docs/roadmap/41`）／369-vault非編集・コード差分なし（記録のみ）**。F1d push 後の CI run 28917520358（run_number 139・push・head_sha 5dda39d8…）実測を記録＝**stage1 success**／**stage3_e2e failure**（基盤 migrate/seed/build/browser install は全緑・"Run E2E (playwright)" step のみ failure）／**Run E2E = 66 passed / 6 failed**（F1d前 run 137 = **62 passed / 10 failed**・差分 **+4 passed / -4 failed**・退行ゼロ・env `LLM_PROVIDER=fake`/`MAIL_PROVIDER=log`/`EXTERNAL_SEND_ENABLED=false`）。**F1d完全成功**: dunning:15・dunning:50・executive_dashboard:15・executive_dashboard:37 が**失敗一覧から消滅**（A=**TEST_SELECTOR_DRIFT** 完全消化・残 A=0）。**executive_dashboard:15 の後続 Golden Path KPI 文言は C 転移なし**（後続アサーションまで全通過）。**regression なし**。**残りは C暫定6件のみ**（golden_path_actions:15・operations:44・planning_hokko:16/24/35/45＝element-not-found・**SEED_DATA_DRIFT**）。**D=TRUE_APP_BUG は証跡なし（0）**（redaction 2件は機密値露出の証跡なし）。artifact 生成済み（**playwright-report・ID 8158244025・17725314 bytes・54 files**・本環境 proxy 403 で**バイナリ download せず**）。**失敗は表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・runtime 解禁なし・network policy回避なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc140/roadmap41 push-only**（別承認）→ 残 C暫定6件の C/D 確定（artifact screenshot 人手取得 or network 許可）→ **F3**（C=6件 seed・schema影響事前停止条件・別承認）。
- **CI Stage 3 E2E F1d test selector fix（docs/roadmap/40 Candidate＋e2e spec 2本）: tests-only 完了（判定 F1d適用完了／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-08・記録: doc139＋`docs/roadmap/40`）／369-vault非編集・app本体変更ゼロ**。F2 log-based 分析（doc138/roadmap39）で **log 確定**した **strict-mode** 4件（A=**TEST_SELECTOR_DRIFT**）を **e2e spec のみ**で最小修正。**修正**: dunning:15（:23）`#dunning内 getByText(/督促/)`→`getByRole('heading',{name:/入金確認・督促/})`／dunning:50（:53）`getByText('承認待ち')`→`getByRole('heading',{name:'承認待ち'})`／executive_dashboard:15（:19）`getByText('社長コックピット',{exact:false})`→`getByRole('heading',{name:'社長コックピット'})`／executive_dashboard:37（:43）`getByRole('link',{name:/プランニングホッコー/})`→`getByRole('link',{name:'プランニングホッコー →'})`＋**同一テスト内の連続する戻り導線で次に露見する同型 strict-mode を予防した最小追加**として :44 `getByRole('link',{name:/社長コックピット/})`→`getByRole('link',{name:'社長コックピット →'})`。要素はいずれも画面に**描画済み**（**SEED_DATA_DRIFT** ではない・#dunning に「未回収 ¥1,320,000」表示・heading/本文リンクは app read-only で実在確認）。**検証**: `pnpm --filter @hokko/web typecheck` exit 0・`pnpm lint` exit 0・`node scripts/check-company-brain-safety.mjs` exit 0・diff --check OK・secret NONE・旧曖昧セレクタ4件 grep=0・新セレクタ4件 grep=存在。e2e 実緑は push 後の CI（stage3_e2e）で確認（**62 passed / 10 failed → 66/6** 見込み）。**C暫定6件は未修正**（golden_path_actions:15・operations:44・planning_hokko:16/24/35/45＝**SEED_DATA_DRIFT**・F3 別承認・schema 影響事前停止条件）。**D=TRUE_APP_BUG は0のまま**。**失敗は表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・runtime 解禁なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1d push-only**（別承認）→ CI で 62/10→66/6 見込み確認 → artifact screenshot 確定 → **F3**（C=6件 seed・別承認）。
- **CI Stage 3 E2E F2 log-based artifact-blocked analysis（docs/roadmap/39 Candidate）: docs-only 完了（判定 log-based分類完了／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-07・記録: doc138＋`docs/roadmap/39`）／369-vault非編集・コード差分なし（記録のみ）**。F2 artifact（playwright-report・id 8145981012・29933595 bytes・expired=false・expires 2026-07-14）はメタデータ取得成功も、**Azure blob storage への download が proxy 403（CONNECT tunnel failed）でブロック＝artifact取得不能**（network policy 回避せず）。代替として run 28885319767 の job logs（read-only）の error/locator/strict-mode/element-not-found を一次証跡に **log-based 分類**。**重大訂正**: doc136/roadmap37 の「残10件は全て C=SEED_DATA_DRIFT」は誤りで、フルログにより **dunning:15・dunning:50・executive_dashboard:15・executive_dashboard:37 の4件は strict-mode（A=TEST_SELECTOR_DRIFT）＝要素は描画済み**（例: #dunning に「未回収 ¥1,320,000」表示・社長コックピット/承認待ち/プランニングホッコー link は2要素一致）と判明。**最終分類: A=4（log確定・tests-only可）／C暫定=6（golden_path_actions:15・operations:44・planning_hokko:16/24/35/45＝element-not-found・screenshot未確認でC暫定＋F=INSUFFICIENT_EVIDENCE）／D=TRUE_APP_BUG=0（証跡なし・security.spec全passed）／E=0**。redaction系2件（operations:44・planning_hokko:45）は機密値露出の証跡なし＝D断定せず。**Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。封印維持（EXTERNAL_SEND_ENABLED=false・FakeLLM・externalAiAllowed 既定false）。**Phase 3 進入は引き続き HOLD**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/playwright.config.ts/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・network policy回避なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1d**（A=4件 tests-only 修正・別承認）→ artifact screenshot 確定（人手 download or network 許可）→ **F3**（C=6件 seed・schema影響事前停止条件・別承認）。
- **CI Stage 3 E2E F2 diagnostics（docs/roadmap/38 Candidate＋config 2本）: config-only 完了（判定 F2診断基盤追加完了／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-07・記録: doc137＋`docs/roadmap/38`）／369-vault非編集・app本体変更ゼロ・テスト合否条件不変**。残 C10件の C/D 最終確定に向け、CI 失敗時に **html report / trace / screenshot** を **artifact** 取得できる診断基盤を追加。**原因**: `reporter:'list'`（html未生成）＋`trace:'on-first-retry'` だが `retries:0`（trace 永久未取得）＝直近 run 28882890123 で `No files were found with the provided path: apps/web/playwright-report`。**F2 変更（config-only）**: `apps/web/playwright.config.ts`（`reporter:[['list'],['html',{outputFolder:'playwright-report',open:'never'}]]`／`use.trace:'retain-on-failure'`／`use.screenshot:'only-on-failure'`）・`.github/workflows/ci.yml`（artifact upload path に `apps/web/playwright-report`＋`apps/web/test-results` を追加・env/service/実行ステップは不変）。**html reporter は @playwright/test 組込のため package.json/lockfile 変更なし**・test-results/playwright-report は .gitignore 済み。**検証**: `pnpm --filter @hokko/web typecheck` exit 0・`pnpm lint` exit 0・safety exit 0・diff --check OK・secret NONE。**テスト結果は不変（62 passed / 10 failed・診断基盤のみ）**。**CI_STAGE3_E2E_RED は C10件残存のため継続**（残: dunning:15/50・executive_dashboard:15/37・golden_path_actions:15・operations:44・planning_hokko:16/24/35/45）。D=TRUE_APP_BUG は0のまま（F2 artifact で最終確定）。**失敗は表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・tests期待値変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals変更なし・package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F2 push-only**（別承認）→ CI で artifact 取得 → 残 C10件を C（SEED_DATA_DRIFT）/D（TRUE_APP_BUG）最終確定（F3 データ整合は別承認）。
- **CI Stage 3 E2E F1c result（docs/roadmap/37 Candidate）: docs-only 完了（判定 CI_STAGE3_E2E_RED / C10件へ収束 / Phase 3 HOLD・2026-07-07・記録: doc136＋`docs/roadmap/37`）／369-vault非編集・app本体変更ゼロ（コード差分なし・記録のみ）**。F1c push 後の CI run 28879821278（run_number 135・HEAD c2d07c5）実測を記録＝**stage1 success**／**stage3_e2e failure**／**Run E2E = 62 passed / 10 failed**（F1c前 60/12・差分 +2 passed / −2 failed・退行ゼロ・基盤 migrate/seed/build/browser 全 success・env `LLM_PROVIDER: fake`/`EXTERNAL_SEND_ENABLED: false` 維持）。**F1c対象2件はいずれも緑化**: finance_bridge:15 `FinanceEvent`（3要素 strict-mode→heading）・invoice_payment:25 `入金実績`（2要素 strict-mode→exact true）＝失敗一覧から消滅。**strict-mode（A=TEST_SELECTOR_DRIFT・計7件）/ 文言ドリフト（B・1件）は全消化**。**残10件は全て C=SEED_DATA_DRIFT へ収束**（dunning:15/50・executive_dashboard:15/37・golden_path_actions:15・operations:44・planning_hokko:16/24/35/45＝element-not-found・前提データ不足）。**D=TRUE_APP_BUG は0のまま**（security.spec passed・redaction 失敗は前提イベント未生成であり機密漏えいではない）。**失敗は operations/planning/finance/dunning/executive の表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・tests変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F2 診断**（別承認・playwright html reporter+trace を ci.yml/config に追加→残 C10件の screenshot/trace 取得で C/D 最終確定）。
- **CI Stage 3 E2E F1b result and F1c（docs/roadmap/36 Candidate＋e2e spec 2本）: tests-only/docs-only 完了（判定 F1c適用完了／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-07・記録: doc135＋`docs/roadmap/36`）／369-vault非編集・app本体変更ゼロ**。F1b push 後の CI run 28876766413（run_number 134）実測を記録＝**stage1 success**／**stage3_e2e failure**／**Run E2E = 60 passed / 12 failed**（F1b前 59/13・F1前 57/15・差分 +1 passed / −1 failed・退行ゼロ・基盤緑）。**F1b で operations_exec spec 全緑**（:15 棚卸・:22 案件名・:39 人員配置/リスク すべて緑・失敗一覧に operations_exec 系 0件）。**残12件の分類精緻化**: CI ログ実エラーで finance_bridge:15 `getByText('FinanceEvent')`（3要素）・invoice_payment:25 `getByText('入金実績')`（2要素）が**新規 strict-mode（A=TEST_SELECTOR_DRIFT）**と判明、残10件は element-not-found の C=SEED_DATA_DRIFT（dunning:15/50・executive_dashboard:15/37・golden_path_actions:15・operations:44・planning_hokko:16/24/35/45）。**F1c（tests-only 2件）**: finance_bridge:19 →`getByRole('heading',{name:'直近の FinanceEvent'})`／invoice_payment:29 →`getByText('入金実績',{exact:true})`。**検証**: `pnpm --filter @hokko/web typecheck` exit 0・`pnpm lint` exit 0・旧曖昧セレクタ 2件 grep=0・safety exit 0。e2e 実緑は push 後の CI（stage3_e2e）で確認（12→10 見込み）。**CI_STAGE3_E2E_RED は C10件残存のため継続**（F2 診断→F3 データ整合へ）。D=TRUE_APP_BUG は0のまま。**失敗は operations/planning/finance/dunning/executive の表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1c push-only**（別承認）→ CI stage3_e2e 再実行で FinanceEvent/入金実績の緑化を確認（12→10 見込み）→ 残 C10件は F2 診断（別承認）。
- **CI Stage 3 E2E F1 result and F1b（docs/roadmap/35 Candidate＋e2e spec 1本）: tests-only/docs-only 完了（判定 F1b適用完了／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-07・記録: doc134＋`docs/roadmap/35`）／369-vault非編集・app本体変更ゼロ**。F1 push 後の CI run 28873089407 実測を記録＝**stage1 success**／**stage3_e2e failure**／**Run E2E = 59 passed / 13 failed**（F1前 57 passed / 15 failed・差分 +2 passed / −2 failed・退行ゼロ）。**F1対象4件**: operations_exec:15 棚卸=緑化／operations:22 E2Eテスト案件=緑化／operations_exec:39 人員配置=部分成功（line45通過・line46 リスク strict-mode 露見）／planning_hokko:16 Golden Path=文言反映もカード未描画で C（SEED_DATA_DRIFT）へ転移。**F1b**: operations_exec:39 line46 `getByText('リスク',{exact:false})`（4要素一致=strict-mode）→`getByRole('heading',{name:/リスク/}).first()`（見出し厳密化・A=TEST_SELECTOR_DRIFT・app表示正常）。物流タスク行は赤化なしのため非変更。**検証**: `pnpm --filter @hokko/web typecheck` exit 0・`pnpm lint` exit 0・旧曖昧セレクタ grep=0・safety exit 0。e2e 実緑は push 後の CI（stage3_e2e）で確認。**CI_STAGE3_E2E_RED は C=SEED_DATA_DRIFT 11件残存のため継続**（F2 診断→F3 データ整合へ）。D=TRUE_APP_BUG は0のまま。**失敗は operations/planning/finance の表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1b push-only**（別承認）→ CI stage3_e2e 再実行でリスク行緑化を確認（13→12 見込み）→ 残 C11件は F2 診断（別承認）。
- **CI Stage 3 E2E F1 test fix（docs/roadmap/34 Candidate＋e2e spec 3本）: tests-only 完了（判定 F1適用完了／CI_STAGE3_E2E_RED 継続／Phase 3 HOLD・2026-07-07・記録: doc133＋`docs/roadmap/34`）／369-vault非編集・app本体変更ゼロ**。doc132/roadmap33 の F1（A=TEST_SELECTOR_DRIFT 3件＋B=TEXT_EXPECTATION_DRIFT 1件）を tests-only で最小修正。**修正**: operations_exec:15 棚卸（`heading /棚卸/`→`'棚卸（実地在庫照合）'`）／operations_exec:39 人員配置（`getByText`→`getByRole('heading',{name:/人員配置/}).first()`）／operations:22 案件名（`getByText('E2Eテスト案件')`→`getByRole('heading',...)`＋同テスト粗利率 `.first()`）／planning_hokko:16 expected text（→「Golden Path — 現在地と次の一手」）。**検証**: `pnpm --filter @hokko/web typecheck` exit 0・`pnpm lint` exit 0・旧文言除去 OK・safety exit 0。e2e 実緑は push 後の CI（stage3_e2e）で確認。**CI_STAGE3_E2E_RED は C=SEED_DATA_DRIFT 11件残存のため継続**（F2 診断→F3 データ整合へ）。D=TRUE_APP_BUG は0のまま。**失敗は operations/planning/finance の表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系は緑（安全中核ゲート影響なし）**。**Phase 3 進入は引き続き HOLD**。**app変更なし・seed変更なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **F1 push-only**（別承認）→ CI stage3_e2e 再実行で該当4件緑を確認 → 残 C11件は F2 診断（別承認）。
- **CI Stage 3 E2E root-cause plan（docs/roadmap/33 Candidate）: docs-only 完了（判定 CI_STAGE3_E2E_RED / PLAYWRIGHT_FAIL / HOLD・2026-07-07・記録: doc132＋`docs/roadmap/33`）／369-vault非編集・実装なし（コード差分ゼロ）**。CI run 28860862696 の15失敗を spec ごとに read-only で root-cause 深掘り。期待文言を app/components に rg 突合＋seed 確認。**最終分類**: A=TEST_SELECTOR_DRIFT（strict-mode）**3件確定**（operations_exec:15 棚卸/39 人員配置・operations:22 案件名＝2要素一致・テスト側`.first()`/exact化・app変更不要）／B=TEXT_EXPECTATION_DRIFT **1件確定**（planning_hokko:16「現在地と次の一手（Golden Path）」が app に不在＝app は「Golden Path — 現在地と次の一手」・テスト期待値更新）／C=SEED_DATA_DRIFT **11件（優勢・要ライブ確認）**（dunning:15/50・executive_dashboard:15/37・finance_bridge:15・golden_path_actions:15・invoice_payment:25・operations:44・planning_hokko:24/35/45＝期待文言はappに存在するが「一覧→先頭クリック→詳細」型で前提データ不足が主因）／**D=TRUE_APP_BUG=0確定**（期待文言ほぼ存在＋security.spec passed＝権限redact機能）／E=0。**最小修正計画 F1-F4**: F1=テスト側4件（A3＋B1・低リスク・app/seed非依存）／F2=診断補強（playwright html reporter+trace・現状report未生成でscreenshotなし）／F3=seed/データ整合（C11件・schema影響の事前停止条件付き）／F4=Dが出た場合のみ。順序 F1→F2→F3→(F4)。**失敗は operations/planning/finance/dunning の表示系で、Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系はゼロ（安全中核ゲート HCG/Consent/Security 緑・影響なし）**。**Phase 3 GO 条件⑥（回帰ゲート緑＝e2e含む）未達→Phase 3 進入 HOLD 継続**。**実装なし・E2E/UI/seed修正なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc132 push-only**（別承認）または F1（A=strict-mode 3件＋B 1件の最小テスト修正・別承認）。
- **CI Stage 3 E2E failure triage（docs/roadmap/32 Candidate）: docs-only 完了（判定 CI_STAGE3_E2E_RED / PLAYWRIGHT_FAIL / HOLD・2026-07-07・記録: doc131＋`docs/roadmap/32`）／369-vault非編集・実装なし（コード差分ゼロ）**。push 後の CI run 28860862696（HEAD 1e8bcd1）を read-only 分析。**stage1 success**／**stage3_e2e failure**だが基盤ステップ（**migrate success**/**seed success**/**build success**/browser install）は全成功、**Run E2E** のみ失敗＝**57 passed / 15 failed**（**PLAYWRIGHT_FAIL**・環境/CI設定は正常）。**15失敗の分類**: A=TEST_SELECTOR_DRIFT（strict-mode）**3件確定**（棚卸/人員配置/案件名＝テスト側の軽微修正）／B=TEXT_EXPECTATION_DRIFT・C=SEED_DATA_DRIFT・D=TRUE_APP_BUG **12件（要精査）**（dunning/executive_dashboard/finance_bridge/golden_path_actions/invoice_payment/operations/planning_hokko の表示系）／E=ENV_RELATED=0。**失敗領域に Customer一覧/LocalBusinessLead/Contact/SuppressionList などの機密・同意・CRM閲覧統制系はゼロ**（Phase 3 安全中核ゲート HCG/Consent/Security は緑・影響なし）。**Phase 3 GO 条件⑥（回帰ゲート緑＝e2e含む）は未達→Phase 3 進入 HOLD 継続**。修正優先度 P1=A3件（テスト側・低リスク）／P2-4=B/C/D（read-only 深掘りで確定・別承認）。**実装なし・E2E/UI/seed修正なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/ci.yml/package.json/lockfile変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc131 push-only**（別承認）または e2e 15件の root-cause 深掘り→修正計画（doc132/roadmap33・実装は承認後の別ミッション）。
- **CI Stage 3 E2E 実装（docs/roadmap/31 Candidate＋`.github/workflows/ci.yml`）: 実装完了・ローカル検証 green・e2e 実 green は push→CI で確認・2026-07-07・記録: doc130＋`docs/roadmap/31`／369-vault非編集**。doc129/roadmap30 の承認済み設計に従い `ci.yml` に **stage3_e2e** ジョブを追加（`stage1` は不変）。**事前停止条件を read-only で解消**（seed=DEMOのみ・外部/secrets なし／web は Redis/S3 起動非依存／`.env` gitignore 済／datasource=DATABASE_URL＋DIRECT_URL）。**stage3_e2e**: needs stage1・**ephemeral Postgres service（pgvector/pgvector:pg16・本番DB非接続・使い捨て）**に `db:migrate:deploy`＋`db:seed`（DEMO）→ `pnpm build` → `playwright install chromium` → `test:e2e`（12 spec）→ 失敗時 report artifact。**封印維持**: `EXTERNAL_SEND_ENABLED=false`・`LogEmailProvider`・`LLM_PROVIDER=fake`（FakeLLM）・`externalAiAllowed` 既定false・runtime 解禁なし・secrets 不要。CI 専用 `.env`（非機密・gitignore 済・未コミット）で dotenv 依存を解消。**ローカル検証 green**（YAML パース OK・env 安全値・stage1 不変・実 secrets なし・lockfile 不変・safety exit 0）。**e2e の実 green は本サンドボックスでは実走不能（Postgres/Actions なし）→ push 後の CI 実行で確認**。**判定＝CI 実装完了／Phase 3 進入は HOLD（残＝e2e 実 green＋最終 Phase Gate 承認）**。**schema変更なし・新規 migration作成なし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals/app コード変更なし・外部送信/実LLM/AIコスト/本番/本番DB接続 なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **ci.yml＋roadmap31 push**（別承認・push すると CI で stage3_e2e が実行される）→ e2e 結果確認。
- **CI Stage 3 E2E 追加 設計（docs/roadmap/30 Candidate）: docs-only 完了（判定 HOLD・設計のみ・2026-07-07・記録: doc129＋`docs/roadmap/30`）／369-vault非編集・実装なし（コード差分ゼロ・.github 実変更なし）**。CI 確認で判明した CI_E2E_NOT_CONFIGURED（run 28859371179=success・stage1 の safety/test/typecheck/lint green・e2e/build は CI 未組込＝別承認）を受け、Phase 3 残条件「e2e green 確認」を満たすため CI に Stage 3 E2E（playwright）を安全に足す設計を整理。**設計要点**: CI に別ジョブ stage3_e2e を追加（needs stage1 or 別ワークフロー）／**ephemeral Postgres service（本番DB非接続）**に `db:migrate:deploy`＋`db:seed`（デモ値）→ `pnpm build` → `pnpm start` → playwright 12 spec 実行／封印維持＝EXTERNAL_SEND_ENABLED 既定OFF・LogEmailProvider・FakeLLM・externalAiAllowed 既定false・runtime 解禁なし・secrets 不要。**事前停止条件**: DATABASE_URL 注入方式（db script の dotenv `.env` 前提の解消）・seed の外部/secrets 依存有無・本番系非接触。**判定＝HOLD（設計 docs-only 完了・実装は別 Phase Gate 承認）**。**実装なし・.github 実変更なし・schema変更なし・新規 migration作成なし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals 変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・本番DB非接続・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **roadmap30/設計 push-only**（別承認）または 本設計の人間承認 → CI Stage 3 E2E 実装（別ミッション）。
- **Phase 3 Gate 移行判断の確定（docs/roadmap/29 Candidate）: docs-only 完了（判定＝方針決定 GO／Phase 3 進入 HOLD・2026-07-07・記録: doc128＋`docs/roadmap/29`）／369-vault非編集・実装なし（コード差分ゼロ）**。roadmap28/doc127 の6論点を人間が「推奨どおり承認」＋回帰ゲートを実測。**§0 人間決定10項目**（VIEW_LIST_ROW_LEVEL_POLICY=DEFER_KEEP_NO_RAW_PII_NOW／LEAD_PII_VIEW_POLICY=KEEP_CURRENT_ADD_DATAACCESSLOG_LATER／CONTACT_VIEW_POLICY=PARENT_CUSTOMER_LABEL_SUBORDINATE／OUTREACH_SEND_POLICY=OPT_OUT_SUPPRESSION_ENFORCED_APPROVED／POSITIVE_CONSENT_POLICY=PURPOSE_SEPARATED_NOT_MANDATORY_FOR_OUTREACH／REGRESSION_GATE_POLICY=REQUIRED_GREEN_BEFORE_PHASE3／E2E_VERIFICATION_POLICY=VERIFY_VIA_CI_RESULT／HIGH_CONFIDENTIAL_RUNTIME_POLICY=NO_RUNTIME_ENABLEMENT_NOW／PHASE3_ENTRY_POLICY=HOLD_UNTIL_E2E_CI_GREEN_AND_PHASE_GATE／IMPLEMENTATION_POLICY=DOCS_ONLY_NOW）。**回帰ゲート実測（`pnpm install --frozen-lockfile`・lockfile不変・main 6f4e16e）＝PARTIAL_GREEN**: safety/test(265)/typecheck/lint/build=GREEN・e2e=ENV_BLOCKED（DATABASE_URL未設定・seed済DB/起動サーバー不在）・RED=0。e2e は CI 実績で確認する方針。**判定＝方針決定 GO（6論点・§0 10項目を人間承認）／Phase 3 進入は残条件2点（e2e CI green 確認＋最終 Phase Gate 承認）まで HOLD**。格上げ実装時は schema/RBAC 変更の要否を事前停止条件として別承認化。**実装なし・runtime 解禁なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals 変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **roadmap29/確定記録 push-only**（別承認）または CI の e2e 結果確認 → doc129。
- **Phase 3 Gate 人間判断チェックリスト（docs/roadmap/28 Candidate）: docs-only 完了（判定 B: HOLD・2026-07-07・記録: doc127＋`docs/roadmap/28`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc126/roadmap27 の設計判断を受け、Phase 3 進入可否を人間が決めるための Phase Gate チェックリストを整理。**Gate 残件6条件**（①Phase 2完了正式記録②高機密ラベル runtime 統制＝詳細は閉/一覧・lead・Contact 未接続③外部送信 Human Certification Gate＝閉④Consent・SuppressionList＝Suppression 送信ゲート強制/positive Consent 用途別⑤AI境界＝閉⑥回帰ゲート green 未実測）。**人間判断6論点（推奨）**: ①Customer一覧＝据え置き（生PII列不追加・高機密運用時に格上げ）②LocalBusinessLead/LocalBusinessContact＝当面現状維持・後日 DataAccessLog 一体化③Contact 単体＝親 Customer ラベル従属④outreach opt-out（SuppressionList 強制）＝正式承認可⑤positive Consent（ConsentRecord）＝用途別分離⑥回帰ゲート green＝Phase 3 前必須条件化。**GO条件**＝①〜⑥充足＋人間 Phase Gate 承認／**HOLD条件**＝②方針未決定 or ⑥未実測 or 承認未取得。格上げ選択時は schema/RBAC 変更の要否を事前停止条件として別承認化。**Phase 3 進入は引き続き HOLD**。**runtime 解禁なし・実装なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers/approvals 変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **roadmap28/チェックリスト push-only**（別承認）または 人間の6論点決定・回帰ゲート green 確認。
- **Phase 3前残件の設計判断メモ（docs/roadmap/27 Candidate）: docs-only 完了（判定 B: HOLD・2026-07-07・記録: doc126＋`docs/roadmap/27`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc125（判定 B: HOLD）＋外部送信・Consent・AI境界の read-only 追加監査を受けた設計判断の整理。**確認済み**: Customer 詳細は `assertCanViewConfidential`＋`canAccessLabel`＋`writeDataAccess`（`DataAccessLog`/`confidential_view`）で閉／外部送信は `decideApprovalAction` が `SuppressionList` を強制（`isSuppressed` 該当は送信せず・lead stage EXCLUDED・usage未計上）・`executeApprovedAction` で二重実行防止・実送信は `EXTERNAL_SEND_ENABLED`==='true' のみで既定 `LogEmailProvider`（ネット送信なし）・AIは `assertAiToolAllowed` で external_send 直接実行不可・AI参照は NORMAL/INTERNAL のみ・`FakeLLM` 決定論・`externalAiAllowed` 既定 false 固定。**設計判断（人間 Phase Gate 承認事項）**: ①Customer一覧の行レベル機密統制は据え置きを暫定既定（生PII列を足さない不変条件・高機密運用開始時に格上げ）②LocalBusinessLead/LocalBusinessContact のPII閲覧はテナント分離＋leadmap 権限＋外部送信ゲートで当面担保③Contact 単体は親 Customer のラベル従属④outreach は opt-out（SuppressionList 強制）を正式方針として記録可⑤positive Consent（ConsentRecord）は用途別分離を暫定既定。**Phase 3 進入は引き続き HOLD**。**runtime 解禁なし・実装なし・schema変更なし・migrationなし・RBAC変更なし・labels変更なし・company-brain-reference変更なし・leadmap/customers 変更なし・externalAiAllowed true 解禁なし・EXTERNAL_SEND_ENABLED true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **roadmap27/設計判断メモ push-only**（別承認）または 人間 Phase Gate 承認判断・回帰ゲート green 確認。
- **高機密ラベル runtime 統制の現状監査（docs/roadmap/25 Candidate）: docs-only 完了（判定 B: HOLD・2026-07-06・記録: doc124＋`docs/roadmap/25`）／369-vault非編集・実装なし（コード差分ゼロ・read-only 監査）**。Phase 3 移行残件②。doc108〜114 設計と実コードのズレを read-only 監査。**AI参照経路（Company Brain）の高機密ラベル封印は閉じている**（`AI_READABLE_LABELS=['NORMAL','INTERNAL']` DBフィルタ＋`canAccessLabel` 二重ガード・外部LLM時 externalAiAllowed＋maskText・CaseStudy は anonymized+private のみ）＝**CUSTOMER_CONFIDENTIAL/顧客PIIは AI 非注入**。`writeDataAccess`/`DataAccessLog`/ABAC(security/policy.ts) 実装済み・`canAccessLabel` 純粋関数存在。**ズレ/未確認**: CRM `Customer`（既定 CUSTOMER_CONFIDENTIAL）/`Contact`/`LocalBusinessLead` の**人間 UI 閲覧経路で canAccessLabel＋writeDataAccess(confidential_view) 配線が未確認**・doc110 標準閲覧式(customer-pain-access.ts)は apps 未接続・runtime 解禁は doc112 停止条件のまま。安全側で **判定 B: HOLD**。**Phase 3 進入は引き続き HOLD・人間 Phase Gate 承認事項**。**runtime 解禁なし・canAccessLabel/writeDataAccess 実装なし・schema変更なし・migrationなし・RBAC変更なし・company-brain-reference変更なし・externalAiAllowed true 解禁なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc124 push-only**（別承認）または CRM Customer/Contact 閲覧統制の追加監査 docs-only。
- **Phase 2 正式完了記録（docs/roadmap/24 Candidate）: docs-only 完了（判定 READY / GO＝docs整理／Phase 2 は CONDITIONAL COMPLETE＝人間 Phase Gate 承認事項／Phase 3 進入は HOLD・2026-07-06・記録: doc123＋`docs/roadmap/24`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc48（Phase 2-A 完了）・doc121（CRM/SFA 既存実装 案A）・doc122/roadmap23（移行条件）を受け Phase 2 正式完了記録を整理。**Phase 2＝CONDITIONAL COMPLETE**（Company Brain foundation は Phase 2-A 正式完了 `85f1bf3`/doc48・CRM/SFA は既存実装あり＝doc121案A、ただし CRM/SFA の本番確認GO・回帰ゲート green の正式記録が未整備・高機密ラベル runtime 統制は設計のみ・CRM 不足補強 archivedAt/internalNote 未着手 → **正式完了宣言は人間 Phase Gate 承認事項**）。**Phase 3 AI Growth Engine 進入は HOLD**（残件6条件＝①Phase 2完了正式記録②高機密ラベル runtime 統制監査③外部送信 Human Certification Gate 運用確認④Consent・SuppressionList 運用確認⑤AI境界再確認⑥回帰ゲート green 確認、GO＝①〜⑥＋人間承認）。roadmap24 に現在地10項目明示見出し（§12-1〜12-10）・50カテゴリ Matrix（§13）・Global AI Rules（§18）を記録。**実装なし・schema変更なし・migrationなし・RBAC変更なし・company-brain-reference変更なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc123 push-only**（別承認）または 高機密ラベル runtime 統制の現状監査 docs-only。
- **Phase 2 完了判定・Phase 3 移行条件整理（docs/roadmap/23 Candidate）: docs-only 完了（判定 READY / GO＝docs整理／Phase 3 進入は HOLD・2026-07-06・記録: doc122＋`docs/roadmap/23`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc117〜121・roadmap18〜22・既存 LeadMap/CRM 実測を前提に Phase 2 完了扱いと Phase 3 進入可否を整理。**Phase 2＝部分完了**（Company Brain foundation は Phase 2-A 正式完了 `85f1bf3`/doc48・CRM/SFA は既存実装あり＝doc121案A、ただし CRM/SFA の Phase 2 完了正式記録は未作成）。**Phase 3 AI Growth Engine 進入は HOLD 推奨**（外部発信リスク・Human Certification Gate/Consent/SuppressionList/高機密ラベル runtime 統制の運用確認が前提）。移行条件6項目（Phase 2完了記録／高機密ラベル runtime 統制監査／外部送信 Human Certification Gate 運用確認／Consent・SuppressionList 運用確認／AI境界再確認／回帰ゲート green 確認）。**これは人間の Phase Gate 承認事項**。Phase 3前禁止=外部送信/広告自動/SNS/PR/SEO/LINE・DM/実LLM/AIコスト/高機密ラベル runtime 解禁/新規Leadモデル/crm:*新設/schema/migration。**実装なし・schema変更なし・migrationなし・RBAC変更なし・company-brain-reference変更なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc122 push-only**（別承認）または Phase 2 正式完了記録の作成 docs-only。
- **既存LeadMap/CRM実装の追認・差分監査（docs/roadmap/22 Candidate）: docs-only 完了（判定 READY / GO＝案A・2026-07-06・記録: doc121＋`docs/roadmap/22`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc121 実装ミッションで、既存に **LeadMap/CRM が既に実装済み**（`LocalBusinessLead`＝実リード・`LocalBusinessContact`/`Customer`/`Contact`/`Deal`/`Account`・enum `LeadStage`16値・`/leadmap/*` UI・`leadmap/actions.ts` の requireUser/hasPermission('leadmap')/tenantId/writeAudit/OutreachApproval・rbac の `leadmap`/`leadmap:external_send`）と判明し**実装せず停止**。read-only 監査で doc118〜120（greenfield 前提の Candidate）とのズレを整理。**判定=案A: 既存 `LocalBusinessLead`/`/leadmap` を CRM Lead 正本候補として追認・新規 `Lead` モデル/`/crm/leads` は作らない・doc118〜120 は削除せず温存**。不足補強候補（archivedAt ソフトアーカイブ・internalNote 等の薄い追加は別承認）。`Customer.label` 既定 CUSTOMER_CONFIDENTIAL で doc108〜114 高機密ラベルラインに接続。**実装なし・schema変更なし・migrationなし・UI変更なし・Server Action変更なし・RBAC変更なし・crm:*新設なし・Contact PII追加なし・company-brain-reference変更なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc121 push-only**（別承認）または 既存 LocalBusinessLead 薄い補強の実装可否判断 docs-only。
- **CRM Lead-only 実装スプリント可否 最終判断（docs/roadmap/21 Candidate）: docs-only 完了（判定 READY / GO＝条件は揃っている・2026-07-06・記録: doc120＋`docs/roadmap/21`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc118/doc119 を前提に、CRM Lead-only を次の実装スプリントへ進める条件が揃っているかを最終判断。**判定 READY / GO は「条件が揃っている」であり、実装着手そのものは §30 別承認文言に基づく別の重い人間承認**。判断項目・schema要否（実装するなら追加1）・migration要否（1本）・Lead候補列/status enum の最終確認・tenantId/RBAC/AIロール mutation 禁止/writeAudit/writeDataAccess/Data Classification（NORMAL/INTERNALのみ）・Contact PII非採用・company-brain-reference非変更・AI下書きのみ・外部送信なし・Human Certification Gate/Consent Gate/Security Gate・Business Event Ledger将来接続・実装案（案X 今すぐ着手/案Y もう1段docs/案Z 別Lineage先行）・**推奨=案X を次の別承認で実行（Lead 1テーブル＋最小CRUD＋一覧・既存knowledge権限暫定・crm:*新設は別承認・schema1＋migration1・本番確認は別承認）**・停止条件・別承認文言（案）を整理。**今回は実装しない・schema変更なし・migrationなし・RBAC変更なし・crm:*新設なし・company-brain-reference変更なし・Contact PII保存なし・外部連携/OAuth/外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc120 push-only**（別承認）または CRM Lead-only 実装スプリント着手（§30 別承認）。
- **CRM Lead-only 最小設計（docs/roadmap/20 Candidate）: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc119＋`docs/roadmap/20`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc118 推奨案A（Lead-only）を、実装判断できる粒度まで最小設計。**Lead 候補列**（tenantId/displayName=会社名・拠点名レベル/source/placeId/fetchedAt/expiresAt/status/assignedUserId/internalNote/archivedAt・**Contact PII は入れない**）、**status enum 候補**（NEW/REVIEWING/QUALIFIED/EXCLUDED/ARCHIVED）、archivedAt ソフトアーカイブ、tenantId 必須、RBAC 方針、**`crm:*` 新設 vs 既存 knowledge 権限代替の比較（本書では RBAC 変更しない）**、writeAudit 対象、**writeDataAccess は Lead-only では原則不要（PII段階で必須＝停止条件）**、Data Classification は NORMAL/INTERNAL のみ、company-brain-reference 非注入、AIは下書き・提案のみ、外部送信なし、Human Certification Gate/Consent Gate/Security Gate、Business Event Ledger 将来接続、Phase対応、50/20/19/5本柱接続、初期MVP非対象、停止条件、推奨判断を整理。**今回は実装しない・実装は別承認・schema変更なし・migrationなし・画面/Server Actionなし・RBAC変更なし・company-brain-reference変更なし・外部連携/OAuth/外部送信/実LLM/AIコスト/本番なし・Contact PII保存なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc119 push-only**（別承認）または CRM Lead-only 実装スプリント可否の最終判断（別の重い承認）。
- **CRM最小縦切り 実装可否判断（docs/roadmap/19 Candidate）: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc118＋`docs/roadmap/19`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc117 CRM/SFA Lineage（`docs/roadmap/18`）を受け、Phase 2 CRM/SFA の最初の実装範囲を **案A: Lead-only / 案B: Lead+Deal / 案C: Lead+Deal+Pipeline** の3案で比較（目的・入れる/入れないデータ・schema要否・migration要否・PIIリスク・tenantId/RBAC/writeAudit/writeDataAccess・Human Certification Gate/Consent Gate/Security Gate・Company Brain接続・AI参照・外部送信・Phase対応・収益/moat・停止条件）。**推奨=案A（Lead-only）**（Contact PII最小・Security Gate負荷最小・薄い縦切り優先・次段停止条件を設計しやすい）。50カテゴリ扱い（直接C08/C09/C46）・4軸Phase（事業Phase 2 主対象）・5本柱（Human Certification Gate/Business Event Ledger 将来接続）も記録。**今回は実装しない（schema/migration/画面/Server Action/外部連携/OAuth/外部送信/実LLM/AIコスト/本番なし）・推奨案Aも実装は別承認・Contact PIIは初期範囲に入れない・company-brain-reference 非注入・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc118 push-only**（別承認）または 案A Lead-only 実装可否の最小設計 docs-only（別の重い承認）。
- **CRM / SFA / Salesforce Mini Lineage（docs/roadmap/18 Candidate）: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc117＋`docs/roadmap/18`）／369-vault非編集・実装なし（コード差分ゼロ）**。doc116 SaaSカタログ（`docs/roadmap/17`）の**最初の個別深掘り**として、369が内包・代替・連携するCRM/SFA領域を整理。**内包・代替機能候補**（Lead / Account / Contact / Deal / Opportunity / Pipeline / Activity / Task / Note / Customer 360 / Sales Playbook / Quote・Contract・Invoice handoff）、**外部連携候補**（CRM/MA/広告/メール/カレンダー・特定SaaS名は例示に留め契約判断なし）、**AI営業提案は下書きのみ・外部送信は Human Certification Gate 必須・AI単独の契約/値引き/請求/送信は禁止**、tenantId/RBAC/writeAudit/writeDataAccess/Data Classification/PII の扱い、Company Brain / AI Growth Engine / CPQ / Contract / Invoice / ERP 接続候補、Service/Marketing/Data Cloud 境界、Phase対応・収益ポイント・moat・やらないこと・承認ゲートを記録。**Candidate（実装/契約/API連携/DB化/schema化/正式採用ではない）・schema/画面/外部連携は各々別承認・顧客PIIは company-brain-reference 非注入・高機密ラベル runtime 解禁は別承認**。**実装なし・schema/migration/RBAC/label定義/company-brain-reference変更なし・外部SaaS連携実装なし・OAuthなし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。**Phase対応を補強済み**（主対象=事業Phase 2 Salesforce Mini/CRM基盤・前提=Phase 0 Core OS/Phase 1 Company Brain・隣接=Phase 3/4/5/12/13/14/19・PDF系 OS本体 Phase 2.5-9/10・戦略 Phase 20/22/23/24/26 は将来接続候補で今回実装しない）。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc117＋Phase補強 push-only**（別承認）または CRM 最小縦切り実装可否判断（別の重い承認）。
- **369組み込み予定SaaSカタログ（docs/roadmap/17 Candidate）: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc116＋`docs/roadmap/17`）／369-vault非編集・実装なし（コード差分ゼロ）**。これまでの戦略docs（AI Workforce Infrastructure・Developer Cloud・Marketplace・PLUG・Employee App・IP moat・Obsidian関係／`docs/roadmap/09-16`）に不足していた「**組み込み予定SaaS一覧**」を、`docs/roadmap/17` の補完 Candidate カタログとして整理。**内包・代替 / 外部連携 / Marketplace / PLUG / Employee App / Candidate保留 / Do Not Build の6区分**で、内包・代替対象のSaaSカテゴリ（Core OS/IAM〜Docs/Audit）を抜け漏れなく列挙し、外部連携対象・収益源・moat（IP moat 含む）・禁止と安全代替（Human Certification Gate）・Phase対応を記録。**特定企業名は例示に留め契約判断なし・Candidate（実装/契約/API連携/DB化/正式採用ではない）・1カテゴリずつ別承認・231-252はCandidate扱いのまま**。**実装なし・schema/migration/RBAC/label定義/company-brain-reference変更なし・外部送信/実LLM/AIコスト/本番なし・369-vault非編集・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc116 push-only**（別承認）または 1カテゴリずつ個別深掘り（Lineage独立docs化）。
- **Obsidian 関係設計（docs/10_obsidian Candidate）: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc115＋`docs/10_obsidian/*`）／369-vault非編集・実同期なし（コード差分ゼロ）**。`docs/roadmap/16` の役割分担 Candidate を受け、GitHub正本・Obsidian閲覧・369-vault の関係を運用に近い粒度で `docs/10_obsidian/` に Candidate 3件（RELATIONSHIP／SYNC_RULES／DASHBOARD_INDEX）＋監査記録 doc115 として記録。**GitHubが正本・Obsidianは閲覧/思考整理/経営ダッシュボード・369-vaultを正本として扱わない・直接編集しない・Obsidianだけで正式判断しない・secrets/個人情報/本番ログ生データ/実顧客データは同期しない・同期実行と自動同期実装は別承認**。**369-vault差分0（非編集）・実同期なし・実装なし・schema/migration/RBAC/label定義/company-brain-reference変更なし・外部送信/実LLM/AIコスト/本番なし・push なし（commit-only）**。安全確認 `node scripts/check-company-brain-safety.mjs` exit 0。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は doc115 push-only（別承認）または現行ライン継続。
- **戦略 Candidate docs 整備（AI Workforce Infrastructure 全体像）: docs-only 完了（Candidate・2026-07-06・記録: `docs/roadmap/09-16`）／実装なし・369-vault非編集（コード差分ゼロ）**。前回不足していた AI社員経済圏・Developer Cloud・Marketplace・PLUG購買・従業員導線・知財moat・Phase 0-26接続を、既存 `docs/roadmap/`（非破壊）に **Candidate** として追加。09=AI Workforce Infrastructure定義(8層/4層)／10=三系統ロードマップ接続(OS本体2.5-18・戦略構想18.5-26・事業0-20 対応表)／11=Developer Cloud・Marketplace・Safety Review・Revenue Share／12=PLUG Commerce・369 Employee App(透明性・データ分離)／13=知財moat(特許/商標/営業秘密・要専門家確認)／14=Function Master 231-252 Candidates(正式昇格せず)／15=広告費ゼロ成長ループ・AI Safety境界(禁止機能・安全代替・Human Certification Gate)／16=GitHub/Obsidian/369-vault役割分担・NEXT_ACTIONS・OPEN_RISKS・次回プロンプト。**GitHubが正本・Obsidianは閲覧・369-vaultは直接編集しない／docs/10_obsidianと369-vaultの関係は別承認の設計候補／231-252はCandidate扱い（DB化・画面・API・正式昇格は別承認）**。**実装なし・schema/migration/RBAC/label定義/company-brain-reference/apps/packages/scripts変更なし・外部送信/実LLM/AIコスト/本番影響なし・369-vault非編集・push なし（commit-only）**。現行 Data Classification / Customer Pain ライン（doc105-114）は正のまま。次は 戦略docs push-only（別承認）または現行ライン継続。
- **Customer Pain schema設計: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc114）／schema変更ではない・migrationなし・本実装なし（コード差分ゼロ・DB非接触）**。doc113（候補C前提設計）を受け、本実装に必要になり得る**最小schema候補を紙で設計**。**Prisma schema変更なし・migrationなし**。候補フィールド（id/tenantId/title/body/category/severity/status/label/archivedAt/createdAt/updatedAt/createdByUserId/updatedByUserId）を、列の意味・PII注意・一覧/詳細可否・writeAudit/writeDataAccess対象で整理。**入れてはいけない列を明記**（customerName/contactName/phone/email/address/rawComplaintText/rawLostReasonText/rawTranscript/sourceUrl/externalAiAllowed/publishStatus/publicUrl/seoSlug/customerVoice/customerId）。**customerIdは結合リスクのため別承認・enum化も別承認**。tenantId必須・archivedAtソフトアーカイブ（物理削除なし）・createdBy/updatedByはuserId参照のみ（個人名複製なし）。標準閲覧式（doc110 canViewCustomerPainDetail）を使う前提候補だが**実接続はまだしない**。**AIに読ませない（company-brain-referenceに入れない・AI参照条件変更なし）・公開なし（externalAiAllowed/publishStatusなし）**。**3案比較→推奨案A（NO_SCHEMA_CHANGE_AND_DESIGN_ONLY）**。事前停止条件（schema/migration/enum追加/customerId参照/PII保存/AI参照/公開導線/本番）を固定。read-only安全確認 `node scripts/check-company-brain-safety.mjs` exit 0（actions4/ui156）。**解禁なし・Customer Pain本実装/runtime実装0・schema/migration/RBAC/label定義変更なし・company-brain-reference変更なし・AI参照条件変更なし・PII/実顧客データ保存なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc114 push-only**（別承認）→ Customer Pain schema実装可否判断 または品質基盤強化。
- **Customer Pain 候補C 前提設計: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc113）／候補C本実装ではない・解禁ではない（コード差分ゼロ）**。doc112（まだruntime解禁しない）を受け、候補C（Customer Pain schema/画面/Server Action/writeDataAccess/writeAudit 実接続）へ進む前の前提を設計。**候補Cは schema/migration を伴うため最も重く別の重い人間承認**。schema要否＝実装するなら最終的に必要だが**今回は決めない（schema変更なし・migrationなし）**、必要と判明したら停止して別承認。最小schema候補の方向性のみ（PII/顧客名/担当者名を入れない・**customerId参照は別承認**・本文に実顧客情報を複製しない・archivedAt前提・tenantId必須・labelはCUSTOMER_CONFIDENTIAL前提候補だがruntime解禁はまだ）。画面＝一覧はプレースホルダ・詳細は候補Aの標準閲覧式を満たす人間のみ・許可判定前に本文非取得・詳細のたび**writeDataAccess**必須。書き込み＝create/update/archiveのみ・物理削除なし・restore/label変更/customerId変更は別承認・**writeAudit**必須。**AIに読ませない（company-brain-referenceに入れない・AI参照条件変更なし）・公開なし（PR/SEO/SNS/externalAiAllowed/publishStatus UIなし）**。**推奨は案A（候補C schema設計docs-only・ただしschema変更/migrationはしない）／案B品質基盤／案C保留**。read-only安全確認 `node scripts/check-company-brain-safety.mjs` exit 0（actions4/ui156）。**解禁なし・Customer Pain runtime実装0・schema/migration/RBAC/label定義変更なし・company-brain-reference変更なし・AI参照条件変更なし・PII/実顧客データ保存なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc113 push-only**（別承認）→ Customer Pain schema設計docs-only または品質基盤強化。
- **高機密ラベル実装・解禁 可否判断（候補A+B完了後）: docs-only 完了（判定 GO・2026-07-06・記録: doc112）／まだruntime解禁しない・これは解禁ではない（コード差分ゼロ）**。候補A（doc110・閲覧判定の純粋関数）＋候補B（doc111・安全ゲート静的検査）完了を受け、人間が **DO_NOT_ENABLE_RUNTIME_YET_AFTER_CANDIDATE_AB＝候補A+B完了でもまだ runtime 解禁しない** を正式決定。**§0 人間決定値12項目をすべて安全側で確定**（CANDIDATE_A_AND_B_COMPLETED_AS_SAFETY_FOUNDATION／CANDIDATE_C_POLICY=REQUIRE_SEPARATE_HEAVY_APPROVAL_BECAUSE_SCHEMA_AND_MIGRATION／NEXT_PATH_POLICY=PREPARE_CANDIDATE_C_PREREQUISITE_DESIGN_DOCS_ONLY_OR_QUALITY_FOUNDATION／DO_NOT_IMPLEMENT_CUSTOMER_PAIN_NOW／NO_SCHEMA_CHANGE_NOW／NO_MIGRATION_NOW／NO_RBAC_CHANGE_NOW／NO_LABEL_DEFINITION_CHANGE_NOW／PROHIBIT_AI_REFERENCE_AND_NO_COMPANY_BRAIN_REFERENCE_CHANGE／PROHIBIT_PII_AND_REAL_CUSTOMER_DATA_NOW／DOCS_ONLY_NOW）。候補A+B完了は**安全の土台であって解禁条件ではない**。runtime解禁＝Customer Painの器（schema）＋実画面/Server Action/writeDataAccess/writeAudit実接続＋本番確認を伴う重い工程。**推奨は案A（候補C前提設計docs-only・ただし本実装ではなく設計のみ）／案B=品質基盤強化／案C=保留**。read-only安全確認で `node scripts/check-company-brain-safety.mjs` exit 0（actions4/ui156）。**解禁なし・Customer Pain runtime実装0・schema/migration/RBAC/label定義変更なし・company-brain-reference変更なし・AI参照条件変更なし・PII/実顧客データ保存なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc112 push-only**（別承認）→ 候補C前提設計docs-only または品質基盤強化。
- **高機密ラベル 安全ゲート静的検査 実装（候補B）: 完了・ローカル検証全green（判定 GO・2026-07-06・記録: doc111）／解禁なし・実行時機能なし（apps/DB/画面変更なし）**。doc110 候補A（閲覧判定の純粋関数）の守りが後退したら機械検知するため、既存 `scripts/check-company-brain-safety.mjs` を**末尾追記で拡張**（新規script作成なし・既存ゲートは1件も削除/弱体化なし）。追加検査: **標準閲覧式5条件（tenantId × knowledge:update=canForRoles × canAccessLabel × AIロール除外=isHumanUser × archivedAt null）の消失FAIL・OR緩和(`||`混入)FAIL・CUSTOMER_PAIN_LABEL=CUSTOMER_CONFIDENTIAL固定・CUSTOMER_PAIN_DENY_REASONS維持・否定系テスト維持・apps配下へのCustomerPain runtime混入FAIL・company-brain-referenceへのCustomerPain注入FAIL・schema/labelsのCUSTOMER_CONFIDENTIAL各2件不変**。既存の会社ブレイン/CaseStudyの externalAiAllowed/publishStatus はグローバル禁止せず（Customer Pain固有トークンのみ検知・node_modules等除外）。**検証 green（safety script exit 0＝actions4/ui156・pnpm test 265・typecheck exit 0・lint exit 0）＋負例でFAILすることも確認（no-opでない）**。build は未実施（.mjs＋docs/tas/vaultのみ・apps runtime不変・typecheckで全ワークスペース型検査済み・理由 doc111 §5）。**解禁なし・Customer Pain実装なし（runtime実装0）・schema/migration/RBAC/label定義変更なし・company-brain-reference変更なし・AI参照条件変更なし・PII/実顧客データ保存なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc111 push-only**（別承認）→ 高機密ラベル実装・解禁の可否判断（候補A+B完了後の個別人間承認・まだ解禁しない選択も可）。
- **高機密ラベル閲覧判定の純粋関数 実装（候補A）: 完了・ローカル検証全green（判定 GO・2026-07-06・記録: doc110）／解禁なし・Customer Pain実装なし（画面/Server Action/DBなし）**。doc109 候補A（純粋権限判定関数＋否定系テスト・schema/migration不要の範囲）を `packages/shared` 内のみで最小実装。新規 `packages/shared/src/customer-pain-access.ts`＝**標準閲覧式（tenantId × knowledge:update × canAccessLabel(CUSTOMER_CONFIDENTIAL) × AIロール除外=isHumanUser × archivedAt null の AND・OR緩和禁止）**の `canViewCustomerPainDetail`（boolean）＋`evaluateCustomerPainAccess`（拒否理由は安全な列挙値のみ）。**AIロール除外は既存 isHumanUser を再利用（重複実装なし）**・`canForRoles`/`canAccessLabel` も既存を利用（**RBAC定義・label定義は変更しない**）。**Prisma import なし・DB非依存・apps/web非参照・company-brain-reference非参照**。否定系テスト9ケース（tenant/権限/label/AIロール混在/archived/allowed/OR緩和なし/安全な理由列挙）新規＝**pnpm test 265（既存256+9・回帰なし）・typecheck exit 0・lint exit 0**。build は未実施（純粋関数のみ・typecheckで全ワークスペース型検査済み・理由 doc110 §8）。**writeDataAccess/writeAudit 実接続なし（「見られるか」の判定まで）・Customer Pain画面/Server Action/schema/migrationなし・解禁なし・AI参照条件変更なし・company-brain-reference変更なし・PII/実顧客データ保存なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc110 push-only**（別承認）→ 候補B（安全ゲート静的検査）の実装可否判断。
- **高機密ラベル運用 最小実装設計: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc109）／解禁なし・実装なし（コード差分ゼロ）**。doc108 の決定（DO_NOT_ENABLE_YET_AND_PREPARE_MIN_IMPL_DESIGN）を受け、将来の最小実装の範囲・順序・停止条件を設計。実装候補を **A（純粋な権限判定関数＝isHumanUser／canViewCustomerPainDetail ＋ 否定系テスト・schema/migration不要）／B（安全ゲート静的検査の追加）／C（Customer Pain schema・画面・Server Action の本実装）** に分割し、**A+B を先に紙で設計、schema/migration が必要な C は別の重い人間承認へ送る**方針を推奨。**標準閲覧式**（tenantId × knowledge:update × canAccessLabel × AIロール除外=isHumanUser × archivedAt null × writeDataAccess 記録可能の AND）・**writeDataAccess / writeAudit** の記録粒度（本文・PII を入れない）・**安全ゲート14種**・**否定系テスト15種**・**事前停止条件**を doc105 から継承・整理。**本書は設計のみ（DOCS_ONLY_NOW）で A・B の実装もさらに別承認**。**解禁なし・実装なし・Customer Pain実装なし・Data Classification実装なし・DB/schema/migration/RBAC/label定義変更なし・AI参照条件変更なし・company-brain-reference変更なし・PII/実顧客データ保存なし・外部送信なし・本番確認なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc109 push-only**（別承認）→ 候補A+B の実装可否判断（別承認）。
- **高機密ラベル解禁可否 方針決定: docs-only 完了（判定 GO・2026-07-06・記録: doc108）／DO_NOT_ENABLE_YET_AND_PREPARE_MIN_IMPL_DESIGN＝まだ解禁しない・これは解禁ではない（コード差分ゼロ）**。doc107 の3案・§0候補を受け、人間が **案A（DO_NOT_ENABLE_YET_AND_PREPARE_MIN_IMPL_DESIGN）＝まだ解禁せず次は高機密ラベル運用の最小実装設計 docs-only に進む** を正式決定。**§0 人間決定値10項目をすべて安全側で確定**（HIGH_CONFIDENTIAL_ENABLEMENT_POLICY=DO_NOT_ENABLE_YET_AND_PREPARE_MIN_IMPL_DESIGN / NO_RUNTIME_ENABLEMENT_NOW / CUSTOMER_CONFIDENTIAL_ONLY_LATER / TENANT_KNOWLEDGE_UPDATE_LABEL_HUMAN_AND_ONLY / WRITE_DATA_ACCESS_REQUIRED_LATER / WRITE_AUDIT_REQUIRED_LATER / PROHIBIT_PII_AND_REAL_CUSTOMER_DATA_NOW / PROHIBIT_AI_REFERENCE / PROHIBIT_PUBLIC_USE / DOCS_ONLY_NOW）。承認範囲は docs-only の方針決定記録のみ（実装・解禁・本番反映・外部送信は未承認）。**実装なし・解禁なし・Customer Pain実装なし・Data Classification実装なし・DB/schema/migration/RBAC/label定義変更なし・AI参照条件変更なし・company-brain-reference変更なし・PII/実顧客データ保存なし・AIに読ませない・外部公開なし・本番確認なし・push なし（commit-only）**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次は **doc108 push-only**（別承認）→ 高機密ラベル運用の最小実装設計 docs-only（別承認）。
- **高機密ラベル実装・解禁 可否判断 前提整理: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc107）／今回は解禁ではない・高機密ラベル解禁なし・Customer Pain 実装なし（コード差分ゼロ）**。doc105 詳細設計＋doc106 番号体系を受け、解禁可否の重い人間判断に入る前の材料を整理。**3案比較**（案A=DO_NOT_ENABLE_YET_AND_PREPARE_MIN_IMPL_DESIGN＝まだ解禁せず次は最小実装設計 docs-only・**推奨**／案B=ENABLE_NARROW_SCOPE_AFTER_SEPARATE_APPROVAL＝対象ラベル/画面/テーブル限定で別承認後・今回ではない／案C=DO_NOT_ENABLE_AND_PRIORITIZE_QUALITY_FOUNDATION＝品質基盤優先）。**§0 人間決定候補10項目**（推奨すべて安全側: NO_RUNTIME_ENABLEMENT_NOW / CUSTOMER_CONFIDENTIAL_ONLY_LATER / TENANT_KNOWLEDGE_UPDATE_LABEL_HUMAN_AND_ONLY / WRITE_DATA_ACCESS_REQUIRED_LATER / WRITE_AUDIT_REQUIRED_LATER / PROHIBIT_PII_AND_REAL_CUSTOMER_DATA_NOW / PROHIBIT_AI_REFERENCE / PROHIBIT_PUBLIC_USE / DOCS_ONLY_NOW）。まだ解禁しない理由（実装/schema/否定系テスト/本番確認がまだ・可逆性）と実装前停止条件、解禁時の最小範囲（CUSTOMER_CONFIDENTIAL × Customer Pain × 限定画面）を記録。**案Aでも今回は実装しない**。**DB/schema/migration/RBAC/label定義変更なし・AI参照条件変更なし・company-brain-reference変更なし・PII/実顧客データ保存なし・docs/10_obsidian と 369-vault の関係は別承認・既存docs改名なし・HOLD記録削除なし**。Baseline Commit は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc107 push（別承認）→ 解禁可否の §0 人間決定。
- **Phase番号体系・Lineage整理: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc106）**／audit番号とPhase番号は別軸であり、今後は Audit Doc / Product Phase / Lineage / Stage / Baseline Commit / Current HEAD / Next Action を併記する方針を固定。audit番号=時系列証拠・Product Phase=ロードマップ上の意味・Lineage=機能ごとの流れ（Company Brain / CaseStudyConsent / Customer Pain / AI Reference / Public Use / Data Classification）・Stage=作業種類・commit hash=Git正本状態。既存docs改名なし・HOLD記録削除なし・frontmatter一括適用なし・docs/10_obsidian と 369-vault の関係確定なし・369-vault構造変更なし。ObsidianではLineage別に現在地を追う。実装/DB/schema/migration/RBAC/label定義/AI参照条件/company-brain-reference 変更なし。プロダクト基準（Baseline Commit）は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc106 push（別承認）。
- **Customer Pain 高機密ラベル運用 詳細設計: docs-only 完了（判定 READY / GO・2026-07-06・記録: doc105）／高機密ラベル解禁なし・Customer Pain 実装なし（設計のみ・コード差分ゼロ）**。doc104 決定（TENANT_AND_KNOWLEDGE_UPDATE_AND_LABEL_ROLE_AND ほか12項目）を実装可能な粒度へ詳細化: **標準閲覧式＝tenantId × knowledge:update × label許可ロール（canAccessLabel）× AIロール除外（isHumanUser）× archivedAt null × writeDataAccess 記録可能の AND 交差**（擬似コード固定・OR 緩和禁止・label単独では不十分）／一覧は非許可者にプレースホルダのみ・顧客名/失注理由/クレーム本文を出さない・badge 表示候補／詳細は許可判定前に本文を取得しない設計候補・**閲覧のたび writeDataAccess 設計**（tenantId/actorUserId/targetType=CustomerPain/action=view/purpose=customer_pain_internal_review/label/route/result/reasonCode——**本文・PII・顧客名・担当者名は記録禁止**）／**書き込みは writeAudit 設計必須**（create/update/archive。restore・label変更・customerId紐づけ変更は別承認）／**安全ゲート候補14種**（tenantId・knowledge:update・canAccessLabel・AIロール除外・writeDataAccess・writeAudit の消失FAIL＋company-brain-reference 注入・AI参照・公開導線・PII混入・ログ本文混入・Customer PII直接表示・externalAiAllowed/publishStatus UI の出現FAIL）／**否定系テスト候補15種**／**実装時の事前停止条件16項目**（schema/migration/RBAC/label定義変更が必要なら停止等）。**PII/顧客名/担当者名/実顧客データ保存なし・customerId 参照のみ（実装も別承認）・AIに読ませない・公開しない・DB/schema/migration/RBAC/label定義変更なし・AI参照条件変更なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc105 push（別承認）→ 高機密ラベル実装・解禁の可否判断（個別人間承認・重い判断）→ 最小実装設計（別承認）。
- **Customer Pain Data Classification 方針決定: docs-only 完了（判定 GO・2026-07-06・記録: doc104）／高機密ラベル実装・解禁なし・Customer Pain 実装なし・実装不要（決定＝現状維持・コード差分ゼロ）**。doc103 §20 の §0 人間決定12項目を**すべて安全側で正式決定**: CUSTOMER_CONFIDENTIAL_AS_PREREQUISITE_DESIGN_ONLY（**既定ラベルは CUSTOMER_CONFIDENTIAL 前提候補・設計のみ**）／DO_NOT_ENABLE_HIGH_CONFIDENTIAL_LABEL_YET（**解禁なし**・解禁は個別の重い人間承認のまま）／TENANT_AND_KNOWLEDGE_UPDATE_AND_LABEL_ROLE_AND（**閲覧は tenantId × knowledge:update × label許可ロールの AND 交差**・doc103 の AIロール除外を含む）／REQUIRE_WRITE_DATA_ACCESS_DESIGN（閲覧は writeDataAccess 記録の設計必須）／KEEP_WRITE_AUDIT_REQUIRED（書き込みは writeAudit 必須）／PROHIBIT_PII_STORAGE_NOW（**PII/顧客名/担当者名/実顧客データ保存なし**）／CUSTOMER_ID_REFERENCE_ONLY_LATER（将来も customerId 参照のみ）／PROHIBIT_AI_REFERENCE_NOW（**AIに読ませない**）／PROHIBIT_PUBLIC_USE（公開しない）／REQUIRE_SAFETY_GATE_DESIGN_BEFORE_IMPLEMENTATION／REQUIRE_NEGATIVE_TEST_DESIGN_BEFORE_IMPLEMENTATION／DOCS_ONLY_NOW。ミッション指定の値名と doc103 候補名の差分は**すべて同義または安全側に厳格化**（doc104 §2 の対応表に記録・意味変更なし）。決定と現状（Pain 未実装 0件・封印維持・ラベル定義不変）の一致を read-only 監査で確認＝**DB/schema/migration/RBAC/label定義変更なし・AI参照条件変更なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc104 push（別承認）→ 高機密ラベル運用の詳細設計（docs-only・別承認）→ 実装・解禁はさらに個別承認。
- **Customer Pain Data Classification / 高機密ラベル運用設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc103）／高機密ラベル解禁ではない・Customer Pain 実装ではない**。doc102 の決定を受けた「守り方の設計」を固定: **CUSTOMER_CONFIDENTIAL 既定候補**（極端な機微は STRICT_SECRET 例外候補）／**中心論点=labels.ts の CUSTOMER_CONFIDENTIAL 許可ロールには AI_AGENT・AI_ASSISTANT・STAFF が含まれる（CRM 用の既存設計）ため、ラベル単独では守りが不足**→ **閲覧は knowledge:update ＋ label許可ロール（canAccessLabel）＋ AIロール除外（isHumanUser）の3条件 AND 交差**を標準式候補に（RBAC・label 定義は変更しない）／一覧はプレースホルダ・詳細のみ閲覧／**閲覧のたび writeDataAccess 記録（既存 helper 流用・本文/PII は記録に入れない）・書き込みは writeAudit 必須**／本文への PII・顧客名複製は禁止方向・**customerId 参照のみ**候補／**AIに読ませない**（company-brain-reference に追加しない＝機械検査候補）・**公開しない**／安全ゲート5種・否定系テスト7種・実装前の承認ゲート（設計→§0→**高機密ラベル解禁の個別人間承認（重い判断）**→実装→本番確認）・**§0 人間決定候補12項目**（推奨すべて安全側）・段階9案を doc103 に固定。**DB/schema/migration/RBAC/label定義変更なし・AI参照条件変更なし・PII/顧客名/実顧客データ保存なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc103 push（別承認）→ Data Classification の §0 人間決定 → 実装はさらに別承認。
- **Customer Pain 方針決定: docs-only 完了（判定 GO・2026-07-05・記録: doc102）／Customer Pain 実装は未着手・実装不要（決定＝現状維持・コード差分ゼロ）**。doc101 §14 の §0 人間決定9項目を**すべて安全側で正式決定**: **HIGH_CONFIDENTIAL_PREREQUISITE_FIRST（守り方が先＝高機密ラベル・権限・監査・AI禁止の前提設計が先で Customer Pain 実装はまだ）**／REQUIRE_CUSTOMER_CONFIDENTIAL_LABEL_DESIGN（**Data Classification / 高機密ラベル運用設計が次の前提**・設計は docs-only・解禁は個別人間承認の重い判断のまま）／PROHIBIT_PII_NOW・PROHIBIT_CUSTOMER_NAME_NOW（**PII・顧客名・担当者名・実顧客データは保存しない**）／PROHIBIT_AI_REFERENCE_NOW（**AIに読ませない**・AI参照条件変更なし）／PROHIBIT_PUBLIC_USE（公開しない）／KNOWLEDGE_UPDATE_ONLY_OR_HIGHER／REQUIRE_ACCESS_LOG_AND_WRITE_AUDIT_DESIGN（閲覧=writeDataAccess・書き込み=writeAudit の設計必要）／DOCS_ONLY_NOW。決定と現状（Pain 未実装・高機密未解禁・封印維持）の完全一致を read-only 監査で確認＝**DB/schema/migration/RBAC/label 定義変更なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc102 push（別承認）→ Data Classification / 高機密ラベル運用設計（doc103 候補・別承認）。
- **Customer Pain 入口レビュー: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc101）／Customer Pain 実装は未着手・§0 人間決定後の別承認待ち・高機密ラベル対応が先**。doc70 以来後続送りだった Customer Pain（顧客課題）の入口レビューを固定: Pain は「未解決の生データ」（**失注理由・クレーム・不満・競合比較・成果未達**）で CaseStudy より高リスク・**顧客名/担当者名/PII と紐づくと顧客への実害リスク**。**3案比較 → 案A（HIGH_CONFIDENTIAL_PREREQUISITE_FIRST）推奨**（高機密ラベル・権限・監査=writeDataAccess・AI禁止方針の前提設計が先・実装はまだ）。実測: ConfidentialityLabel enum に CUSTOMER_CONFIDENTIAL 等は定義済みだが **Company Brain 系での高機密扱いは保留中（doc39）＝解禁は別の重い承認**。**AI は原則参照禁止で開始（DO_NOT_INJECT）・公開は PROHIBIT_NOW・PII/実顧客データは保存しない**。**§0 人間決定候補9項目**（推奨すべて安全側）と段階9案を doc101 に固定。**AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない・公開活用なし・DB/schema/migration 変更なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc101 push（別承認）→ Customer Pain の §0 人間決定 → Data Classification / 高機密ラベル設計（別承認）。
- **CaseStudyConsent 公開活用方針決定: docs-only 完了（判定 GO・2026-07-05・記録: doc100）／公開活用は未実装・実装不要（決定＝現状の非公開状態の維持）**。doc99 §7 の §0 人間決定10項目を**すべて安全側で正式決定**: **PUBLIC_USE_POLICY=PROHIBIT_NOW（今は公開しない）**／PURPOSE_NOT_SUFFICIENT_ALONE（公開系 purpose は単独では公開条件にならない）／APPROVAL_REQUEST_REQUIRED_LATER／HUMAN_REVIEW_AND_FACT_CHECK_REQUIRED（表現審査・公開前人間承認）／EXPLICIT_PERMISSION_REQUIRED（顧客名）／EVIDENCE_REQUIRED_AND_APPROVAL（成果数値）／CUSTOMER_VOICE_PURPOSE_AND_APPROVAL_REQUIRED（顧客の声）／TAKEDOWN_REQUIRED_LATER（取り下げ運用）／NO_PR_SEO_SNS_NOW／DOCS_ONLY_NOW。**ApprovalRequest接続なし・PR/SEO/SNS/顧客の声公開なし・導入事例公開なし**のまま＝決定と現状（公開機能ゼロ）が完全一致でコード差分ゼロ。恒久禁止（AI虚偽口コミ・ステマ・根拠なし成果数値/No.1・公開系の自動実行）は許諾があっても解除されない。将来公開を進める場合は doc99 §8 の段階承認から再開。**AI参照条件変更なし（KEEP_ANONYMIZED_TRUE_ONLY のまま）・CaseStudyConsent は AI 文脈へ注入しない**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc100 push（別承認）→ Customer Pain / Stage 2・3・★2・UX / 品質基盤強化。
- **CaseStudyConsent 公開活用判断 前提整理: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc99）／公開活用は未実装・別承認待ち**。doc91 §6 段階9・doc82 §7 の5前提を実装前に詳細整理: **外部公開なし・PR配信なし・SEO公開なし・SNS投稿なし・顧客の声公開なし**のまま、**3案比較**（案A=**PROHIBIT_NOW_AND_DESIGN_GATES**（今は公開せず関門要件だけ固定・推奨）／案B=社内下書き＋人間承認のみ将来／案C=公開実装・現時点非推奨）。**external_publish / pr / seo / customer_voice purpose は単独では公開条件にならない**（PURPOSE_NOT_SUFFICIENT_ALONE・必要条件であって十分条件ではない）。**ApprovalRequest・表現審査・公開前人間承認・取り下げ運用・根拠確認が将来前提**として固定。**恒久禁止**も明文化（AI虚偽口コミ・実体験でない体験談・根拠なし成果数値/No.1表記・ステマ・公開系の自動実行）。**§0 人間決定候補10項目**（推奨すべて安全側・今は進めない前提）と段階9案。**AI参照条件変更なし（doc98 の KEEP_ANONYMIZED_TRUE_ONLY のまま）・CaseStudyConsent は AI 文脈へ注入しない・code/schema 変更なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc99 push（別承認）→ 公開活用の §0 人間決定（今は進めない前提）。
- **CaseStudyConsent AI参照条件決定: docs-only 完了（判定 GO・2026-07-05・記録: doc98）／実装不要（決定＝現状維持・コード差分ゼロ）**。doc97 の §0 人間決定9項目を**すべて安全側で正式決定**: **KEEP_ANONYMIZED_TRUE_ONLY**（AIが読む顧客事例は anonymized=true のみを維持）／ANONYMIZED_TRUE_ONLY／**DO_NOT_INJECT**（CaseStudyConsent は AI 文脈へ注入しない）／**NOT_SUFFICIENT_ALONE**（ai_reference purpose は単独では解禁条件にしない）／**STRUCTURAL_ZERO**（外部LLM送信なし）／**NO_NEW_AI_COST**（実LLMなし・AIコストなし）／KEEP_EXISTING_AI_REFERENCE_LOG／KEEP_CURRENT_NON_INJECTION_GATE／**DOCS_ONLY_NOW**（実装なし）。決定内容と現状実装の完全一致を read-only 監査で確認（anonymized: true 2件・非注入 0件・封印 UI 0件）＝**company-brain-reference 変更なし・AI参照条件変更なし**。将来この決定を変更する場合は doc97 §8 の段階承認が前提。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc98 push（別承認）→ 公開活用判断 / Customer Pain / Stage 2・3・★2・UX / 品質基盤強化。
- **CaseStudyConsent AI参照条件設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc97）／実装・変更は §0 人間決定後の別承認待ち（推奨案は現状維持＝変更ゼロ）**。全段本番確認GO後の次段（doc91 §6 段階8）として、AIが読む顧客事例の条件の扱いを実装前に設計固定: **3案比較**（案A=**KEEP_ANONYMIZED_TRUE_ONLY**（現状維持・推奨）／案B=DERIVED_ANONYMIZED_SUMMARY_ONLY（人間承認済み匿名化要約のみを将来 AI 候補に）／案C=RESTRICTED_REALNAME_AI_REFERENCE_LATER（強い承認・監査・マスキング前提の将来検討・現時点非推奨））。**ai_reference purpose は台帳にあっても単独では AI参照の解禁条件にしない（NOT_SUFFICIENT_ALONE）・CaseStudyConsent の台帳行・証跡・purpose は AI 文脈へ注入しない（DO_NOT_INJECT）・外部LLM送信は構造的ゼロ・AIコスト発生なし**を不変事項として固定。**§0 人間決定候補9項目**（AI_REFERENCE_POLICY / CASE_STUDY_SOURCE_POLICY / CASE_STUDY_CONSENT_INJECTION_POLICY / AI_REFERENCE_PURPOSE_POLICY / EXTERNAL_LLM_POLICY / AI_COST_POLICY / DATA_ACCESS_LOG_POLICY / SAFETY_GATE_POLICY / IMPLEMENTATION_POLICY・推奨はすべて安全側）と段階案を doc97 に固定。**AI参照条件変更なし・company-brain-reference 無変更（anonymized=true のみ参照を実測確認）・anonymized=false は未解禁・code/schema 変更なし**。プロダクト基準は **CaseStudyConsent anonymized=false 本格扱い / `611e51e`** のまま。次: doc97 push（別承認）→ AI参照条件の §0 人間決定。
- **CaseStudyConsent anonymized=false 本格扱い: 本番確認 GO（実装: doc95／本番確認: doc96＋doc14 §60・2026-07-05）。anonymized=false 本格扱い（表示統治）は本番確認まで完全クローズ**。利用者実測で Vercel Ready・`611e51e`・build green・CI green・**実名寄りバッジ1周**（架空・匿名事例作成→internal_view の有効な台帳登録→許諾あり＋匿名化オフ保存が通る→一覧に「実名寄り（許諾あり）」「AI参照対象外」「外部公開不可」バッジ→編集画面に「自動では匿名化に戻りません」の注意→匿名化に戻す→アーカイブ片付け）・externalAiAllowed/publishStatus/公開系 UI なし・既存画面無回帰を確認（※編集権限なしユーザーでの「閲覧制限」表示は確認用ユーザーなしのため未実測=doc96 Unknowns・GO 阻害ではない）。**本番確認GO済みプロダクト基準は CaseStudyConsent anonymized=false 本格扱い / `611e51e` へ昇格**（前基準 保存条件接続 `5e9461f` は歴史的保持）。**ConsentRecord 連携ライン（doc82〜doc96）は設計→器→台帳UI→突合→保存条件接続→表示統治の全段が本番確認GOまで完了**。**anonymized=false は未解禁（表示統治まで）・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない**のまま。次: doc96 push（別承認）→ AI参照条件判断（doc91 §6 段階8）/ 公開活用判断（段階9）。以下は実装時の記録。doc94 §7 の §0 人間決定9項目（**INTERNAL_ONLY_RESTRICTED / knowledge_update_only / badge_only / require_manual_reanonymize / title_body_only_no_customer_master_join / separate_approval_required / separate_customer_voice_purpose_required / keep_anonymized_true_only / prohibit_now**・すべて安全側・矛盾なし）に基づく表示統治の最小実装: **一覧は badge_only**（実名寄り行に「AI参照対象外」「外部公開不可」バッジ追加・表示面は広げない）＋**閲覧は knowledge_update_only**（実名寄り行のタイトル・本文断片は knowledge:update 保有者だけに表示・非保有者には「実名寄り事例（閲覧制限）」のみ・匿名化済み事例と通常一覧は無変更）＋**編集画面に手動匿名化戻しの注意**（許諾が無効になっても自動では匿名化に戻らない・自動書き換えなし）。**安全ゲートに表示統治の機械検査3種**（バッジ消失 FAIL・閲覧制限消失 FAIL・case-studies 画面の Customer マスタ join＝`prisma.customer` 参照で FAIL）＋**smoke 23本目**（実名寄り1周: 台帳登録→匿名化オフ保存→バッジ表示→手動戻し注意→匿名化に戻す→片付け・23/23 green・既存22本回帰なし）。**anonymized=false は未解禁（表示統治のみ・AI/公開に使わない）・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない・company-brain-reference 無変更**。検証全green（gate・test 256・typecheck・lint・build・**smoke 23/23**）。schema/migration/seed/package/doc14 無変更。プロダクト基準は **CaseStudyConsent 保存条件接続 / `5e9461f`** のまま。次: doc95 push（別承認）→ 本番確認（実名寄りバッジ1周）→ AI参照条件/公開活用の判断。
- **CaseStudyConsent anonymized=false 本格扱い設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc94）／実装は §0 人間決定後の別承認待ち**。保存条件接続の本番確認GO（doc93）を受け、実名寄り事例（匿名化オフ）の本格扱いを実装前に設計固定: **3案比較**（案A=社内限定・制限表示 INTERNAL_ONLY_RESTRICTED／案B=表示拡張／案C=統治整備まで封印）→ **案A推奨**（badge＋注意文言＋閲覧権限の統治のみ・解禁ゼロ・PII 露出面を増やさない）。**§0 人間決定候補9項目**（ANONYMIZED_FALSE_POLICY / VIEW_PERMISSION_POLICY / LIST_DISPLAY_POLICY / REVOCATION_POLICY（自動書き換えはしない推奨）/ CUSTOMER_NAME_POLICY / OUTCOME_NUMBERS_POLICY / CUSTOMER_VOICE_POLICY / AI_REFERENCE_POLICY（keep_anonymized_true_only 推奨）/ PUBLIC_USE_POLICY（prohibit_now 推奨））と実装9段階を doc94 に固定。**AI/公開との境界を不変事項として明文化**（anonymized=false は AI に読ませない・external_publish/pr/seo/customer_voice の purpose があっても公開には ApprovalRequest 等の別承認が必要）。**anonymized=false は未解禁・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない・code/schema 変更なし**。プロダクト基準は **保存条件接続 `5e9461f`** のまま。次: doc94 push（別承認）→ §0 人間決定 → UI/表示方針の最小実装承認（doc95 候補）。
- **CaseStudyConsent 保存条件接続: 本番確認 GO（実装: doc92／本番確認: doc93＋doc14 §59・2026-07-05）。保存条件接続は本番確認まで完全クローズ**。利用者実測で Vercel Ready・`5e9461f`・build green・CI green・**保存条件接続の1周実測**（新規作成では「許諾あり」でも匿名化オフ拒否→架空・匿名事例作成→台帳なしで匿名化オフ保存拒否（案内表示）→internal_view の有効な台帳登録→匿名化オフ保存が通る→台帳取り消し（行が残り取り消し済み表示）→再び拒否→匿名化ありに戻す→アーカイブ片付け）・externalAiAllowed/publishStatus/公開系 UI なし・既存画面無回帰を確認。**本番確認GO済みプロダクト基準は `5e9461f`（保存条件接続）へ昇格**（前基準 CaseStudyConsent UI / `1913456` は歴史的基準として保持）。「granted 申告だけで実名寄り保存が通る」暫定状態は本番でも解消済み。**anonymized=false は未解禁（保存できるだけ）・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない**のまま。次: doc93 push（別承認）→ anonymized=false 本格扱い判断（doc91 §6 段階7）。以下は実装時の記録。doc91 の §0 人間決定5項目（**CONNECT_ONLY** / TARGET_PURPOSE=**internal_view** / **CALLER_RESOLVES_SUPPRESSED_BOOLEAN** / **NO_BACKFILL** / **REDIRECT_WITH_REASON_CODE**）に基づき、匿名化を外す保存に許諾台帳の突合を必須化: **create は anonymized=false を一律拒否**（台帳行は作成後にしか登録できないため・「匿名化ありで作成→台帳登録→編集で外す」運用に固定）＋**update は anonymized=false のときだけ** 台帳を tenantId/caseStudyId で取得し、suppressed を actions 層で解決（Customer/SuppressionList を tenantId スコープで読み shared の新純粋関数 **resolveCaseStudyConsentSuppressed** で boolean 化・Customer 不在は安全側 true）して **validateCaseStudyConsentReconciliation（internal_view）** で照合、拒否は reason コード付き redirect（画面文言は PII・抑止詳細を出さず一般化）。**安全ゲートは「接続禁止」→「承認済み接続の形」検査へ更新**（接続が消えたら FAIL・テナント境界/create 拒否/internal_view 固定も機械検査。門番不変・AI 非注入 FAIL は維持）＋テスト6本追加（**test 256**）。**anonymized=false は未解禁（保存できるだけ・AI/公開に使われない）・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない**。検証全green（gate・test 256・typecheck・lint・build・**smoke 22/22**）。schema/migration/seed/package/doc14 無変更。プロダクト基準は **CaseStudyConsent UI / `1913456`** のまま。次: doc92 push（別承認）→ 本番確認（拒否→台帳登録→通る→取り消し→拒否の1周）→ anonymized=false 本格扱い判断。
- **CaseStudyConsent 保存条件接続設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc91）／接続実装は §0 人間決定後の別承認待ち**。doc90 の突合判定を保存条件（anonymized=false 保存時の必須化）へ接続する方針を実装前に設計固定: **3案比較**（案A=接続だけ先行・案B=実名寄り方針とセット・案C=封印寄り）→ **案A（CONNECT_ONLY）推奨**（申告値だけでの実名寄り保存を防ぐ純粋な安全強化・解禁ゼロ・本番に実名寄りデータ無しのため影響ゼロ）。**§0 人間決定候補5項目**（SAVE_CONNECTION_POLICY / TARGET_PURPOSE_FOR_ANONYMIZED_FALSE（推奨 internal_view）/ SUPPRESSION_CHECK_POLICY / EXISTING_DATA_POLICY（推奨 NO_BACKFILL）/ ERROR_UX_POLICY）と実装9段階を doc91 に固定。consentRecordId は据え置き（突合は caseStudyId ベース）。**保存条件接続なし・anonymized=false 未解禁・AI参照条件変更なし・code/schema 変更なし**。次: doc91 push（別承認）→ §0 人間決定 → 接続の最小実装承認（doc92 候補）。
- **CaseStudyConsent 突合判定実装: 純粋関数のみ完了（判定 GO・2026-07-05・記録: doc90）／保存条件への接続・anonymized=false はいずれも未着手・別承認待ち**。doc89 準拠で **validateCaseStudyConsentReconciliation** を shared に追加（**純粋関数・DB 読み出しなし**・Prisma import なし・now/suppressed は引数・evidence は存在確認のみ・理由コード17種）＋**否定系テスト20本（test 230→250）**＋**安全ゲートに段階分離の機械検査3種**（①case-studies/actions.ts への接続混入で FAIL ②匿名化の門番 canDisableAnonymization の実装変更で FAIL ③company-brain-reference への突合/台帳参照混入で FAIL）。**どこにも接続していない＝挙動不変**。**anonymized=false 未解禁・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない**。検証全green（gate・test 250・typecheck・lint・build。smoke は UI 変更なしのため未実施=成功扱いしない）。次: doc90 push（別承認）→ 保存条件接続（段階3）/ anonymized=false 扱い（段階6）の人間判断。
- **CaseStudyConsent 突合判定設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc89）／実装は未着手・別承認待ち**。doc83 §9 段階3（granted の真正性を台帳で機械確認する突合判定）を実装前に設計固定: **有効条件15項目 AND**（granted＋有効な台帳行＋tenantId/caseStudyId 一致＋status granted＋revoked/expired/suppressed でない（SuppressionList 照会）＋purpose に対象用途＋archivedAt null＋private＋NORMAL/INTERNAL＋人間登録＋evidence 存在・本文は注入しない）／拒否条件の明示／purpose 6区分の扱い（**公開系は台帳だけでは足りない=ApprovalRequest 等の5前提・今回解禁しない**）／関数は**案B推奨=新純粋関数 validateCaseStudyConsentReconciliation**（既存の匿名化門番 validateCaseStudyConsent を壊さない・DB 読み出しは呼び出し層）／否定系テスト17本案／安全ゲート案（**匿名化解禁が同時に入ったら FAIL の段階分離検査**含む）／実装8段階（今回は段階1のみ）。**validateCaseStudyConsent 拡張なし・anonymized=false 未解禁・AI参照条件変更なし・code/schema 変更なし**。次: doc89 push（別承認）→ 突合判定の実装承認判断（純粋関数＋テスト＋ゲートのみ・保存条件接続と匿名化解禁はさらに別承認）。
- **CaseStudyConsent UI: 本番確認 GO（実装: doc87／本番確認: doc88＋doc14 §58・2026-07-05）。CaseStudyConsent UI は本番確認まで完全クローズ**。利用者実測で Vercel Ready・`1913456`・build green・CI green・**架空・匿名事例で台帳1周**（作成→許諾台帳→登録（証跡ガイド表示確認・所在説明のみ・期限必須）→一覧→詳細→取り消し（行が残り取り消し済み表示=物理削除禁止が本番で成立）→アーカイブ片付け）・externalAiAllowed/publishStatus/公開系 UI なし・既存画面無回帰を確認。**本番確認GO済みプロダクト基準は CaseStudyConsent UI / `1913456` へ昇格**。**CaseStudyConsent は AI 文脈へ注入しない・AI参照条件変更なし・anonymized=false 未解禁**のまま。次: doc88 push（別承認）→ **突合判定（doc83 §9 段階3）承認判断**。以下は実装時の記録。
- **CaseStudyConsent UI実装: 完了・ローカル検証全green（判定 GO・2026-07-05・記録: doc87）／本番確認はこれから（push・本番確認とも別承認）**。doc86 準拠で許諾台帳UIを最小実装: `/brain/case-studies/[id]/consents`（一覧・登録・閲覧・revoke の**4操作のみ**・自由編集なし・**物理削除禁止**）＋Server Action 2本（create/revoke・**人間のみ**=isHumanUser・knowledge:update・tenantId・**writeAudit（audit に証跡本文/PII を入れない）**）＋shared 純粋関数 validateCaseStudyConsentInput（**用途未記載不許可・evidence 空拒否・expiresAt 必須・期限逆転拒否**・否定系テスト8本=test 230）＋**安全ゲート拡張**（consents actions の isHumanUser/writeAudit/物理削除禁止/入力検証・登録画面の証跡ガイド文言・**company-brain-reference に CaseStudyConsent 非存在=AI 非注入の機械検査**）＋**smoke 22本目**（事例作成→台帳登録→取り消し→片付けの1周・22/22 green・既存21本回帰なし）。**Customer picker なし（CaseStudy.customerId 自動反映・PII 非複製）・status は granted 固定・AI参照条件変更なし・anonymized=false 未解禁・連携までは anonymized=true のみAI参照**。schema/migration/seed/package/company-brain-reference 無変更。プロダクト基準は **CaseStudyConsent schema / `812ae69`** のまま。次: doc87 push（別承認）→ 本番確認（架空事例で1周→片付け）→ 突合判定（doc83 §9 段階3）承認判断。
- **CaseStudyConsent UI設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc86）／UI 実装は未着手・別承認待ち**。台帳UI（doc83 §9 段階2）の実装前安全設計を固定: 推奨スコープ=**事例単位の一覧・新規登録・閲覧・取り消し（revoke）のみ**（ルート案1 `/brain/case-studies/[id]/consents`・自由な編集なし・物理削除禁止・誤登録は revoke→再登録の運用）。**人間のみ**（isHumanUser・AIロールは作成/取り消し不可）・knowledge:update 基本候補・tenantId スコープ・**writeAudit 必須（audit に証跡本文/PII を入れない最小情報）**。入力: purpose 6区分 checkbox（空拒否）・**evidence は所在説明のみ**（原本/PII を貼らないガイド明記）・**expiresAt 必須**（grantedAt 以前は拒否）・customerId は自由入力させず CaseStudy.customerId 自動反映（Customer picker は作らない=PII 近接）。**AI参照条件変更なし**（CaseStudyConsent は AI 文脈へ注入しない・**anonymized=false 未解禁**・連携までは anonymized=true のみAI参照）。実装時のテスト/安全ゲート/smoke 案・停止条件も doc86 に固定。次: doc86 push（別承認）→ **台帳UI 実装承認判断（doc87 候補）**。
- **CaseStudyConsent schema: 本番確認 GO（実装: doc84／本番確認: doc85＋doc14 §57・2026-07-05）。CaseStudyConsent schema は本番確認まで完全クローズ**。利用者実測で Vercel Ready・`812ae69`・build green（追加のみ migration のため build 成功=migrate deploy 成功の前例枠組み）・CI green・既存主要画面無回帰・**CaseStudyConsent 画面なし=schema-only のため正常**を確認。**本番確認GO済みプロダクト基準は CaseStudyConsent schema / `812ae69` へ昇格**。**UI 未実装・writeAudit 未実装・突合判定未実装・anonymized=false 未解禁・ConsentRecord連携までは anonymized=true のみAI参照**＝書き込み経路ゼロで本番の台帳テーブルは空のまま。次: doc85 push（別承認）→ 台帳UI（doc83 §9 段階2）/ 突合判定（段階3）の承認判断。以下は実装時の記録。
- **CaseStudyConsent schema: 追加完了（schema-only・判定 GO・2026-07-05・記録: doc84）／UI・突合判定は未実装・別承認待ち**。§0 人間決定4点（consentRecordId 据え置き・evidence は所在説明のみ TEXT_POINTER_ONLY・**expiresAt 必須=NULL_NOT_ALLOWED（期限なし許諾は認めない）**・登録は人間のみ）に基づき、doc83 案B の器 **CaseStudyConsent model 1件のみ**を schema に追加＋migration `20260705002819_phase2c_consent_case_study_consent`（**追加のみ・破壊的SQLなし**: CREATE TABLE＋INDEX 2本のみ・機械確認済み）。**既存 ConsentRecord/CaseStudy/SuppressionList/Customer 無変更・relation 追加なし・PII 複製なし**。検証全green（validate・migrate dev＋status・test 222・typecheck・lint・build。smoke は schema-only のため未実施=成功扱いしない）。**書き込み経路ゼロ（UI なし）＝本番テーブルは空のまま・writeAudit/validateCaseStudyConsent 拡張/突合判定は未実装（doc83 §9 の段階2〜3・別承認）**。**ConsentRecord連携までは anonymized=true のみAI参照・anonymized=false は未解禁**。次: doc84 push（別承認）→ 本番確認（schema-only・画面なしが正常）→ 台帳UI/突合判定の承認判断。
- **ConsentRecord連携器選択: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc83）／schema実装は未着手・別承認待ち**。doc82 の人間選択事項だった許諾台帳の器を確定: **案B推奨 = CaseStudyConsent（事例許諾専用新モデル）**。理由: 既存 ConsentRecord はメール営業チャネル同意用で意味が異なり、相乗り（案A）は配信同意と事例許諾の行が混在して監査・運用リスクになる。案B は**追加のみ migration・破壊的SQLなし・既存テーブル無変更**で前例どおりの最小審査。フィールド案（tenantId/caseStudyId/customerId=ID参照のみ/status granted・revoked/purpose 6区分明示・用途未記載は不許可/evidence/grantedAt/expiresAt/revokedAt）と突合判定・実装5段階（schema→UI→突合判定→匿名化解除判断→公開活用、各段個別承認）を doc83 に固定。**ConsentRecord連携までは anonymized=true のみAI参照維持・anonymized=false は別承認**・外部公開/PR/SEO/顧客の声公開は禁止のまま。schema 承認前に人間確定する Unknowns 4点（consentRecordId の扱い・evidence 保管・expiresAt null の意味・登録権限者）も記録。次: doc83 push（別承認）→ CaseStudyConsent schema 実装承認判断。
- **ConsentRecord連携設計: docs-only 完了（判定 READY / GO・2026-07-05・記録: doc82）／実装は未着手・別承認待ち**。Phase 2-C クローズ後の後続設計第一弾。**consentStatus=granted だけでは真正な許諾証拠として扱わない**ことを固定し、真正な許諾は将来 ConsentRecord 突合（誰が・何の用途で・いつまで・どんな証跡で）で確認する方針を設計。**ConsentRecord連携までは anonymized=true のみ参照を維持**・実名寄り事例/成果数値/顧客の声は AI にも公開にも使わない。read-only 監査の発見: **既存 ConsentRecord はメール営業のチャネル同意用で、用途・失効・期限・証跡を持たない**→ 連携の器は（案A）既存拡張 /（案B）事例許諾専用新モデルの人間選択（**推奨は案B**=追加のみ migration）。匿名化解除の将来条件（granted＋有効な consentRecordId＋同一tenant＋revoked/expired/suppressed でない＋用途明示＋公開系は ApprovalRequest）・取り消し時の安全側挙動（anonymized=true へ戻す・AI参照除外・非公開化・writeAudit・ai_reference 履歴保持）・公開活用の5前提（連携・表現審査・公開前人間承認・取り下げ・証跡）を doc82 に固定。**schema/コード変更なし・anonymized=false 解禁なし・外部公開/PR/SEO/SNS/顧客の声公開は禁止のまま**。次: doc82 push（別承認）→ ConsentRecord連携の実装判断（段階承認）。
- **Phase 2-C: 正式完了（Phase 2-C-CLOSE・判定 GO・2026-07-05）。完了対象の最新の本番確認GO済みプロダクト基準: Phase 2-C-5 / `6d656a3`**（詳細 `docs/audit/81_phase2c_close.md`・doc14 §56）。CaseStudy 領域（顧客事例）＝「入口レビュー（doc70）→ 絞り込み設計（doc71・Case Study 先行/Customer Pain 後続）→ schema（doc72/73）→ read-only（doc74/75）→ 人間書き込み（doc76/77・許諾の門番本番実証）→ AI参照（doc78/79/80）」の全段が本番確認GOまで完了。**Company Brain AI参照4テーブル体制（会社方針・商品カタログ・営業プレイブック・顧客事例）が本番確認済み**・AIが読める顧客事例は匿名化済み（anonymized=true）のみ・外部LLM注入ゼロ・未解消HOLDなし。後続送り（いずれも別承認・後続課題ゼロではない）: ConsentRecord連携・Customer Pain（高機密ラベル対応が先）・公開活用（PR配信/SEO/SNS/口コミ/顧客の声公開）・Stage 2・Stage 3・★2・UX改善・品質基盤強化。以下は Phase 2-C 各段の当時記録。
- **Phase 2-C-5: CaseStudy AI参照 — 完了。本番確認 GO（設計: doc78／実装: doc79／本番確認: doc80＋doc14 §55・2026-07-05）。Phase 2-C-5 は本番確認まで完全クローズ**。利用者実測で「架空・匿名の事例1件作成 → タイトルでナレッジ検索 → AIの回答＋参照した会社の頭脳に表示 → **ai_reference（entityType=CaseStudy）記録** → テスト事例アーカイブ片付け」の end-to-end を本番確認（Vercel Ready・`6d656a3`・build green・CI green・既存画面無回帰）。**本番確認GO済みプロダクト基準は Phase 2-C-5 / `6d656a3` へ昇格**。**Company Brain AI参照4テーブル体制（会社方針・商品カタログ・営業プレイブック・顧客事例）が本番確認済み**・AIが読める CaseStudy は匿名化済み（anonymized=true）のみ・外部LLM注入ゼロ。次: doc80 push（別承認）→ **Phase 2-C 完了判定（人間判断・doc81 候補）** / ConsentRecord 連携設計 / Customer Pain / Stage 2・3・★2・UX の人間選択。以下は実装時の記録。doc78 §3/§5 どおり company-brain-reference に**4テーブル目**として追加（既存3テーブル無変更）: where = tenantId・archivedAt:null・**publishStatus 'private' のみ・anonymized=true（匿名化済み）のみ**・NORMAL/INTERNAL のみ。**sourceNote/customerId/consentRecordId/consentStatus は select せず注入しない**（granted を参照根拠にしない）。【顧客事例/業種】prefix 文脈化・**MAX_TOTAL=5 据え置き**・externalAiAllowed ゲート維持（外部LLM時は構造的ゼロ）・ai_reference は既存ループでレコード単位自動記録（knowledge/search・audit・db 無変更）。**安全ゲートに「匿名化済みのみ参照」等の機械検査4件を追加**。検証全green（gate actions4/ui151・test 222・typecheck・lint・build・**smoke 21/21**・既存20本回帰なし・ローカルのみ）。schema/migration/seed/package/lock 無変更。GO済み基準は Phase 2-C-4 / `11e8f51` のまま（本番確認前のため昇格しない）。次: doc79 push（別承認）→ 本番確認 → 2-C-5-PROD（doc80 候補）。以下は設計時の記録。AI が顧客事例を読む条件を設計固定: **anonymized=true（匿名化済み）のみ参照・publishStatus 'private' のみ・NORMAL/INTERNAL のみ・tenantId/archivedAt:null 必須・consentStatus は参照条件に使わない（granted でも ConsentRecord 未連携のため慎重扱い）**。company-brain-reference の4テーブル目候補として既存の型に追加するだけ（MAX_TOTAL=5 据え置き候補・sourceNote/customerId/consentRecordId は注入しない・ai_reference はレコード単位で自動記録・外部LLM時は externalAiAllowed ゲートで構造的ゼロ）。**実装・company-brain-reference.ts 変更・ConsentRecord 連携はいずれも別承認**。GO済み基準は Phase 2-C-4 / `11e8f51` のまま。
- **Phase 2-C-4: CaseStudy 人間書き込み（作成・編集・アーカイブ）— 完了。本番確認 GO（実装: doc76／本番確認: doc77＋doc14 §54・2026-07-05）。Phase 2-C-4 は本番確認まで完全クローズ**。利用者実測で Vercel Ready・`11e8f51`・build green・CI green・**作成→編集→アーカイブの1周（架空・匿名のみ・片付け済み）・許諾なしでの匿名化解除が本番で拒否されること・externalAiAllowed/publishStatus UI なし**・既存画面無回帰を確認。**本番確認GO済みプロダクト基準は Phase 2-C-4 / `11e8f51` へ昇格**。**会社の頭脳4テーブルすべてが「人間が書き・AIは書けない・消せない・writeAudit が残る」体制で本番稼働**（顧客事例のみ許諾の門番つき）。次: doc77 push（別承認）→ Phase 2-C-5（AI参照・別承認）/ ConsentRecord 連携設計 / Stage 2・3・★2・UX の人間選択。以下は実装時の記録。2-B-4 の型を流用した Server Action 3操作＋**AI mutation禁止（shared isHumanUser・actions 4本目）**＋**writeAudit 3操作**＋tenantId スコープ＋label 2択＋externalAiAllowed false 固定＋**publishStatus 'private' 固定（公開機能なし）**＋物理削除禁止＋**匿名化解除は consentStatus='granted' のときだけ許可（shared validateCaseStudyConsent で機械拒否・否定系テスト6本・安全ゲートで常時検査）**＋入力ガイド（顧客名・取引先名・成果数値・顧客の声は許諾なしに書かない）。**静的安全ゲートは 4actions 体制へ拡張**（CaseStudy の非公開固定・許諾判定使用も機械検査）。検証全green（安全ゲート actions4/ui151・**test 222**・typecheck・lint・build・**smoke 20/20**・既存回帰なし・ローカルのみ）。schema/migration/seed/labels/RBAC/company-brain-reference/package/lock 無変更。**AI参照は 2-C-5 の別承認・ConsentRecord 連携は後続別承認**。GO済み基準は Phase 2-C-3 / `408857d` のまま（本番確認前のため昇格しない）。次: doc76 push（別承認）→ 本番確認 → 2-C-4-PROD（doc77 候補）。
- **Phase 2-C-3: CaseStudy read-only 画面＋架空 seed — 完了。本番確認 GO（実装: doc74／本番確認: doc75＋doc14 §53・2026-07-05）。Phase 2-C-3 は本番確認まで完全クローズ**。利用者実測で Vercel Ready・`408857d`・build green・CI green・**ナビ「顧客事例」表示・`/brain/case-studies` 表示・一覧は EmptyState（本番 seed 未投入のため正常）・作成/編集/アーカイブボタンなし**・既存画面無回帰を確認。**本番確認GO済みプロダクト基準は Phase 2-C-3 / `408857d` へ昇格**。本番の CaseStudy テーブルは空のまま＝書き込み経路ゼロが本番でも成立し、実顧客情報は本番に存在しない。次: doc75 push（別承認）→ Phase 2-C-4（人間書き込み・doc76 候補）承認判断。以下は実装時の記録。2-A-3a/2-B-3 の型で閲覧専用一覧 `/brain/case-studies`（requireUser→knowledge:read→tenantId・archivedAt:null・**publishStatus='private' のみ・label NORMAL/INTERNAL のみ**）＋ナビ「顧客事例」1行＋**架空 seed 4件**（全件「（架空）」明記・anonymized=true・consentStatus='none'・externalAiAllowed=false・実在顧客名/実成果数値/顧客の声なし）＋smoke 19本目（作成/編集/アーカイブ UI が無いことも機械確認）。**作成・編集・削除・Server Action・writeAudit・AI参照は未実装＝書き込み経路ゼロのまま**（2-C-4/2-C-5 の別承認）。検証全green（安全ゲート ui 148・test 216・typecheck・lint・build・**smoke 19/19**・既存18本回帰なし・ローカルのみ）。schema/migration/labels/RBAC/package/lock 無変更。GO済み基準は Phase 2-C-2 / `b012bd0` のまま（本番確認前のため昇格しない）。次: doc74 push（別承認）→ 本番確認 → 2-C-4 承認判断。
- **Phase 2-C-2: CaseStudy schema 追加 — 完了。本番確認 GO（実装: doc72／本番確認: doc73＋doc14 §52・2026-07-05）。Phase 2-C-2 は本番確認まで完全クローズ**。利用者実測で Vercel Production Ready・対象 commit `b012bd0`・build green（migrate ログは直接未確認＝build 成功で判定・前例どおり）・CI green・既存主要画面無回帰・**CaseStudy 画面なし=schema-only のため正常**を確認。**本番確認GO済みプロダクト基準は Phase 2-C-2 / `b012bd0` へ昇格**。本番DBには CaseStudy テーブルが追加のみ（破壊的SQLなし）・書き込み経路ゼロで実データ混入は構造的に不可能。次: doc73 push（別承認）→ Phase 2-C-3（read-only 画面＋架空 seed・doc74 候補）承認判断。以下は実装時の記録。doc71 §6-2 準拠で schema.prisma に **CaseStudy model 1件のみ追加**＋migration `20260704160836_phase2c2_case_study`（**追加のみ・破壊的SQLなし**・CREATE TABLE＋INDEX 3本）。安全 default を器に固定: **anonymized=true・publishStatus='private'・consentStatus='none'・externalAiAllowed=false・label INTERNAL（UI/action 側で NORMAL/INTERNAL 2択に制限予定）**・customerId/consentRecordId は relation なし ID 参照。**CustomerPain model なし・ConsentRecord/SuppressionList/Customer/ConfidentialityLabel enum/labels.ts/RBAC/company-brain-reference.ts 無変更**。検証全green（ローカルのみ: migrate dev＋status・安全ゲート・test 216・typecheck・lint・build。smoke は schema-only のため未実施=成功扱いしない）。**seed/UI/Server Action/writeAudit/AI参照は 2-C-3〜2-C-5 の別承認**。GO済み基準は Phase 2-B-5 / `83d35bc` のまま（本番確認前のため昇格しない）。
- **Phase 2-C-1: Case Study / Customer Pain 絞り込み詳細設計 — 完了（docs-only・判定 READY / GO・記録: doc71）／実装は未着手・別承認待ち**。read-only 監査（doc33・doc50・doc70・schema・labels・ConsentRecord/SuppressionList 実測）に基づき、**Case Study（顧客事例）先行・Customer Pain（顧客課題）後続で確定**（doc33 以来の評価と矛盾なし。Customer Pain は高機密ラベル解禁という別の重い承認が先）。絶対条件を設計に固定: **顧客名・取引先名・成果数値・顧客の声は許諾なしに扱わない・公開/SNS/PR配信/SEO公開しない・初期スコープに公開機能を作らない・非公開/架空データ/匿名化 default true・label 2択（高機密ラベル解禁なし）・externalAiAllowed false 固定・AI mutation禁止・物理削除禁止・writeAudit**。段階計画 = 2-C-2 schema → 2-C-3 read-only 画面 → 2-C-4 人間書き込み → 2-C-5 AI参照（各段別承認）。**外部送信なし・DB操作なし・schema変更なし**。**Phase 2-C実装・Case Study実装・Customer Pain実装は別承認**。**Phase 8 / ENSHiN OS外部発信なし**。GO済み基準は **Phase 2-B-5 / `83d35bc`** のまま。
- **Phase 2-C-ENTRY: 次領域入口レビュー — 完了（判定 READY / GO・記録: doc70）／実装は未着手・別承認待ち**。Phase 2-B 正式完了＋Phase X-05 第一波完了後の8候補（Case Study / Customer Pain / Stage 2 / Stage 3 / ★2 / UX改善 / GitHub・Obsidian整備 / Candidate整理）を比較。**推奨1位 = Phase 2-C-1: Case Study / Customer Pain の絞り込み詳細設計 docs-only**（品質基盤の主要な穴が塞がったため事業価値側へ戻る。ただし顧客名・成果数値・顧客の声・許諾・公開リスクがあるため**まだ実装しない**・非公開/架空/許諾前提の設計から）。推奨2位=Stage 2・3位=★2。**Phase 2-C実装・Case Study実装・Customer Pain実装・Stage 2・Stage 3・★2 はいずれも別承認**。
- **Phase X-05-3: Company Brain 静的安全ゲート第一弾 — 完了。CI実走確認 GO（実装: doc68／実走確認: doc69・確認日 2026-07-04 申告）。X-05-3 は完全クローズ**。利用者実測で、CI 最新 run（commit `58be7c7`）が **green・失敗なし・step 一覧に「Company Brain safety checks」があり緑で成功**を確認（AI が直接確認したものではない）。**静的安全ゲートが本稼働**＝push のたびに「安全境界の存在検査→test 216→typecheck→lint」が自動実行される。doc63 §5 の ★1/★3/★4/★5 が CI実走まで完了。残: ★2・Stage 2/3 は別承認。以下は実装時の記録。`scripts/check-company-brain-safety.mjs`（Node標準のみ・package/lock無変更）で ★3 label 2択・★4 externalAiAllowed 封印（UI 147ファイル走査）・★5 物理削除禁止・isHumanUser 共通化維持を機械検査し、**CI Stage 1 に step 追加（db:generate → 安全ゲート → test → typecheck → lint）**。検証全green（script exit 0・test 216・typecheck・lint・build。smoke は app 挙動不変のため未実施＝成功扱いしない）。doc63 §5 の ★1/★3/★4/★5 が自動検証化済み。残: ★2 権限拒否 E2E・Stage 2/3 は別承認。
- **Phase X-05-2: 否定系テスト第一弾（AIロール拒否）— 完了。CI実走確認 GO（実装: doc66／実走確認: doc67・確認日 2026-07-04 申告）。X-05-2 は完全クローズ**。利用者実測で、push 後の GitHub Actions CI 最新 run（commit `b27a979`）が **green・失敗なし**を確認（AI が直接確認したものではない）。**test 216（否定系5本含む）が CI で自動実行される品質ゲートとして本稼働**＝「AIは会社の頭脳を書き換えられない」が push のたびに自動検証され続ける。残: ★2〜★5・Stage 2 は別承認。以下は実装時の記録。A案採用: brain 3actions に3重複していた isHumanUser を packages/shared の純粋関数へ抽出し、**否定系テスト5本を追加（test 211→216）**。「rbac 上 AI_AGENT は knowledge:create を持つが actions 層で書き込み拒否」という前提自体もテストで恒久固定。**挙動不変（smoke 18/18 green で実証）・RBAC変更なし・schema/seed/package/lock/workflow 無変更**。doc63 の最大の穴（AI書き込み禁止層のテストゼロ）が解消。残: ★2〜★5（権限拒否・label制限・封印・物理削除の各テスト）と Stage 2 は別承認。
- **Phase X-05-1: CI Stage 1 — 完了。実走確認 GO（実装: doc64／実走確認: doc65・確認日 2026-07-04 申告）。Phase X-05-1 は完全クローズ**。`.github/workflows/ci.yml`（push/PR 時に install→db:generate→test→typecheck→lint を自動実行・secrets不使用・DB不要）が main 反映済みで、**利用者実測により GitHub Actions の最新 run（commit `116efd6`）が green・失敗なしを確認**。AI が実走を直接確認したものではない。**品質ゲートが自動で常時稼働する状態になった**。Stage 2 build・Stage 3 smoke・否定系テスト（X-05-2）は別承認。
- **Phase X-05: CI・否定系テスト — 設計確認済み（Phase X-05-ENTRY・判定 READY / GO・記録: doc63）／実装は X-05-1 で CI Stage 1 のみ着手済み**。要点: CI は3段階導入（Stage 1=test/typecheck/lint・Stage 2=build・Stage 3=smoke on CI は後日判断）・否定系テストは8対象（最優先=**actions 層 isHumanUser の無テスト状態**: rbac 上 AI_AGENT は knowledge:create を持つため actions 層が唯一の砦なのにテストゼロ、と read-only 調査で特定）。実装方式 A（shared へ純粋関数抽出＋単体テスト・推奨）/ B（E2E＋静的チェックのみ）は実装ミッションで人間判断。**workflow作成・テスト実装・package/lock 変更は次ミッションの別承認**。**Phase 2-C実装・Case Study実装・Customer Pain実装にも個別人間承認なしに進まない**。
- **Phase 2-B: 正式完了（Phase 2-B-CLOSE・判定 GO）。完了対象の最新本番確認GO済みプロダクト基準: Phase 2-B-5 / `83d35bc`**（詳細 `docs/audit/62_phase2b_completion_record.md`・doc14 §51）。営業プレイブック（SalesPlaybookEntry）＝「設計（doc51）→ schema（doc52/53）→ read-only（doc54〜56）→ 人間書き込み（doc57/58）→ AI参照（doc59〜61）」の全5段が本番確認GOまで完了。**会社の頭脳3テーブル（会社方針・商品カタログ・営業プレイブック）すべてが「人間が書き・AIが読み・読んだら記録・外部AIには出さない」体制で本番稼働**。HOLD 2件（2-B-3 ナビ／2-B-5 ai_reference 表示場所）は追記主義で解消済み・未解消HOLDなし。後続送り（別承認）: 頭脳2画面→プレイブックのタブ導線・アーカイブ文言/視認性・実LLMキー設定（外部送信解禁とセット）・CI導入・否定系テスト・doc49 script化・Case Study / Customer Pain・次領域入口レビュー。以下は Phase 2-B 各段の当時記録。
- **Phase 2-B-5: SalesPlaybookEntry AI参照追加 — 完了。本番確認 GO（設計: doc59／実装: doc60／本番確認: doc61＋doc14 §50・2026-07-04）。Phase 2-B-5 は本番確認まで完全クローズ**。利用者実測で、本番UIで作成した営業プレイブック（未アーカイブ・架空内容）→ ナレッジ検索 → 「参照した会社の頭脳」にタイトル表示 → **`/admin/data-access-logs` に ai_reference（entityType=SalesPlaybookEntry）** の end-to-end を確認。初回報告の「ai_reference 見当たらず」は表示場所の違い（監査ログ本体ではなく機密参照ログ）で、68点表示は FakeLLM 仕様（信頼度0.68固定）と切り分け済み。改善候補（別承認）: 頭脳2画面→プレイブックのタブ導線・アーカイブ文言/視認性・実LLMキー設定。以下は実装時の記録。doc59 どおり company-brain-reference.ts に3テーブル目として追加（read-only・tenantId・archivedAt:null・NORMAL/INTERNAL のみ・canAccessLabel・**MAX_TOTAL=5 据え置き**・doNotSay は「言わない:」プレフィックス・related IDs 未展開・**外部LLM時は externalAiAllowed=true＋maskText のみ＝全件 false のため構造的にゼロ**）。ai_reference は既存 writeAIDataAccess の流用でレコードごと自動記録（knowledge/search・audit.ts・db.ts は無変更）。smoke は18本体制で **18/18 green**（18本目=参照元表示・既存17本回帰なし）。schema/migration/seed/rbac/labels/package/lock 無変更。**GO済み基準は Phase 2-B-4 / `26a7a30` のまま**（本番確認 GO までは昇格しない）。**Phase 2-B 全体クローズ判定は本番確認後**。外部LLM送信解禁・高機密・Phase 8・ENSHiN OS外部発信には進まない。
- **Phase 2-B-4: SalesPlaybookEntry 人間書き込み（作成・編集・アーカイブ）— 完了。本番確認 GO（実装: doc57／本番確認: doc58＋doc14 §49・2026-07-04）。Phase 2-B-4 は本番確認まで完全クローズ**。利用者実測で作成→編集→アーカイブの1周・ラベル2択・externalAiAllowed UIなし・入力ガイド表示・writeAudit 3操作の監査ログ・既存画面無回帰をすべて確認（テストデータは架空内容のみ・アーカイブで片付け済み）。以下は実装時の記録。2-A-3b-1/3b-2 の型を流用: Server Action 3操作（create/update/archive）＋**AI mutation禁止（actions 層で AIロール一律拒否・rbac.ts 無変更）**＋writeAudit 3操作＋物理削除なし（archivedAt のソフトアーカイブのみ）＋**label 2択（NORMAL/INTERNAL）**＋**externalAiAllowed 封印（create=false 固定・update 不変更・true UI なし）**＋**入力ガイド画面明記（顧客名・会社名・成果数値・口コミ・顧客の声・実価格を書かない）**。smoke は17本体制で **17/17 green**（16本目はナビ経由確認へ意図的に期待値更新=doc57 §5・17本目=作成フロー追加・既存15本回帰なし）。schema・migration・seed・rbac・labels・package/lock 無変更。AI参照（writeDataAccess/ai_reference）は未実装=2-B-5 の別承認。**GO済み基準は Phase 2-B-3 / `a2bb2b6` のまま**（本番確認前のため昇格しない）。次: push-only（別承認）→ 本番確認（doc49 の型・doc58 候補）→ 2-B-5 承認判断。
- **Phase 2-B-3: SalesPlaybookEntry read-only 可視化 — 完了。一度 HOLD → 再実測 GO で解消済み（実装: doc54／HOLD経緯: doc55＋doc14 §47／解消GO: doc56＋doc14 §48・2026-07-06）。Phase 2-B-3 は本番確認まで完全クローズ**。HOLD の唯一のNG（ナビ表示）はハードリロードで解消＝ブラウザキャッシュ起因の可能性が高く、read-only 調査どおり repo 側は潔白・**コード修正は不要だった**。教訓: NG時の一次対処はハードリロード／smoke のナビ表示検証追加は改善候補（別承認）。書き込み（2-B-4）・AI参照（2-B-5）は別承認。seed デモデータ6件（playbookType 4種網羅・PII/実価格/口コミ/顧客の声ゼロ・全件 externalAiAllowed=false・NORMAL/INTERNAL のみ）＋read-only 一覧 `/brain/playbooks`（knowledge:read＋tenantId・作成/編集/削除/Server Action なし）＋ナビ1行＋**smoke 16/16 green（既存15本回帰なし）**。書き込み（2-B-4）・AI参照（2-B-5）は別承認。本番は seed 未実行のため一覧が空で正常（doc49 原則で本番確認条件を事前定義済み）。
- **Phase 2-B-2: SalesPlaybookEntry schema変更 — main反映済み・本番確認GO（実装記録: doc52／本番確認: doc53＋doc14 §46・2026-07-05）。Phase 2-B-2 は完全クローズ**。§0 人間承認（APPROVED・呼称=Phase 2-B のまま・参照構造=ID配列・playbookType=approach/objection/preparation/talk_track）に基づき、doc51 §4 どおり model 追加＋migration 1つ（CREATE TABLE＋INDEX 3本のみ・destructive 0・既存model無変更）。検証: validate/migrate dev(ローカル)/status/test 211/typecheck/lint/build 全green。次は push-only（別承認）→ 本番確認（doc49 の型・既存画面無回帰が主眼）→ Phase 2-B-3 承認判断。
- **Phase 2-B-1: SalesPlaybookEntry 設計 docs-only 完了（判定 GO・記録: doc51）／schema変更は 2-B-2 で実施済み**。設計の柱: 顧客名・事例・顧客の声を最初から扱わない「売り方の型」専用・既存2モデルの流儀を踏襲（tenantId スカラ・label 2択・externalAiAllowed false 封印・ソフトアーカイブ・関連参照は ID 配列案を推奨）・AI mutation禁止を actions 層で最初から・AI参照追加は 2-B-5 の別承認・三段承認計画（2-B-2 schema → 2-B-3 read-only → 2-B-4 書き込み → 2-B-5 AI参照 → 各段 PROD は doc49 の型）。
- **Phase 2-B: 入口レビュー完了（Phase 2-B-ENTRY・判定 READY / GO・記録: doc50）／実装・schema変更・migration は未着手・次は人間判断**。doc33 の後続3領域を再評価し、**推奨 = Phase 2-B-1: SalesPlaybookEntry の設計 docs-only**（PII最遠・2-Aの安全境界を流用可）。Case Study は許諾管理・公開前承認・広告表現チェックの設計とセットで後続、Customer Pain は高機密ラベル対応（別の重い承認）の後で最後。呼称注意: roadmap 01 の「2-B」は CRM/Sales AI を指す（呼び分けは人間判断）。ENSHiN OS 詳細仕様は未提供のまま（証拠不足・doc07 方針維持）。
- **Phase X-04（本番スモーク定型化）: docs-only 完了（判定 GO・2026-07-04・記録: doc49）**。Phase 2-A の HOLD 2件の教訓を「本番確認プレイブック」として固定（利用者実測のみ・§0 テンプレート・GO/HOLD/STOP 判定・**本番に実在するデータで確認**・ENSHiN OS 追加停止条件・標準プロンプト骨子）。以後の本番確認は doc49 の型に従う。**script化・E2E拡張・本番自動監視は未実装・後続別承認**。
- **Phase 8（実課金・Stripe・usage billing・credits・cap/alert）には進まない**（別設計・別承認が前提）。

## 最新の本番確認GO済みプロダクト基準

- 最新の本番確認 GO 済みプロダクト基準: **CaseStudyConsent anonymized=false 本格扱い**
- 内容: **実名寄り（匿名化オフ）事例の社内限定・制限表示（INTERNAL_ONLY_RESTRICTED）の本番確認 GO 記録。一覧は badge_only（実名寄り行に AI参照対象外・外部公開不可バッジ）・閲覧は knowledge_update_only（実名寄りの内容は編集権限者のみ）・手動匿名化戻し注意（自動書き換えなし）。anonymized=false は未解禁（表示統治まで・AI/公開に使われない）・AI参照条件変更なし・CaseStudyConsent は AI 文脈へ注入しない**
- 基準 commit（本番確認 GO 済み基準）: CaseStudyConsent anonymized=false 本格扱い / `611e51e`（※現在 HEAD ではなく基準 commit。現在位置は git を参照。昇格記録: doc96＋doc14 §60・2026-07-05）
- 詳細: `docs/audit/96_case_study_consent_anonymized_false_policy_production_confirmation.md`・doc14 §60
- （前基準: CaseStudyConsent 保存条件接続 = 匿名化オフ保存への突合必須化の本番確認 GO。以下は当時の記録として保持）
- 内容: **匿名化を外す保存（anonymized=false）に許諾台帳の有効な行（internal_view・期限内・取り消しなし・証跡・人間登録・非抑止）との突合を必須化（CONNECT_ONLY）した保存条件接続の本番確認 GO 記録。新規作成では匿名化を外せない（作成→台帳登録→編集で外す運用）**
- 基準 commit（本番確認 GO 済み基準）: CaseStudyConsent 保存条件接続 / `5e9461f`（※現在 HEAD ではなく基準 commit。現在位置は git を参照。昇格記録: doc93＋doc14 §59・2026-07-05）
- 詳細: `docs/audit/93_case_study_consent_save_condition_connection_production_confirmation.md`・doc14 §59
- （前基準: CaseStudyConsent UI = 許諾台帳UIの本番確認 GO。以下は当時の記録として保持）
- 内容: **許諾台帳UI（一覧・登録・閲覧・取り消しの4操作のみ・人間のみ・writeAudit・物理削除禁止・証跡は所在説明のみ・期限必須）の本番確認 GO 記録。CaseStudyConsent は AI 文脈へ注入しない・AI参照条件変更なし・anonymized=false 未解禁・突合判定は別承認**
- CaseStudyConsent UI 基準 commit（本番確認 GO 済み基準）: `1913456`（※現在 HEAD ではなく基準 commit。現在位置は git を参照。昇格記録: doc88＋doc14 §58・2026-07-05）
- 詳細: `docs/audit/88_case_study_consent_ui_production_confirmation.md`・doc14 §58
- （前基準: CaseStudyConsent schema = 許諾台帳の器の本番反映 GO。以下は当時の記録として保持）
- CaseStudyConsent schema 基準 commit（本番確認 GO 済み基準）: `812ae69`（昇格記録: doc85＋doc14 §57・2026-07-05）
- 詳細: `docs/audit/85_case_study_consent_schema_production_confirmation.md`・doc14 §57
- （前基準: Phase 2-C-5 = CaseStudy AI参照の本番確認 GO。以下は当時の記録として保持）
- 内容: **CaseStudy AI参照（ナレッジ検索での顧客事例参照＝匿名化済み anonymized=true のみ＋ai_reference（entityType=CaseStudy）監査ログ）の本番確認 GO 記録。Company Brain AI参照4テーブル体制が本番確認済み**
- Phase 2-C-5 基準 commit（本番確認 GO 済み基準）: `6d656a3`（※現在 HEAD ではなく基準 commit。現在位置は git を参照。昇格記録: doc80＋doc14 §55・2026-07-05）
- 本番確認: 利用者の Vercel Production / 本番画面実測による **GO（2026-07-05 申告・実測値はチャット提出・実測日は利用者申告値をそのまま記録）**。AI が本番接続確認したものではない。架空・匿名の事例1件作成→タイトルでナレッジ検索→「参照した会社の頭脳」に表示→`/admin/data-access-logs` に ai_reference（entityType=CaseStudy）→テスト事例アーカイブ片付け・既存画面無回帰・外部送信/SNS/口コミ/顧客の声公開なし。
- 詳細: `docs/audit/80_phase2c5_case_study_ai_reference_production_confirmation.md`・doc14 §55
- **Phase 2-C は完了判定済み（Phase 2-C-CLOSE・判定 GO・doc81＋doc14 §56・2026-07-05）**。ただし **ConsentRecord連携 / Customer Pain / Stage 2 / Stage 3 / ★2 / UX改善 は別承認**。
- Phase 2-C-4 基準 commit: `11e8f51`
- Phase 2-C-3 基準 commit: `408857d`
- Phase 2-C-2 基準 commit: `b012bd0`
- （前基準: Phase 2-B-5 = SalesPlaybookEntry AI参照の本番確認 GO。以下は当時の記録として保持）
- Phase 2-B-5 基準 commit: `83d35bc`
- 本番確認: 利用者の Vercel Production / 本番画面実測による **GO（2026-07-04 申告・実測値はチャット提出・実測日は利用者申告値をそのまま記録）**。AI が本番接続確認したものではない。本番UIで作成した「テスト1」→検索「test1」→「参照した会社の頭脳」表示→`/admin/data-access-logs` に AI参照（SalesPlaybookEntry）・既存画面無回帰・エラー/外部送信/SNS/口コミ/顧客の声公開なし。68点=FakeLLM 仕様・Supabase dashboard ログは無関係の可能性大（断定しない）。
- 詳細: `docs/audit/61_phase2b5_production_confirmation.md`・`docs/audit/14_release_stabilization.md` §50
- （前基準: Phase 2-B-4 = SalesPlaybookEntry 人間書き込みの本番確認 GO。以下は当時の記録として保持）
- Phase 2-B-4 基準 commit: `26a7a30`
- 本番確認: 利用者の Vercel Production / 本番画面実測による **GO（2026-07-04・実測値はチャット提出・実測日は利用者申告値をそのまま記録）**。AI が本番接続確認したものではない。作成→編集→アーカイブの1周 OK・機密ラベル2択のみ・外部AI送信を許可するUIなし・入力ガイド表示・監査ログに3操作すべて記録・既存画面無回帰・エラーなし・外部送信/SNS/口コミ/顧客の声公開なし。
- 詳細: `docs/audit/58_phase2b4_production_confirmation.md`・`docs/audit/14_release_stabilization.md` §49
- （前基準: Phase 2-B-3 = SalesPlaybookEntry read-only 可視化の本番確認 GO（一度 HOLD → 再実測で解消）。以下は当時の記録として保持）
- Phase 2-B-3 基準 commit: `a2bb2b6`
- 本番確認: 利用者の Vercel Production / 本番画面実測による **GO（2026-07-06・HOLD解消の再実測）**。AI が本番接続確認したものではない。HOLD の唯一のNG（ナビに「営業プレイブック」が出ない）は**ハードリロードで解消**（ブラウザキャッシュ起因の可能性が高い・repo側潔白確認済み・コード修正不要だった）。直打ちOK・空一覧=正常・ボタンなし・既存画面無回帰・エラーなし・外部送信なし。
- 詳細: `docs/audit/56_phase2b3_prod2_nav_recheck.md`・`docs/audit/14_release_stabilization.md` §48（HOLD の経緯は doc55・§47）
- （前々基準: Phase 2-B-2 = SalesPlaybookEntry schema変更の本番確認 GO。以下は当時の記録として保持）
- Phase 2-B-2 基準 commit: `811b8c6`
- 本番確認: 利用者実測による **GO（2026-07-05）**。詳細: `docs/audit/53_phase2b2_production_confirmation.md`・doc14 §46
- （前々基準: Phase 2-A-3c-2 = Company Brain AI参照の本番確認 GO（一度 HOLD → 再実測で解消）。以下は当時の記録として保持）
- Phase 2-A-3c-2 基準 commit: `85f1bf3`（`700f79e` は HOLD記録の docs commit）
- 本番確認: 利用者実測による **GO（2026-07-04・HOLD解消の再実測）**。詳細: `docs/audit/47_phase2a3c2_hold_resolution_go.md`・doc14 §44（HOLD の経緯は doc46・§43）
- （前々基準: Phase 2-A-3b-2 = ProductCatalogItem 書き込みの本番確認 GO。以下は当時の記録として保持）
- Phase 2-A-3b-2 基準 commit: `aa40f2f`
- 本番確認: 利用者実測による **GO（2026-07-04）**。詳細: `docs/audit/43_phase2a3b2_production_confirmation.md`・doc14 §42
- （前々基準: Phase 2-A-3b-1 = CompanyPolicy 書き込み＋安全補正の本番確認 GO。以下は当時の記録として保持）
- Phase 2-A-3b-1 基準 commit: `706358e`（実装 `9eea086`＋安全補正）
- 本番確認: 利用者実測による **GO（2026-07-04）**。詳細: `docs/audit/41_phase2a3b1_production_confirmation.md`・doc14 §41
- （前々基準: Phase 2-A-3a = Company Brain read-only 可視化の本番確認 GO（一度 HOLD → 再実測で解消）。以下は当時の記録として保持）
- Phase 2-A-3a 実装 commit: `9533488`
- 本番確認: 利用者実測による **GO（2026-07-03・HOLD解消の再実測）**。詳細: `docs/audit/38_phase2a3a_hold_resolution_go.md`・doc14 §40（HOLD の経緯は doc37・§39）
- （前々基準: Phase 2-A-2 = Company Brain schema 変更の本番確認 GO。以下は当時の記録として保持）
- Phase 2-A-2 実装 commit: `ca18450`
- 本番確認: 利用者の Vercel Production / 本番画面実測による **GO（2026-07-02）**。AI が本番接続確認したものではない。
- 詳細: `docs/audit/35_phase2a2_production_confirmation.md`・`docs/audit/14_release_stabilization.md` §38
- （前々基準: Phase 1-44 = Phase 1-43 `/admin/usage` の本番確認 GO。以下は当時の記録として保持）
- Phase 1-43 実装 commit: `ce858c7`
- Phase 1-44 完了基準 commit（本番確認 GO 記録）: `3e3409f`（※これは「現在 HEAD」ではなく Phase 1-44 の完了基準 commit。現在位置は git を参照）
- 本番確認: 利用者の Vercel Production `main` / CI / 本番画面確認による **GO（2026-07-01）**。AI が本番接続確認したものではない。
- 詳細:
  - `docs/audit/14_release_stabilization.md` §37
  - `docs/audit/15_monetization_usage_design.md` §33.1
  - `tasks/PROGRESS.md` Phase 1-43

## UsageEvent emit 対象（8種類・すべて本番 GO）

| # | 対象 | eventType | category | 本番GO |
|---|------|-----------|----------|--------|
| 1 | LeadMap export | `export.generated` | export | GO |
| 2 | AIOutput（apps/web） | `ai.output.generated` | ai | GO |
| 3 | admin danger-actions export | `export.generated` | export | GO |
| 4 | approvals outreach | `external_send.outreach` | external_send | GO |
| 5 | invoice-send | `external_send.invoice` | external_send | GO |
| 6 | dunning | `external_send.dunning` | external_send | GO |
| 7 | Webhook success | `webhook.delivered` | webhook | GO |
| 8 | worker 朝礼AI出力 | `ai.output.generated` | ai | GO |

- billing は全件 `usage_only`（**非課金記録**）。金額カラムなし。metadata は固定の非PIIキーのみ。
- 可視化は `/admin/usage`（read-only・audit:read ガード・tenantId スコープ・件数と quantity 合計のみ）。

## 現在の安全境界

- 課金なし
- 決済なし
- サブスクなし
- billable_candidate runtime 使用なし
- never_billable runtime 使用なし
- schema / migration / package / lock 変更なし
- 本番DB操作なし
- Prisma migrate 手動実行なし
- 外部送信なし
- 実メール送信なし
- Webhook 実送信なし
- worker / queue / outbox dispatch 手動実行なし

## Phase X（短期品質フェーズ）のタスク一覧（クローズ済み）

> **Phase X の残タスク表はクローズ**（完了記録は `docs/audit/32_phase_x_completion_record.md`・履歴は `tasks/PROGRESS.md`）。以下は完了実績の一覧。X-04 のみ任意候補として残る。

| Phase | 内容 | 状態 |
|-------|------|------|
| Phase X-01 | 本番スモーク / E2E / 検証基盤整理（`docs/audit/26_phase_x01_verification_baseline.md`・GO） | 棚卸し完了（反映状態は git refs を正とする） |
| Phase X-02 | E2E smoke 実行の実証（`docs/audit/27_phase_x02_e2e_smoke_result.md`・環境GREEN／smoke 11本RED・原因特定済み） | 実証完了（反映状態は git refs を正とする） |
| Phase X-RM-01 | 長期構想17領域の非破壊統合＋Phase 2 ロードマップ／Feature Registry／各種 Matrix 作成（`docs/roadmap/00〜08`・`docs/audit/28_long_term_strategy_integration.md`） | 統合完了（反映状態は git refs を正とする） |
| Phase X-RM-02 | Roadmap Review / Gap Reconciliation（追加構想リストとの突合・IKEZAKI MCP/API Gateway 表記統一・分類23項目と Enshin OS 表記ルールの明文化。`docs/audit/29_phase_x_rm_02_roadmap_review.md`） | レビュー完了（反映状態は git refs を正とする） |
| Phase X-03 | E2E smoke green 化（X-03: label関連付け＋X-03b: セレクタ明確化。`docs/audit/30_phase_x03_e2e_green.md`・**smoke 11/11 green**） | green化完了（反映状態は git refs を正とする） |
| Phase X-RM-03 | Phase 2 入口条件の最終確定（`docs/audit/31_phase_x_rm_03_phase2_entry_review.md`・**入口レビュー READY/GO・Phase 2-A 実装は人間承認待ち HOLD**） | 判定完了（反映状態は git refs を正とする） |
| Phase X-CLOSE | Phase X 完了記録（`docs/audit/32_phase_x_completion_record.md`・**Phase X 完了 GO**） | 記録完了（反映状態は git refs を正とする） |
| Phase 2-A | Company Brain foundation の設計準備（doc31 §5 準備メモあり。三段承認: 設計docs→schema→実装） | 候補（**人間の個別承認待ち**） |
| Phase X-04 | 本番スモーク定型化／検証準備 script 化／残り E2E 11スペックの段階実行 | プレイブック docs-only 完了（doc49・判定 GO）。script化・E2E拡張は別承認の残候補 |

## 次にやること（人間が選択）

1. **WIP2 push 後の CI 確定確認**: run のログ本文で **85 passed / 0 failed**・env fake/log/false を確認（push は GO#2 で実施済みの場合。未 push なら push GO から）。
2. **次の WIP 選定（人間判断・1件だけ・設計 docs-only から）**: 推奨 = **既存 /growth ページの集計値の権限ゲート**（金額を含むのに無ゲート・roadmap62 Unknown）。他候補 = Control Tower ページ自体の resource 権限ゲート／Customer 一覧の DataAccessLog／join 経路監査／Topbar 件数の need-to-know（roadmap63 §5）。
3. **369-vault の追随同期**: roadmap61-63 世代の知識ノート（本オートパイロットで反映済みの場合は増分のみ）。
4. **外部送信・実LLM・課金・本番 deploy・runtime 解禁・externalAiAllowed true・EXTERNAL_SEND_ENABLED true・状態永続化（dismiss/snooze/pin 等）は、各々個別の人間承認まで行わない**（封印は個別承認制を維持）。
5. **CI / Test / Release Governance 等の品質基盤強化（別承認）**。
6. **高機密ラベル実装・解禁は、最小実装設計後の個別人間承認**。
- いずれの場合も **外部LLM送信・高機密ラベル解禁・EXTERNAL_SEND_ENABLED true 解禁・Phase 8 実課金・ENSHiN OS 外部発信には、個別人間承認なしに進まない**。
- 旧候補・過去の判断履歴は各 audit / roadmap docs に温存し、CURRENT_STATE は現時点の次アクション候補を優先表示する。

## 今は絶対にやらないこと

- 実課金
- Stripe 等の決済連携
- billable_candidate / never_billable の runtime 使用
- usage cap / alert
- tenant 横断 usage dashboard
- raw metadata viewer
- 個人情報・本文・金額の表示
- schema / migration
- 外部送信
- AI 自動実行範囲の拡張
