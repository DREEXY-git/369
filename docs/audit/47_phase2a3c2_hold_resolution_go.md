# 47. Phase 2-A-3c-2 本番確認HOLD解消記録 — Company Brain AI参照（利用者再実測・判定 GO）

> docs-only / HOLD解消・再実測GOの記録。**コード・DB・schema・migration の変更は一切なし。**
> フェーズ: Phase 2-A-3c-2-PROD-2 / 現在位置は git refs を正とする。
> 対象実装 commit: `85f1bf3`（ナレッジ検索への Company Brain 参照＋ai_reference ログ。main 反映済み）。
> 前回HOLD記録: doc46＋doc14 §43（docs commit `700f79e`）— **消さず・上書きせず、本書で解消を記録する（追記主義）**。
> **判定: HOLD解消・再実測GO（2026-07-04・利用者実測）。**

---

## 1. 非エンジニア向け要約

- 前回（doc46）、「値引き承認ルール」で AI回答と「参照した会社の頭脳」セクションが本番で確認できず **HOLD** になりました。
- read-only 原因調査の結果、有力仮説は「**本番には seed 由来の『値引き承認ルール』データがそもそも存在しない**（＝故障ではなく前提データ差）」でした。
- 今回の再実測でこの仮説が**実測で裏付けられました**: 本番の会社方針一覧に「値引き承認ルール」は**存在せず**（NOT_PRESENT）、初回検索でも実は**既存ナレッジ由来の AI回答は表示されていた**ことが分かりました。
- 本番UI（GO済みの人間用機能）から会社方針「値引き承認ルール」を1件作成して再検索したところ、**AI回答・「参照した会社の頭脳」セクション・参照元タイトル・CompanyPolicy の ai_reference ログがすべて表示**されました。
- **判定: HOLD解消・再実測GO。コード修正は不要でした（バグではなくデータ前提差）。**

## 2. 前回HOLDの経緯（doc46・doc14 §43）

- 2026-07-04 の初回本番確認で「AI回答が表示されない」「参照セクションが表示されない」の2点NG → **HOLD**（GO済み基準は Phase 2-A-3b-2 / `aa40f2f` のまま保持）。
- read-only 原因調査（コード精読）で確定した設計挙動:
  - AI回答は検索データ0件でも必ず返る（FakeLLM は「見つかりませんでした」回答を返し、外部LLM失敗時も fake にフォールバック）。
  - 「参照した会社の頭脳」は参照候補0件のとき非表示が実装どおり。
  - 「値引き承認ルール」は seed（デモデータ）専用で、本番では seed 未実行のため存在しない可能性が高い。
- 結論仮説: 「本番データ不在 → セクション非表示は仕様どおり」「AI回答は実際には表示されていた可能性」→ 本再実測で検証。

## 3. 再実測記録（利用者実測・2026-07-04）

**本確認は利用者の Vercel 画面・本番画面の実測によるものであり、AI が本番接続確認したものではない。** AI は本番DB・本番環境に直接触っていない。

| 項目 | 利用者実測値 | 判定 |
|---|---|---|
| Vercel build / deploy | Ready / green | GO |
| Vercel latest deploy commit | `700f79e`（docs反映。アプリ実装は `85f1bf3` から不変） | GO |
| 再実測前の「値引き承認ルール」会社方針の有無 | **NOT_PRESENT（本番一覧に存在しなかった）** | 仮説どおり |
| 初回検索の実際の画面 | **既存ナレッジ由来の AI回答は表示されていた**（「ご質問『値引き承認ルール』について、社内ナレッジからの要点は次のとおりです。(1) 値引き運用のルール…」）。Company Brain 参照セクションのみ非表示 | 仮説どおり（仕様どおりの挙動） |
| 本番UIで会社方針「値引き承認ルール」を作成 | OK（GO済みの 2-A-3b-1 機能の通常利用。DB直接操作ではない） | GO |
| 作成後の再検索: AI回答 | **OK（表示された）** | **GO** |
| 「参照した会社の頭脳」セクション | **OK（表示された）** | **GO** |
| 参照元タイトル「値引き承認ルール」 | **OK（表示された）** | **GO** |
| 機密参照ログ（/admin/data-access-logs の CompanyPolicy ai_reference） | **OK（記録を確認）** | **GO** |
| 既存のナレッジ検索 | OK（従来どおり） | GO |
| 既存画面（会社の頭脳・顧客・LeadMap 等） | OK | GO |
| 外部送信・SNS投稿・口コミ投稿・顧客の声公開の不発生 | OK（**外部送信なし**） | GO |
| **総合判定** | | **HOLD解消・再実測GO** |

利用者メモ: Phase 2-A-3c-2 の再実測。初回は本番に「値引き承認ルール」会社方針が存在しなかったため、Company Brain参照セクションは出なかった。その後、本番UIで会社方針を作成して再検索したところ、AI回答・参照セクション・参照元タイトル・CompanyPolicy ai_referenceログがすべてOK。既存検索・既存画面もOK。外部送信・SNS投稿・口コミ投稿・顧客の声公開は発生していない。AIが本番接続確認したものではなく、利用者の本番画面実測による確認。

## 4. 原因の確定と本番データ前提の整理

- **前回HOLDの原因（実測で確定）**: 本番DBに Company Brain の対象データ（「値引き承認ルール」）が存在しなかったこと。参照候補0件 → セクション非表示は**実装どおりの挙動**であり、コードのバグではない。
- 「AI回答が表示されない」という前回報告は、実際には**既存ナレッジ由来の回答が表示されており**、期待した Company Brain 反映が無かったことの観測だったと整理できる（初回検索の画面文言を §3 に記録）。
- **教訓（vault にも記録）**: 本番確認は「本番に実在するデータ」で確認する手順にする。ローカルの seed（デモデータ）前提で本番確認項目を設計しない。
- コード修正・原因修正は**不要**（本記録は docs-only。`85f1bf3` 以降アプリコードは無変更）。

## 5. 変更していないもの・発生していないこと

- コード修正・schema / migration / rbac / labels / seed / package / lock: **無変更**
- 本番DB直接操作（AI・開発側による）: **なし**（会社方針の作成は利用者が本番UIのGO済み機能で実施）
- 外部LLM送信解禁・高機密ラベル対応・externalAiAllowed true UI: **なし**
- **ENSHiN OS 外部発信・口コミ投稿・SNS投稿・顧客の声公開・推薦コメント掲載: なし（外部送信なし）**
- **Phase 8（課金）: なし**・MCP/API公開なし

## 6. 判定と次アクション

- **判定: HOLD解消・再実測GO（Phase 2-A-3c-2 本番確認・2026-07-04・利用者実測）。**
- 「最新の本番確認GO済みプロダクト基準」を **Phase 2-A-3c-2 / `85f1bf3`** に更新する（`700f79e` は HOLD記録の docs commit であり、アプリ実装基準は `85f1bf3`）。
- 前基準 **Phase 2-A-3b-2 / `aa40f2f`** は履歴として保持する。doc46 / doc14 §43 の HOLD 記録も消さない（HOLD→解消の経緯が追える状態を維持）。
- 次アクション:
  1. 本GO記録（doc47 commit）の main 反映（push-only・別承認）。
  2. **Phase 2-A 全体クローズ判定 または 3c-5（外部LLM送信解禁等）の判断**（いずれも個別人間承認。勝手に進まない）。
- 参照: HOLD記録=doc46／原因調査=本セッションの read-only 調査報告／実装=doc45／設計=doc44／HOLD解消の前例=doc38（2-A-3a）。
