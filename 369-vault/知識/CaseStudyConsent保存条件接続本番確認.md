# CaseStudyConsent 保存条件接続 本番確認 — 台帳の裏付けなしでは実名寄り保存が通らないことを本番で実測（GO）

> 出典（正本）: `369` リポジトリ `docs/audit/93_case_study_consent_save_condition_connection_production_confirmation.md`（判定 GO）。本ノートはその要約。
> 関連: [[CaseStudyConsent保存条件接続実装]] / [[CaseStudyConsent保存条件接続設計]] / [[CaseStudyConsentUI本番確認]]

## 何を確認したか（利用者本人の実測・2026-07-05）

- 本番（Vercel Ready・対象 commit `5e9461f`・build/CI green）で、架空・匿名の事例を使って**保存条件接続の1周**を実測:
  1. **新規作成では匿名化を外せない**（「許諾あり」にしても拒否・案内表示あり）
  2. 台帳なしの状態では、編集でも**匿名化を外す保存が拒否される**（台帳登録が必要の案内）
  3. 許諾台帳に**社内閲覧（internal_view）の有効な許諾**を登録（証跡は所在説明のみ・期限入力）すると**保存が通る**
  4. 台帳を**取り消すと再び拒否**される（行は残り「取り消し済み」表示＝消せない台帳）
  5. テスト事例は匿名化ありに戻して**アーカイブで片付け**済み
- 外部AI送信・公開状態・公開/PR/SEO/SNS/顧客の声のUIが**ないこと**も確認。本番DBには触っていない。

## 判定

**GO**。本番確認GO済みプロダクト基準は **CaseStudyConsent UI（`1913456`）→ 保存条件接続（`5e9461f`）** へ昇格（前基準は歴史として保持）。
「許諾ありの申告だけで実名寄り保存が通る」暫定状態は、**本番でも解消されたことが実測で確認できた**。

## 変わらないこと

- **実名解禁ではない**（保存できるだけ・AIや公開には使われない）。
- **AIの読み方は変わらない**（AIが読む事例は匿名化済みだけ・許諾台帳をAIは読まない）。
- AI は Vercel / GitHub / 本番を直接確認していない（すべて利用者本人の実測値の記録）。

## 次の一手（すべて人間判断・別承認）

1. この記録（doc93）の push
2. anonymized=false の本格扱い判断（運用・表示・閲覧範囲）／AI参照条件の扱い判断
3. Customer Pain / Stage 2・3・★2・UX / 品質基盤強化 は随時

## 現在地

現在地は `369` の `tasks/CURRENT_STATE.md`＋git refs を正とする。
