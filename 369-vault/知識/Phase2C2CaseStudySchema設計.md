# Phase 2-C-2 CaseStudy Schema — 顧客事例の器ができた（中身と画面はまだ）

> 出典（正本）: `369` リポジトリ `docs/audit/72_phase2c2_case_study_schema.md`（判定 GO・schema-only）。本ノートはその要約。
> 関連: [[Phase2C1顧客事例課題詳細設計]] / [[Phase2C入口レビュー]] / [[Phase2B2SalesPlaybookSchema変更]] / [[Phase2A2Schema変更]]

## 何をしたか

- 顧客事例（Case Study）を保存する**データベースの器だけ**を追加した。画面・書き込み・AI参照はまだない（各段別承認）。
- 追加のみ・既存のテーブルやデータには一切触っていない（破壊的SQLゼロ・ローカル検証のみで本番には触れていない）。

## 器の時点で固定した安全

- **匿名化が既定値**（実名に近づけるには許諾の記録が前提・その制御は書き込み段で実装）
- **非公開が既定値**（公開機能そのものを作らない方針のまま）
- **外部AI送信は禁止が既定値**
- **許諾状態は「なし」から開始**（許諾なしに顧客名・取引先名・成果数値・顧客の声を扱わない）
- 顧客課題（Customer Pain)は後続のまま（高機密対応の重い承認が先）

## 検証

- 自動テスト216本・型・lint・ビルド・安全ゲートすべて green（ローカルのみ）。
- ブラウザテストは画面変更がないため未実施（成功扱いしない）。

## 現在地

現在地は `369` の `tasks/CURRENT_STATE.md`＋git refs を正とする。GO済み基準は Phase 2-B-5 / `83d35bc` のまま（本番確認前のため動かさない）。
