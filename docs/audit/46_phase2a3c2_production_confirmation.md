# 46. Phase 2-A-3c-2 本番確認記録 — Company Brain AI参照（利用者実測・判定 HOLD）

> docs-only / production confirmation record。**コード・DB・schema・migration の変更は一切なし。**
> フェーズ: Phase 2-A-3c-2-PROD / 現在位置は git refs を正とする。
> 対象 commit: `85f1bf3`（ナレッジ検索への Company Brain 参照＋ai_reference ログ実装。main 反映済み）。
> **判定: HOLD** — 既存機能は正常だが、AI参照の新機能2点が本番で確認できなかったため、GO とせず原因調査へ送る。

---

## 1. 非エンジニア向け要約

- Phase 2-A-3c-2 で main に反映した **AI参照機能（`85f1bf3`）の本番確認**を、あなた（利用者）の実測に基づいて記録します。
- 良い知らせ: **Vercel は Ready / green・最新コミット `85f1bf3`・ログイン・既存のナレッジ検索・既存画面はすべて正常**。今回の変更が既存機能を壊していないことは確認できました。
- 問題: **「値引き承認ルール」で検索しても AI回答が表示されず、「参照した会社の頭脳」セクション自体も表示されませんでした**。
- **判定: HOLD** — GO にはできません。原因は未特定のため、次は **read-only の原因調査**から始めます（コード修正はまだしません）。

## 2. 本番確認記録（利用者実測・2026-07-04）

**本確認は利用者の Vercel 画面・本番画面の実測によるものであり、AI が本番接続確認したものではない。** AI は本番DB・本番環境に直接触っていない。

| 項目 | 利用者実測値 | 判定 |
|---|---|---|
| Vercel build / deploy | Ready / green | GO |
| Vercel latest commit | `85f1bf3` | GO |
| 本番ログイン | OK | GO |
| ナレッジ検索「値引き承認ルール」の AI回答 | **NG（AI回答が表示されない）** | **NG** |
| 「参照した会社の頭脳」セクション | **NG（セクション自体が表示されない）** | **NG** |
| 参照元「値引き承認ルール」の表示 | **確認不可**（セクション未表示のため） | **NG** |
| 機密参照ログ（ai_reference） | OK（利用者報告「その他はOK」に含めて記録。詳細表示内容は未記述） | OK※ |
| 既存のナレッジ検索（通常質問） | OK（利用者報告「その他はOK」） | GO |
| 既存画面（会社の頭脳・顧客・LeadMap 等） | OK（利用者報告「その他はOK」） | GO |
| 外部送信・SNS投稿・口コミ投稿等の不発生 | OK（利用者報告「その他はOK」） | GO |
| 利用者メモ | Phase 2-A-3c-2 本番確認。Vercel Ready / green・latest commit 85f1bf3・ログインOK。ただし「値引き承認ルール」でAI回答が表示されず、「参照した会社の頭脳」セクション自体も表示されないため、判定はHOLD。その他は利用者実測でOK。AIが本番接続確認したものではなく、利用者の本番画面実測による確認です。 | — |
| **総合判定** | | **HOLD** |

※ ai_reference ログの OK は包括報告によるもの。参照セクションが未表示の状況と整合しない可能性があるため、原因調査時に再確認する。

## 3. 原因について（未特定・断定しない）

**原因は未特定**。次の read-only 原因調査で切り分ける。参考として、調査の入口になり得る観点のみ列挙する（いずれも仮説であり断定しない）:

- **本番DBのデータ前提**: seed は本番で自動実行されないため、**本番には「値引き承認ルール」という会社方針レコードが存在しない可能性が高い**（2-A-3b-1 本番確認時のテスト方針はアーカイブ済み＝doc41）。参照候補ゼロならセクション非表示は実装どおりの挙動であり、この部分は**バグではなく前提データ差**の可能性がある。
- ただし **「AI回答自体が表示されない」は参照候補ゼロでは説明できない**（実装上、回答は Company Brain 参照の有無に関係なく表示されるはず）。既存質問では回答が出ているとの報告のため、このクエリ固有の挙動・キャッシュ・デプロイ反映タイミング・本番環境のプロバイダ設定差などを含め、症状の再現条件を read-only で特定する必要がある。
- 調査では、まず本番での再実測（ハードリロード・別クエリとの比較・エラーメッセージ/ブロック表示の有無）と、repo 側の read-only 確認を行う。**DB・認証・RBAC・本番環境・Vercel 環境変数は変更しない。**

## 4. 変更していないもの・発生していないこと

- コード修正・原因調査のためのコード編集: **なし**（本記録は docs-only）
- schema / migration / rbac / labels / seed / package / lock: **無変更**
- 外部LLM送信解禁・高機密ラベル対応・externalAiAllowed true UI: **なし**
- **ENSHiN OS 外部発信・口コミ投稿・SNS投稿・顧客の声公開・推薦コメント掲載: なし**
- **Phase 8（課金）: なし**・MCP/API公開なし・本番DB操作（AIによる）なし

## 5. 判定と次アクション

- **判定: HOLD（Phase 2-A-3c-2 本番確認・2026-07-04・利用者実測）。**
- 「最新の本番確認GO済みプロダクト基準」は **Phase 2-A-3b-2 / `aa40f2f` のまま更新しない**。Phase 2-A-3c-2 / `85f1bf3` は「main反映済み・本番確認HOLD」の状態。
- 次アクション（優先順）:
  1. 本HOLD記録の main 反映（push-only・別承認）。
  2. **Phase 2-A-3c-2 の read-only 原因調査**（別ミッション。本番再実測＋repo側確認。コード修正はしない）。
  3. 原因特定後に必要なら最小修正 → 再実測 → GO記録（doc47 候補）。
- **GO記録の作成・原因修正・3c-5（外部LLM解禁等）・Phase 8 には進まない。**
- 参照: 実装=doc45／設計=doc44／HOLD前例=doc37（2-A-3a・キャッシュ起因で再実測GOになった事例）。
