# 158. P3-CT-5 承認導線 deep link 強化 設計＋実装前 Gate — docs/roadmap/59 の記録

## 1. 非エンジニア向け要約

- Control Tower（成長機会の見るだけ画面）に、**「人間の判断待ち」への案内リンク**を付ける設計です。①承認待ちの件数＋承認画面への入口（承認権限のある人にだけ表示）②送信待ちの営業メール下書き件数＋下書き一覧への入口（営業閲覧権限のある人に表示）。
- **この画面からは何も実行できません**。承認・却下・送信のボタンは置かず、リンクだけです。実行は今までどおり承認画面での人間の判断のみです。
- 実装前チェック（Gate A〜I）はすべて合格: 新しいデータベース・権限・デモデータは一切不要で、既存の仕組みだけで作れます。金額や個人情報は取得せず、件数だけを数えます。
- 変更予定は画面1ファイル＋自動テスト1ファイルのみ。テストは3件増えて 77→80 件の見込みです。

## 2. 判定

判定: **設計完了・Gate PASS（A〜I）・STOP 非該当**。新規送信なし・実行ボタンなし・状態永続化なし・schema/migration/RBAC/seed 変更なし・外部送信/実LLM/AIコスト/課金/本番なし・redaction 不変・PII 非増加・369-vault非編集。次は同一オートパイロット内で実装フェーズへ。
