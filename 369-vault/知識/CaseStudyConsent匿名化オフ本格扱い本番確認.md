# CaseStudyConsent 匿名化オフ本格扱い 本番確認 — 実名寄り事例の社内統治が本番で機能することを実測（GO）

> 出典（正本）: `369` リポジトリ `docs/audit/96_case_study_consent_anonymized_false_policy_production_confirmation.md`（判定 GO）。本ノートはその要約。
> 関連: [[CaseStudyConsent匿名化オフ本格扱い実装]] / [[CaseStudyConsent匿名化オフ本格扱い設計]] / [[CaseStudyConsent保存条件接続本番確認]]

## 何を確認したか（利用者本人の実測・2026-07-05）

- 本番（Vercel Ready・対象 commit `611e51e`・build/CI green）で、架空・匿名の事例を使って**実名寄り表示の1周**を実測:
  1. 許諾台帳に**社内閲覧（internal_view）の有効な許諾**を登録（証跡は所在説明のみ・期限入力）
  2. 許諾あり＋**匿名化オフの保存が通る**
  3. 一覧に「**実名寄り（許諾あり）**」「**AI参照対象外**」「**外部公開不可**」のバッジが表示される
  4. 編集画面に「**自動では匿名化に戻りません**」の注意が表示される
  5. **匿名化に戻して保存** → **アーカイブで片付け**済み（本番に実名寄りデータは残っていない）
- 外部AI送信・公開状態・公開/PR/SEO/SNS/顧客の声のUIが**ないこと**も確認。本番DBには触っていない。
- ※「編集権限のない人には閲覧制限表示になる」ことは、確認用ユーザーがいないため本番では未実測（仕組みとしては実装済み・自動テストと自動見張りで担保。今後ユーザーを用意すれば実測できる）。

## 判定

**GO**。本番確認GO済みプロダクト基準は **保存条件接続（`5e9461f`）→ 匿名化オフ本格扱い（`611e51e`）** へ昇格（前基準は歴史として保持）。
これで顧客事例の許諾ライン（設計→台帳→照合→保存の門番→見せ方の統治）は**全段が本番確認済み**になった。

## 変わらないこと

- **実名解禁ではない**（表示の統治まで。AIには読ませない・公開はしない）。
- **AIの読み方は変わらない**（AIが読む事例は匿名化済みだけ・許諾台帳をAIは読まない）。
- AI は Vercel / GitHub / 本番を直接確認していない（すべて利用者本人の実測値の記録）。

## 次の一手（すべて人間判断・別承認）

1. この記録（doc96）の push
2. AI参照条件の扱い判断／公開活用判断（今は進めない）
3. Customer Pain（高機密ラベル対応が先）／品質基盤強化 は随時

## 現在地

現在地は `369` の `tasks/CURRENT_STATE.md`＋git refs を正とする。
