# P3-CT-4 FakeLLM 下書き生成 — 「AI 下書きメモ」ボタンを設計→Gate→実装の三段で作った回（Gate PASS・実装完了）

> 出典（正本）: `369` リポジトリ `docs/audit/153_p3_ct4_control_tower_fake_llm_draft_design.md`（判定 設計完了）＋ `docs/audit/154_p3_ct4_control_tower_fake_llm_draft_preimplementation_gate.md`（判定 Gate PASS）＋ `docs/audit/155_p3_ct4_control_tower_fake_llm_draft_implementation.md`（判定 実装完了）。roadmap正本は `docs/roadmap/54`〜`56`。本ノートはその要約。
> 関連: [[P3CT3監査ログ配線]] / [[P3CT4完全クローズとP3CT5準備]]

## これは何か

- Control Tower（成長機会の見るだけ画面）の各カードに、**「AI 下書きメモを作る」ボタン**を付けた作業です。権限のある人がボタンを押すと、「次に何を考えるべきか」のたたき台メモを AI（無料の疑似 AI = FakeLLM）が作り、画面下に**下書きとして**表示します。
- **設計（doc153）→ 実装前 Gate 最終安全点検（doc154）→ 実装（doc155）の三段**で進めました。設計と Gate は docs-only（コード1行も変えない）で、Gate で「既存の DB 設計・権限・デモデータだけで作れる」ことを実物のコードで確認してから実装に入りました。

## どんな作りか（やさしい言い換え）

- **AI は提案するだけ**。メールは送りません・承認しません・削除しません・請求や契約を確定しません。メモ本文にも「送信・承認・実行は行いません」と明記されます。
- 使う AI は FakeLLM（無料の疑似 AI）または固定文言のみ。実 AI・AIコスト・課金はありません。
- 生成メモは既存の「AI 出力の帳簿（AIOutput）」に**必ず下書き**として保存。リード宛の営業文が必要なときは既存の LeadMap 下書き機能（承認後のみ送信）へリンクで誘導するだけで、新しい送信経路は作りません。
- ボタンを押せるのは**人間だけ**（AI ロールは砦関数で拒否）。財務が伏せられた担当者の金額系カードでは**ボタン自体が出ず、裏口からのリクエストもサーバー側で再拒否**（二重防御）。
- AI への入力はカード名・優先度・件数などの安全な項目だけ。**金額実値・顧客の個人情報・秘密情報は渡しません**。
- 帳簿: 生成1回につき重要操作の帳簿（ai_run）1件＋AI 出力の標準保存＋利用量の帳簿（ai.output.generated が自動1件・課金なし）。閲覧の帳簿は [[P3CT3監査ログ配線]] のまま。
- 変えたコードは3ファイルだけ（生成のサーバー処理を新規1・画面1・自動テスト1）。

## push 前レビューでの重要な発見（doc155 §7）

- push する前に **6視点の独立 AI レビュー（15エージェント）**で総点検したところ、**high 1件の見落としを発見**: 社長が金額系カードの下書きメモを作ると、メモ本文に「未回収・延滞の案件が N 件」という**財務件数**が入り、それが**財務権限のない担当者にもメモ表示経由で見えてしまう**穴。
- **push 前に修正済み**: 財務権限のない人には金額系カード由来のメモを表示しないフィルタを追加（見る側の二重防御）。この穴を CI で恒久的に見張る自動テストも追加し、e2e は **74→77件**に。未 push の段階で発見・修正したため外部への影響はゼロ。

## 変わらない約束

- DB 設計・権限・デモデータ・パッケージは一切変更なし（新しいテーブルや権限が必要になったら即 STOP して人間承認、の条件も明文化）。
- 外部送信・実LLM・AIコスト・課金・本番反映・SNS/口コミ/Googleレビュー投稿なし。担当者への金額隠し（redaction）不変・個人情報非増加。
- 検証: 単体テスト 278 件すべて合格・型/lint/安全チェック緑。E2E 実走は push 後 CI で確認。

## 次の一手（すべて人間判断・別承認）

1. P3-CT-4 実装 push-only（main へ push しない・force なし）
2. push 後 CI を read-only 確認し、全件合格と Control Tower 専用テストの緑をログ本文で確認
3. 緑なら P3-CT-5 設計へ（外部送信・実LLM・課金・本番・状態永続化は引き続き個別承認制）

## 現在地

現在地は `369` の `tasks/CURRENT_STATE.md`＋git refs を正とする。
